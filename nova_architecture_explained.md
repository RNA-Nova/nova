# Nova 架构逐层详解（投资人 + 技术专家版）

> 本文档详细解释 Nova 四层架构中每一层的设计目标、核心能力和技术亮点，帮助你在投资人路演和技术尽调中清晰表达。

---

## 一、Nova 四层架构总览

```
┌─────────────────────────────────────────┐
│  第 4 层：Agent Bundle（场景应用层）      │
│  coding_agent / 靶点调研 / 自动化实验     │
├─────────────────────────────────────────┤
│  第 3 层：nova_harness（高阶 SDK 层）     │
│  会话树 · 上下文压缩 · 包管理 · JSON-RPC  │
├─────────────────────────────────────────┤
│  第 2 层：nova_agent（Agent 运行时层）    │
│  Agent 类 · agent_loop · 工具执行 · 事件  │
├─────────────────────────────────────────┤
│  第 1 层：nova_ai（模型抽象层）           │
│  多模型适配 · API 注册表 · 统一流式调用   │
└─────────────────────────────────────────┘
```

**设计哲学**：每一层只依赖下层，上层为下层提供场景化封装。底层变化（如新模型、新 API）不影响上层业务逻辑。

---

## 二、第 1 层：nova_ai — 模型抽象层

### 2.1 为什么要这一层？

大模型 API 百花齐放：OpenAI、Anthropic、Google、Moonshot、DeepSeek、本地 vLLM/Ollama 等，每家接口、消息格式、鉴权方式都不一样。

如果没有抽象层，每次换模型都要改大量代码。`nova_ai` 让上层只关心"用哪个模型"，不关心"怎么调 API"。

### 2.2 核心组件

| 组件 | 职责 | 投资人能听懂的说法 |
|------|------|-------------------|
| **ApiRegistry** | 管理不同 API 协议的适配器 | "像插排一样，不同模型插头都能插进来" |
| **ModelRegistry** | 管理模型静态数据与动态配置 | "模型的身份证和参数表" |
| **统一调用接口** | `stream()` / `complete()` | "不管底层是哪家模型，调用方式都一样" |
| **消息类型系统** | Context、Message、AssistantMessage | "统一的语言，让不同模型能互相理解" |
| **流式事件** | AssistantMessageEventStream | "模型生成内容不是一次性返回，而是像打字一样流出来" |

### 2.3 技术亮点

1. **适配器模式**
   - 每个模型厂商实现一个 `ApiAdapter`。
   - 适配器负责：鉴权、请求构造、响应解析、流式转换。
   - 新增模型只需新增适配器，上层零改动。

2. **模型即配置**
   - 模型以 `Model` 对象描述，包含 provider、model id、context window 等。
   - 支持运行时切换模型，实现模型路由与降级。

3. **国产模型与本地模型友好**
   - 已预留 Volcengine 等国产模型数据。
   - 抽象层设计支持本地推理服务（vLLM、Ollama、Xinference）。

### 2.4 投资人常问的问题

**Q：你们支持哪些模型？**

> 架构上支持任何提供 API 的模型。当前 OpenAI 实现最完整，国产模型和本地模型在按优先级接入。

**Q：为什么要模型中立？**

> 三个原因：一是避免被单一云厂商绑定；二是可以按成本和性能选择模型；三是生物数据敏感时必须在本地部署。

---

## 三、第 2 层：nova_agent — Agent 运行时层

### 3.1 为什么要这一层？

这是 Nova 的**发动机**。`nova_agent` 负责让 Agent 真正"动起来"：接收输入、调用模型、执行工具、管理状态、发布事件。

### 3.2 核心组件

| 组件 | 职责 | 投资人能听懂的说法 |
|------|------|-------------------|
| **Agent 类** | 状态管理、事件订阅、消息队列 | "Agent 的躯体，保存记忆和状态" |
| **agent_loop** | 主循环：模型调用 → 工具执行 → 结果反馈 | "Agent 的大脑决策循环" |
| **工具执行器** | 校验参数、调用工具、处理结果 | "Agent 的手，能操作文件、命令、数据库" |
| **事件系统** | 每一步操作都发射事件 | "Agent 做任何事都会广播，外部可以监听和干预" |
| **AbortSignal** | 支持取消正在运行的任务 | "紧急制动系统" |

### 3.3 agent_loop 是怎么工作的？

这是投资人或技术专家最可能追问的部分。核心流程如下：

```
用户输入
  ↓
TurnStartEvent（开始新一轮）
  ↓
调用 LLM 生成响应（流式）
  ↓
检查响应中是否包含 tool calls
  ├─ 没有 → 直接返回给用户
  └─ 有 → 进入工具执行阶段
        ↓
ToolExecutionStartEvent（工具开始）
  ↓
校验工具参数（jsonschema）
  ↓
执行工具（可并行/可串行）
  ↓
ToolExecutionEndEvent（工具结束）
  ↓
工具结果写回上下文
  ↓
再次调用 LLM
  ↓
循环直到没有 tool calls 或满足停止条件
  ↓
TurnEndEvent / AgentEndEvent
```

### 3.4 工具是怎么被调用的？

工具调用流程是 Agent 框架的核心能力之一：

1. **工具注册**：每个工具通过 `AgentTool` 描述，包含名称、描述、参数 schema、执行函数。
2. **模型决定调用**：LLM 根据当前上下文决定调用哪个工具、传入什么参数。
3. **参数校验**：`nova_agent` 用 jsonschema 校验参数是否符合 schema，防止模型传错参数。
4. **Hook 机制**：
   - `before_tool_call`：执行前可以拦截、修改参数或拒绝执行。
   - `after_tool_call`：执行后可以修改结果、记录日志。
5. **执行模式**：
   - **并行**：多个独立工具同时执行，提升效率。
   - **串行**：有依赖关系的工具按顺序执行。
6. **结果回传**：工具执行结果以 `toolResult` 消息形式写回上下文，供下一轮 LLM 推理使用。

### 3.5 事件驱动有什么好处？

传统框架（如 LangChain 的 Chain）是"黑盒"：你输入，它输出，中间发生了什么很难知道。

Nova 的事件驱动设计让 Agent 的每个关键环节都发出事件：

- `AgentStartEvent`：Agent 开始运行
- `TurnStartEvent`：新一轮对话开始
- `MessageStartEvent` / `MessageUpdateEvent` / `MessageEndEvent`：消息生成过程
- `ToolExecutionStartEvent` / `ToolExecutionEndEvent`：工具调用过程
- `AgentEndEvent`：Agent 结束运行

**价值**：

1. **可观测**：可以实时监控 Agent 在做什么。
2. **可干预**：可以在工具执行前拦截、修改或拒绝。
3. **可审计**：完整记录 Agent 决策路径，满足合规要求。
4. **可扩展**：外部系统可以订阅事件，构建 UI、日志、安全策略等。

### 3.6 投资人常问的问题

**Q：什么是 agent loop？**

> agent loop 是 Agent 的核心决策循环。简单说，就是"模型思考 → 决定调用工具 → 执行工具 → 观察结果 → 再思考"的循环，直到任务完成。

**Q：工具调用安全吗？**

> 我们有参数校验和 before/after hook。比如模型想执行 `rm -rf /`，hook 可以识别并拒绝。这对生物研发很重要，因为实验设备不能乱动。

**Q：事件驱动和 LangChain 的链式有什么区别？**

> LangChain 的 Chain 像一条流水线，数据单向流动，中间状态不透明。Nova 的事件驱动像一台有仪表盘的发动机，每个部件运转都实时可见、可干预。

---

## 四、第 3 层：nova_harness — 高阶 SDK 层

### 4.1 为什么要这一层？

`nova_agent` 提供了 Agent 运行的核心能力，但要用于真实研发场景，还需要：

- 管理长期会话（不是一轮对话就结束）
- 控制上下文长度（避免 token 爆炸）
- 管理工具/Agent 的版本与分发
- 与前端/UI 通信
- 项目级安全控制

`nova_harness` 就是把这些能力封装起来。

### 4.2 核心组件

| 组件 | 职责 | 投资人能听懂的说法 |
|------|------|-------------------|
| **AgentSession** | 会话运行时核心 | "一个长期运行的 Agent 工作空间" |
| **会话树（Tree）** | 支持分支、fork、导航、摘要 | "对话不是直线，而是可以分叉的树" |
| **上下文压缩（Compaction）** | 自动/手动压缩历史消息 | "把长篇对话压缩成摘要，省钱省 token" |
| **包管理（nova-pkg）** | Agent/Tool/Extension 的 bundle 化 | "像 app store 一样安装和管理 Agent 组件" |
| **JSON-RPC 服务器** | 与 TUI 前端通信 | "后端和界面的通信协议" |
| **Project Trust** | 项目级安全门控 | "确认是否信任当前项目目录" |

### 4.3 会话树有什么用？

传统 ChatGPT 式对话是线性的：你说一句，AI 回一句，错了只能重开。

Nova 的会话树让对话可以**分支**：

```
用户提问 A
    ├── AI 回答 B（分支 1）
    ├── AI 回答 C（分支 2）
    └── 用户修改后得到 D（分支 3）
```

**价值**：

1. **实验性探索**：可以同时尝试多种思路，比较结果。
2. **错误回滚**：走错了可以回到任意节点重新来。
3. **知识沉淀**：分支可以生成摘要，成为可复用的知识。

对生物研发的意义：靶点调研中，一个假设可能有多个证据分支，会话树正好支持这种探索式工作。

### 4.4 上下文压缩是怎么工作的？

当 Agent 对话越来越长，token 消耗会指数级增长。Nova 提供两种压缩：

1. **手动压缩**：用户或系统触发，把历史对话总结成摘要。
2. **自动压缩**：当上下文达到阈值或溢出时自动触发。

压缩过程：

```
历史消息 → 选择保留窗口 → 用模型生成摘要 → 用摘要替换旧消息 → 继续对话
```

**技术细节**：

- 保留最近 N 条消息（保留窗口）。
- 对更早的消息生成摘要。
- 摘要作为系统消息或特殊消息保留在上下文中。
- 支持扩展 hook，可以自定义压缩策略。

**场景化压缩**：不同任务类型可以配置不同的压缩策略。例如：

- coding：保留代码修改点和错误，压缩编译输出。
- 文献调研：保留实体关系与结论，压缩原始摘要。
- 实验记录：保留协议与异常，压缩常规传感器数据。

### 4.5 nova-pkg 是什么？

`nova-pkg` 是 Nova 的包管理器，让 Agent、工具、扩展可以以 bundle 形式打包、安装和分发。

一个 bundle 包含：

- `agent.yaml`：Agent 元数据、工具白名单、扩展白名单。
- `schema.json` + `executor.py`：工具定义与执行逻辑。
- `description.md`：描述文档。

**价值**：

1. **复用**：一个实验协议可以打包成工具，多个项目复用。
2. **版本管理**：bundle 可以更新、回滚。
3. **生态**：未来可以形成内部或公开的 Agent 市场。

### 4.6 投资人常问的问题

**Q：会话树是不是过度设计？**

> 对普通聊天可能是，但对生物研发这种探索性工作非常关键。靶点调研中一个靶点可能有多条证据链，分支能力让研究员可以并行探索多个假设。

**Q：压缩会不会丢失重要信息？**

> 普通压缩会。但我们的压缩是场景感知的，可以配置保留什么、丢弃什么。而且压缩前会保留窗口，最近的关键信息不会丢。

**Q：nova-pkg 和 npm/pip 有什么区别？**

> npm 管理代码包，pip 管理 Python 包，nova-pkg 管理的是 Agent、工具、扩展这些可执行知识单元。

---

## 五、第 4 层：Agent Bundle — 场景应用层

### 5.1 为什么要这一层？

底层框架再强，最终也要落地到具体场景。Agent Bundle 就是把框架能力封装成解决具体问题的 Agent。

更重要的是，**Bundle 化让"专业的人干专业的事"**：

- 框架工程师负责底层运行时和 SDK；
- 生物信息学家负责定义靶点分析的逻辑和工具；
- 实验专家负责定义实验协议和设备交互；
- 领域专家不需要理解 `agent_loop` 或事件系统，只需要用 `agent.yaml` 和工具 schema 描述自己的专业知识。

这样，**真正懂生物的人能做出更专业的生物 Agent**，而不是让通用工程师去猜测生物场景的需求。

### 5.2 当前 Bundle

| Bundle | 用途 | 状态 |
|--------|------|------|
| **coding_agent** | 编程辅助：读代码、改代码、执行命令 | 已可用，验证框架能力 |
| **靶点调研 Agent** | 文献检索、实体抽取、证据汇总 | 开发中 |
| **自动化实验 Agent** | 设备调度、协议执行、异常记录 | 开发中 |
| **生物信息学分析 Agent** | Pipeline 编排、长序列压缩、结果追溯 | 开发中 |

### 5.3 Bundle 的结构

```
agents/coding_agent/
├── agent.yaml          # Agent 元数据
├── description.md      # 描述
└── sections/
    ├── role.md         # 角色定义
    └── setup.md        #  setup 说明

tools/bash/
├── schema.json         # 工具参数 schema
└── executor.py         # 工具执行逻辑
```

**关键设计**：

- `agent.yaml` 让领域专家用声明式方式定义 Agent。
- `schema.json` 让工具输入输出标准化。
- `executor.py` 让工具实现与框架解耦。

### 5.4 投资人常问的问题

**Q：为什么先做 coding_agent？**

> coding 是验证框架能力的理想场景：工具调用频繁、上下文长、结果可验证。我们通过 coding_agent 验证了场景化压缩和事件驱动机制，再把能力迁移到生物场景。

**Q：生物 Agent 和 coding Agent 有什么本质区别？**

> 底层运行时一样，但工具集、压缩策略、系统提示词不同。就像同一台发动机，可以装在不同车上。

**Q：Bundle 化有什么独特价值？**

> 它让领域专家不需要懂框架底层，也能定义专业 Agent。我们的生物信息学家可以写靶点分析工具，实验专家可以写设备协议工具，框架团队只负责提供运行时。这样产出的 Agent 更专业，也更容易迭代。

---

## 六、四层如何协同工作

以一个靶点调研任务为例：

```
1. 用户在 TUI 输入："调研 BRCA1 相关靶点"
            ↓
2. nova_harness 创建 AgentSession，加载靶点调研 bundle
            ↓
3. nova_agent 启动 agent_loop，发射 AgentStartEvent
            ↓
4. nova_ai 调用 LLM（可能是本地模型）生成响应
            ↓
5. LLM 决定调用文献检索工具
            ↓
6. nova_agent 校验参数并执行工具，发射 ToolExecutionStartEvent
            ↓
7. 工具返回文献列表，写入上下文
            ↓
8. LLM 再次生成响应，提取实体关系
            ↓
9. 上下文过长时，nova_harness 触发场景化压缩
            ↓
10. 用户可以选择分叉探索另一个靶点，会话树记录分支
            ↓
11. 最终结果生成调研报告
```

---

## 七、与竞品的架构对比

| 维度 | Nova | Kimi Code / Codex CLI | OpenClaw | LangChain |
|------|------|----------------------|----------|-----------|
| **架构层次** | 4 层清晰分层 | 终端应用，内部架构不透明 | Channel + Agent Core + Skill | 组件化组合 |
| **模型层** | 抽象层，多模型支持 | 绑定单一厂商 | 支持多模型 | 支持多模型 |
| **运行时** | 自研事件驱动 agent_loop | 内置黑盒循环 | 消息驱动循环 | Chain/Graph 执行 |
| **工具执行** | 校验 + Hook + 并行/串行 | 内置工具 | Skill 插件 | Tool 装饰器 |
| **会话管理** | 会话树 + 压缩 | 线性会话 | 会话隔离 | 简单 Memory |
| **可观测性** | 全链路事件 | 有限 | 有限 | 有限 |
| **本地部署** | ✅ 支持 | ❌ 弱 | ✅ 本地优先 | ⚠️ 需配置 |
| **垂直扩展** | ✅ Bundle 化 | ❌ coding 为主 | ⚠️ Skill 偏个人助理 | ⚠️ 需大量定制 |

---

## 九、未来能力：Memory 与 安全沙箱

除了当前已实现的四层架构，Nova 的技术路线图中还有两项关键能力正在规划：**长期记忆（Memory）**和**安全沙箱（Sandbox）**。这两项不是已有功能，而是框架演进的重要方向。

### 9.1 长期记忆（Memory）

#### 为什么需要？

当前 Agent 的"记忆"主要依赖会话上下文。一旦开启新会话，之前的知识就丢失了。对于生物研发这种长期项目：

- 一个靶点可能持续研究数月；
- 实验结论需要跨会话复用；
- 研究员的个人偏好和项目规范需要被记住。

因此需要**跨会话的长期记忆系统**。

#### 设计思路

| 记忆类型 | 作用 | 示例 |
|----------|------|------|
| **事实记忆** | 存储长期不变的知识 | 某个基因的功能、某条通路的结论 |
| **程序记忆** | 存储工作方式和偏好 | 用户喜欢的报告格式、实验协议版本 |
| **情景记忆** | 存储历史会话摘要 | 上次调研到一半的靶点 |
| **工作记忆** | 当前会话的活跃上下文 | 正在进行的实验任务 |

#### 在 Nova 中的位置

```
AgentSession（当前会话上下文）
        ↑
   Memory Layer（长期记忆层）
        ↑
   Vector Store / Knowledge Graph / 本地文件
```

Memory 层将作为 `nova_harness` 的扩展模块：

- 在 Agent 运行前，从记忆中检索相关知识；
- 在 Agent 运行后，把重要结论写回记忆；
- 支持按项目、按用户、按实体组织记忆。

#### 对生物研发的价值

- 靶点调研的阶段性结论可以跨会话累积；
- 实验失败/成功的经验可以被记住并复用；
- 新加入的研究员可以继承项目记忆，降低 onboarding 成本。

---

### 9.2 安全沙箱（Sandbox）

#### 为什么需要？

Agent 可以执行代码、调用工具、访问文件。如果不加限制，可能带来严重风险：

- 误删重要实验数据；
- 执行恶意代码；
- 越权访问敏感系统；
- 对实验设备发出危险指令。

特别是在生物实验室场景，Agent 可能控制真实设备，安全沙箱是**刚需**。

#### 设计思路

| 安全层级 | 机制 | 作用 |
|----------|------|------|
| **工具白名单** | Agent 只能调用明确授权的工具 | 防止越权 |
| **文件系统隔离** | 工具只能在指定目录读写 | 防止误删/泄露 |
| **网络隔离** | 禁止或限制网络访问 | 防止外联 |
| **执行沙箱** | 代码在 Docker / 虚拟机中运行 | 防止恶意代码破坏宿主 |
| **审批流程** | 高风险操作需人工确认 | Human-in-the-Loop |
| **审计日志** | 所有操作完整记录 | 事后追溯 |

#### 在 Nova 中的位置

```
用户输入
  ↓
nova_agent agent_loop
  ↓
安全策略层（Policy Layer）
  ├─ 工具权限检查
  ├─ 文件路径校验
  ├─ 网络访问控制
  └─ 沙箱执行环境
  ↓
实际工具执行
```

安全沙箱将作为 `nova_agent` 工具执行层和 `nova_harness` Project Trust 的扩展：

- 每个工具声明所需权限；
- 每个 AgentSession 配置安全策略；
- 扩展可以通过事件参与安全裁决（类似 Project Trust 的已有机制）。

#### 对生物研发的价值

- 防止 Agent 误操作实验设备；
- 保护敏感的基因组、化合物数据；
- 满足生物医药行业的合规与审计要求；
- 让 Agent 在受控环境中尝试多种假设。

---

### 9.3 多前端生态（飞书 / 钉钉 / 企业微信 / Web）

#### 为什么需要？

当前 Nova 的前端是终端 TUI（`nova-tui`），同时**已经通过 WebSocket 支持多前端接入**。企业内部不同角色可以在熟悉的入口中使用 Agent：

- **飞书/钉钉/企业微信**：国内企业内部沟通主入口，用户无需切换应用。
- **Web 界面**：适合非技术同事（如项目经理、实验主管）查看报告和审批任务。
- **邮件/日历集成**：Agent 可以通过邮件发送报告、通过日历安排实验。
- **TUI**：适合研究员进行复杂分析。

对生物科技公司而言，研究员、实验员、管理层对 Agent 的交互方式完全不同，多前端生态是扩大使用范围的关键。

#### 架构基础

`nova_harness` 提供 **WebSocket 接口**，实现前后端完全解耦：

```
飞书 Bot ──┐
钉钉 Bot ──┤
企业微信 ──┼──→ WebSocket ──→ nova_harness ──→ Agent Runtime
Web UI ────┤
TUI ───────┘
```

新增一个前端只需：

1. 通过 WebSocket 连接到 `nova_harness`。
2. 适配各平台的 UI 形态（消息卡片、审批按钮、文件上传等）。
3. 处理平台特定的鉴权、权限、消息格式。

所有前端共享同一套 Agent 运行时、同一套事件日志、同一套安全策略。

#### 对生物研发的价值

- **研究员**：用 TUI 做复杂分析。
- **实验员**：用企业微信接收设备异常提醒。
- **管理层**：用 Web 查看靶点调研报告和实验进度。
- **合规审计**：所有前端共享同一套事件日志。

---

### 9.4 如何向投资人表达这三个"饼"

**关键原则**：诚实地说这是规划中的能力，但强调它们的战略价值和架构上的可扩展性。

#### Memory 的话术

> "当前 Nova 的'记忆'主要依赖单一会话上下文。我们正在规划跨会话的长期记忆层，让 Agent 能够记住项目级知识、用户偏好和历史结论。这对生物研发非常关键——一个靶点的调研可能持续数月，结论需要被持续积累和复用。"

#### 安全沙箱的话术

> "Agent 能执行代码和调用设备，就必须有安全边界。我们正在设计分层安全沙箱：工具白名单、文件系统隔离、执行环境沙箱、操作审计。在生物实验室场景，这是让 Agent 真正可控运行的前提。"

#### 多前端生态的话术

> "Nova 的后端通过 WebSocket 与前端解耦。我们已经有 TUI，并且可以无缝接入飞书、钉钉、企业微信、Web 等多种前端。这意味着研究员可以用 TUI 做复杂分析，实验员可以在企业微信里接收设备异常提醒，管理层可以用 Web 查看报告——同一套 Agent 能力，服务不同角色。"

#### 一句话总结

> "Memory 让 Agent 越用越聪明，Sandbox 让 Agent 越用越安全，多前端生态让 Agent 触达企业的每个角落。"

---

## 十、给投资人的一句话总结

**Nova 的四层架构本质上做了三件事：**

1. **屏蔽模型差异**（nova_ai）：让企业不被单一模型厂商绑定。
2. **掌控 Agent 运行时**（nova_agent）：让每一步决策都可观测、可干预、可审计。
3. **支撑长期复杂任务**（nova_harness）：让 Agent 从一次性对话工具变成可分支、可压缩、可复用的研发工作空间。

**已支持能力：**

- **多前端生态**：后端通过 WebSocket 解耦，已支持 TUI，可接入飞书、钉钉、企业微信、Web 等前端。

**未来两项关键能力：**

- **Memory**：让 Agent 拥有跨会话的长期记忆，持续积累项目知识。
- **Sandbox**：让 Agent 在受控环境中安全执行，满足生物医药合规要求。

**最终价值**：

> Nova 不是某个具体应用，而是让我们能够持续构建、管理、优化各类科学计算 Agent 的**研发操作系统**。短期它加速内部研发，长期它可能成为生命科学 Agent 基础设施的行业标准。
