# Nova 一页 PPT 投资人介绍指南（生物科技 + 专业框架版）

> 这一版强调 Nova 作为自研 Agent 框架的专业性，同时锚定生物科技场景。让投资人看到：团队懂框架、懂架构、不是在 LangChain 上套壳。

---

## 一、核心叙事逻辑

```
我们不是在做生物应用 → 我们是在自研 Agent 操作系统
                            ↓
                  现有操作系统（LangChain 等）不满足生物场景
                            ↓
        所以我们从零设计了分层、事件驱动、场景感知的框架
                            ↓
          先验证 coding 场景，再扩展至生物信息/自动化实验/靶点调研
```

投资人要get到的关键信息：

1. **我们是框架团队**，不是应用团队。
2. **现有框架有结构性缺陷**，我们无法在它们之上做出符合生物场景要求的产品。
3. **Nova 的架构是重新设计的**，分层、事件驱动、模型解耦、场景化压缩。
4. **coding 场景是验证场**，生物场景是主战场。

---

## 二、推荐页面布局

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│   Nova —— 自研 AI Agent 运行时框架                             │
│   分层 · 事件驱动 · 场景感知 · 模型解耦                        │
│                                                             │
├──────────────────────────┬──────────────────────────────────┤
│                          │                                  │
│   为什么自研框架？        │   Nova 框架架构                   │
│                          │                                  │
│   · LangChain 等框架      │         ┌──────────────┐         │
│     抽象过重、难定制      │         │  Agent Bundle │         │
│                          │         │  coding / bio │         │
│   · 通用框架不懂科学场景  │         └──────┬───────┘         │
│     上下文成本高          │                │                 │
│                          │         ┌──────▼───────┐         │
│   · 生物数据不能上云      │         │ nova_harness │         │
│     需要本地/私有化部署   │         │ 会话树·压缩·包管理│       │
│                          │         └──────┬───────┘         │
│   · 长文本·多步骤·工具链  │         ┌──────▼───────┐         │
│     需要场景化优化        │         │ nova_agent   │         │
│                          │         │ 事件驱动运行时 │        │
│                          │         └──────┬───────┘         │
│                          │         ┌──────▼───────┐         │
│                          │         │   nova_ai    │         │
│                          │         │ 多模型统一抽象 │        │
│                          │         └──────────────┘         │
│                          │                                  │
├──────────────────────────┴──────────────────────────────────┤
│                                                             │
│   核心差异：场景化上下文压缩 │ 模型层解耦 │ 事件驱动可观测 │ Bundle 化分发 │
│   进展：Alpha 已发布 │ coding 压缩策略验证 │ 扩展生物场景中            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、推荐文案（可直接使用）

### 大标题

**Nova —— 自研 AI Agent 运行时框架**

### 副标题

**分层 · 事件驱动 · 场景感知 · 模型解耦**

### 一句话价值主张

**为复杂科学计算场景重新设计的 Agent 基础设施，让生物研发从手工脚本演进为可复用、可观测、可本地部署的智能体工作流。**

### 左侧：为什么自研框架？

1. **现有框架抽象过重**：LangChain 等框架组件嵌套深、版本变化快，难以针对科学场景深度定制。
2. **通用框架不懂科学上下文**：生物信息学、自动化实验、靶点调研的长文本、多步骤、工具链特性，需要场景化优化。
3. **数据主权是硬约束**：基因组、化合物、实验数据不能随意上云，需要支持本地/私有化模型部署。
4. **上下文成本不可控**：通用压缩策略在长序列、文献、日志场景下 token 浪费严重。

### 右侧：Nova 框架架构

用 4 层架构说明专业性：

1. **`nova_ai` — 模型抽象层**
   - 统一多厂商模型接口
   - 支持国产模型与本地私有化部署
   - 屏蔽底层 API 差异

2. **`nova_agent` — 事件驱动运行时**
   - 自研 `agent_loop` 异步循环
   - 工具执行、状态管理、事件订阅/发布
   - 比链式/图状态机更透明、更可干预

3. **`nova_harness` — 高阶 SDK**
   - 会话树：支持分支、fork、导航
   - 场景化上下文压缩：按任务类型定制保留策略
   - `nova-pkg`：Agent / 工具 / 扩展的 bundle 化包管理

4. **Agent Bundle — 场景化应用层**
   - `coding_agent`：官方编程 Agent，验证压缩策略
   - 生物 Agent（研发中）：生物信息学、自动化实验、靶点调研

### 底部：核心差异与进展

**核心差异**：

- **场景化上下文压缩**：coding 已验证，可向生物场景扩展
- **模型层解耦**：国产模型 + 本地私有化部署友好
- **事件驱动运行时**：Agent 行为全链路可观测、可干预
- **Bundle 化生态**：工具、协议、文献检索组件可复用分发

**关键进展**：

- Alpha 版本已发布
- 4 层核心框架全部自研完成
- `coding_agent` + TUI 已可用，验证场景化压缩思路
- 正在扩展生物信息学与自动化实验场景

**融资用途**：核心运行时稳定、生物场景压缩策略研发、生物 Agent bundle 开发、商业化团队。

---

## 四、30 秒口述脚本（专业框架版）

"大家好，我们是 Nova。

我们不只是在生物场景里用 LangChain 做应用，而是在自研一套 AI Agent 运行时框架。

现有框架比如 LangChain，抽象层次太深，版本变化快，而且它们不理解科学计算场景。我们做 Nova 是因为需要在生物信息学、自动化实验、靶点调研这些场景里，拥有完全可控的 Agent 基础设施。

Nova 是四层架构：底层 `nova_ai` 统一模型抽象，支持国产和本地私有化部署；中间 `nova_agent` 是事件驱动的运行时；上层 `nova_harness` 提供会话树和场景化上下文压缩；最上面是各类 Agent bundle。

coding 场景已经验证了我们的压缩策略，下一步会复制到生物场景。我们希望成为生命科学领域 Agent 基础设施的底层提供者。"

---

## 五、60 秒口述脚本（专业框架版）

"各位投资人好。

我先说清楚一点：**我们不是一家做生物应用套壳的公司，我们在自研 AI Agent 框架。**

为什么自研？因为市面上的框架满足不了我们的需求。LangChain 生态很丰富，但它的抽象层次太深、版本变化快，而且它是一个通用框架，根本不理解生命科学场景。生物研发里有大量长文本、多步骤、工具链复杂的工作流——比如靶点调研要看成百上千篇文献，自动化实验要处理设备日志和协议，生物信息学分析要跑很长的 pipeline。通用框架的上下文压缩是'一刀切'的，token 成本会爆炸。

所以我们做了 Nova。Nova 是一个分层的 Agent 运行时框架：

- 最底层 `nova_ai` 是模型抽象层，统一对接 OpenAI、国产模型和本地私有化部署模型；
- 中间 `nova_agent` 是事件驱动的运行时，我们自研了 `agent_loop`，工具执行、状态流转、事件发布都是透明的，可以精细干预；
- 上层 `nova_harness` 提供会话树、场景化上下文压缩和包管理；
- 最上面是 Agent bundle，coding 已经跑通，生物场景正在扩展。

这里我想重点讲**场景化上下文压缩**。通用框架把所有上下文当作文本一起压缩，但我们认为不同任务类型应该有不同的压缩策略。coding 场景里我们保留代码修改点和关键错误，压缩重复编译输出；生物文献场景里我们会保留实体关系和结论，压缩原始摘要；自动化实验场景里我们会保留协议和异常事件，压缩常规传感器读数。目前 coding 已经验证，这是我们要向生物场景快速复制的能力。

我们的目标不是做一个生物工具，而是成为生命科学 Agent 的底层基础设施。"

---

## 六、如何让投资人看出"你们真懂框架"

以下几个表达细节能显著提升专业感：

### 1. 用"运行时"而不是"框架"

❌ "我们做了一个 Agent 框架。"  
✅ "我们自研了 Agent 运行时（Agent Runtime）。"

"运行时"这个词会让懂技术的投资人立刻意识到你们在造底层基础设施。

### 2. 强调"事件驱动"的对比价值

❌ "我们支持工具调用。"  
✅ "我们采用事件驱动架构，Agent 的每个状态变更、每次工具调用、每次模型响应都是可观测、可干预的事件。"

这暗示你们理解为什么 LangChain 的链式/图抽象在某些场景下不够灵活。

### 3. 区分"通用压缩"和"场景化压缩"

❌ "我们做上下文压缩。"  
✅ "通用压缩是损失信息的，我们的压缩策略是场景感知的——不同任务类型保留不同信息密度。"

这是真正的技术洞察，不是功能罗列。

### 4. 讲清楚"为什么分层"

❌ "我们代码分了几层。"  
✅ "分层让模型层、运行时层、SDK 层、应用层可以独立演进。模型厂商在变化，Agent 策略在变化，UI 形态也在变化，分层让底层改动不影响上层业务逻辑。"

### 5. 强调"不是套壳"

❌ "我们比 LangChain 更好用。"  
✅ "LangChain 是组件编排框架，我们是自底向上构建的运行时。我们需要的是对 Agent 生命周期的完全控制，这是套壳做不到的。"

---

## 七、一页 PPT 中应该出现的"专业词汇"

适度使用这些词汇可以提升专业感，但不要堆砌：

- **Agent Runtime**（Agent 运行时）
- **事件驱动架构**（Event-driven Architecture）
- **上下文压缩**（Context Compaction / Compression）
- **模型抽象层**（Model Abstraction Layer）
- **Bundle 化分发**（Bundle Distribution）
- **会话树**（Conversation Tree / Session Tree）
- **工具链编排**（Toolchain Orchestration）
- **私有化部署**（On-premise Deployment）

---

## 八、视觉设计建议

### 整体风格

- **科技感优先，生物科技为辅**
- 不要让 PPT 看起来像生物学术海报
- 要让投资人感觉在看一家 infra 公司的 deck

### 配色

- **主色**：深海军蓝 `#0A1628`
- **强调色**：电青色 `#06B6D4`
- **生物科技点缀**：少量紫色 `#8B5CF6` 或绿色 `#10B981`

### 关键视觉元素

1. **架构图**：必须放在页面右侧，用 4 层方块表示
2. **连接线**：用发光线条表示事件流/数据流
3. **图标**：
   - 模型层：云端/服务器图标
   - 运行时：循环/齿轮图标
   - SDK：工具箱/层叠图标
   - Bundle：包裹/应用图标

### 字体

- 标题：粗体无衬线，大字号
- 架构层文字：等宽字体或粗体无衬线
- 正文：简洁无衬线

---

## 九、与 Kimi/Codex/OpenClaw 的赛道差异

投资人可能会问：市场上已经有 Kimi Code CLI、OpenAI Codex CLI、OpenClaw 了，Nova 还有什么机会？

### 核心回答：赛道不同

| 产品 | 定位 | 与 Nova 的关系 |
|------|------|---------------|
| **Kimi Code CLI** | Moonshot 的终端 coding 工具 | 它是"房子"，Nova 是"地基" |
| **OpenAI Codex CLI** | OpenAI 的终端 coding 工具 | 绑定 OpenAI 模型，Nova 模型中立 |
| **OpenClaw** | 本地优先的个人 AI 助理框架 | 面向消息平台和生活工作流，Nova 面向企业科学计算 |

### 为什么这些不是直接竞品？

1. **Kimi/Codex 是应用，Nova 是基础设施**
   - 它们解决"如何写好代码"这个单点问题。
   - Nova 解决"如何拥有、控制、扩展一个企业级 Agent 系统"。

2. **Kimi/Codex 绑定单一模型厂商**
   - Kimi 绑定 Moonshot，Codex 绑定 OpenAI。
   - Nova 的 `nova_ai` 抽象层支持国产模型、OpenAI、Anthropic 及本地私有化部署。

3. **它们不懂科学场景，数据要上云**
   - 生物研发的长文本、多步骤、工具链特性需要场景化压缩。
   - 基因组、化合物数据不能随意上云，Nova 支持本地部署。

4. **OpenClaw 是个人助理，Nova 是企业计算**
   - OpenClaw 连接 WhatsApp/Telegram/Slack，做生活/工作流自动化。
   - Nova 承载生物信息学 pipeline、自动化实验、靶点调研等企业级长流程。

### 一页 PPT 中的表达

```
Kimi Code / Codex CLI = 模型公司的 coding 工具
OpenClaw              = 个人 AI 助理
Nova                  = 企业可拥有、可控、可扩展的 Agent 运行时基础设施
```

**核心话术**：

> "Kimi 和 Codex 很强，但它们是模型公司做的 coding 应用；OpenClaw 是个人助理。Nova 要造的是地基——一个模型中立、本地部署友好、支持场景化压缩的 Agent 运行时。coding 是我们验证地基的第一个场景，生物研发才是我们要建的主楼。"

---

## 十、AI 图像生成提示词（专业框架 + 生物科技风格）

```
A premium technical pitch deck background for an AI agent runtime framework slide, 
deep navy blue canvas with a glowing cyan 4-layer stacked architecture diagram 
floating on the right side, thin luminous lines connecting layers suggesting event flow, 
subtle DNA double helix and molecular structures woven into the background texture, 
large empty negative space on the left for white title and bullet text, 
professional, engineering-first, biotech-infused, cinematic soft lighting, 
no text, no logos, no humans, 8K, ultra clean.
```

**中文参考翻译**：
高端技术感路演PPT背景，用于 AI Agent 运行时框架主题，深海军蓝画布，右侧悬浮着发光的电青色四层堆叠架构图，纤细光线连接各层暗示事件流，背景纹理中微妙融入 DNA 双螺旋与分子结构，左侧留有大量负空间用于白色标题和要点文字，专业、工程优先、生物科技元素点缀，电影级柔光，无文字、无 Logo、无人物，8K，极度简洁。

---

## 十一、一句话总结

**投资人需要记住的：**

- **问题**：现有 Agent 框架是通用型的，无法满足生物科技对场景化压缩、数据主权、复杂工具链的需求。
- **答案**：Nova 是一支生物科技团队自研的专业 Agent 运行时框架，分层清晰、事件驱动、模型解耦、场景感知，coding 已验证，生物场景是主战场。

**核心信息**：

> **我们既懂 Agent 框架，又懂生物研发。Nova 是专为复杂科学计算场景重新设计的 Agent 基础设施。**
