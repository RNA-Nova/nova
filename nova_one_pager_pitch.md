# Nova 一页 PPT 投资人介绍指南

> 投资人每分钟要看无数份 BP，一页 PPT 的核心目标是：让对方在 30 秒内记住你是谁、解决什么问题、为什么重要。

---

## 一、一页 PPT 的信息公式

```
[大标题] = 我们是谁
[一句话] = 解决什么问题，带来什么价值
[左侧：痛点] = 市场为什么需要这个
[右侧：解法] = 我们如何差异化地解决
[底部：证据 + 诉求] = 为什么现在、需要多少资源
```

---

## 二、推荐页面布局

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│     Nova —— 面向产品化的 AI Agent 轻量编排框架                 │
│     让 Agent 从一次性脚本，变成可分发、可管理、可商业化的产品   │
│                                                             │
├──────────────────────────┬──────────────────────────────────┤
│                          │                                  │
│   现有框架的问题          │   Nova 的解法                     │
│                          │                                  │
│   · LangChain 抽象过重    │   · 分层 monorepo，自底向上       │
│     学习曲线陡峭          │     架构清晰可控                   │
│                          │                                  │
│   · 直接调 SDK 太底层      │   · 事件驱动运行时 + 原生 TUI      │
│     重复造轮子            │     开发体验完整                   │
│                          │                                  │
│   · 云厂商绑定严重        │   · 模型层解耦，国产与本地模型友好 │
│     国产/本地部署难       │     成本低、合规灵活               │
│                          │                                  │
├──────────────────────────┴──────────────────────────────────┤
│                                                             │
│   进展：Alpha 已发布 │ 5 个核心子包 │ 官方 Coding Agent + TUI 已可用 │
│   本轮融资用于：核心稳定 · 生态市场 · 商业化团队               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、推荐文案（可直接使用）

### 大标题

**Nova —— 面向产品化的 AI Agent 轻量编排框架**

### 一句话价值主张

**让 Agent 从一次性脚本，变成可分发、可管理、可商业化的产品。**

### 左侧：为什么市场需要 Nova

1. **主流框架太重**：LangChain 抽象层次深、学习曲线陡峭，难以深度定制。
2. **直接开发太碎**：调用 OpenAI/Anthropic SDK 过于底层，团队反复造轮子。
3. **Agent 难管理**：缺乏统一的包管理、分发和商业化机制。

### 右侧：Nova 的核心解法

1. **分层架构**：`nova_ai` → `nova_agent` → `nova_harness` 职责清晰，可独立演进。
2. **事件驱动**：Agent 运行时支持观测、干预与扩展，比链式组合更可控。
3. **模型层解耦**：`nova_ai` 统一抽象国产模型与本地部署模型，成本低、合规灵活。
4. **Bundle 生态**：`nova-pkg` 让 Agent、工具、扩展像应用一样安装与分发。

### 底部：关键进展与融资诉求

- **关键进展**：Alpha 版本已发布；5 个核心子包成型；官方 Coding Agent 与 TUI 已可用。
- **融资用途**：核心框架稳定、生态市场建设、商业化团队搭建。

---

## 四、30 秒口述脚本

> 用于电梯演讲或路演开场。

"大家好，我们是 Nova。

当前开发者构建 AI Agent 面临两个极端：要么用 LangChain，抽象太重、难以控制；要么直接调 OpenAI SDK，又太底层、重复造轮子。

Nova 是一个面向产品化的 AI Agent 轻量编排框架。我们采用分层架构，底层 `nova_ai` 统一对接国产模型和本地部署模型，避免被单一云厂商绑定；中间事件驱动的运行时让 Agent 行为可观测、可干预；上层通过 `nova-pkg` 实现 Agent 的 bundle 化分发。

目前 Alpha 版本已发布，官方 Coding Agent 和 TUI 已可用。我们希望成为中文开发者构建 Agent 产品的首选基础设施。本轮融资将用于核心稳定、生态市场和商业化团队。"

---

## 五、60 秒口述脚本

> 用于正式路演时的一页 PPT 讲解。

"各位投资人好，这页 PPT 是我们整个项目的缩影。

左边是问题。AI Agent 是未来最重要的应用形态，但开发者今天只有两个选择：LangChain 生态丰富但抽象过重、版本变化快；直接调用 SDK 又太底层，每个团队都在重复造轮子。更关键的是，大多数框架深度绑定海外云厂商，国产模型适配和本地部署困难，导致成本高、合规风险大，Agent 做出来之后也很难产品化。

右边是我们的解法。Nova 是一个分层 monorepo 框架：最底层 `nova_ai` 统一多厂商模型调用，国产模型和本地部署模型都能被标准化接入；中间 `nova_agent` 是事件驱动的运行时，让 Agent 行为可观测、可干预；上层 `nova_harness` 提供会话树、上下文压缩、包管理和 JSON-RPC 接口；最上面是 `nova-tui` 终端界面和 `nova_coding_agent` 这样的官方 bundle。

我们的核心差异在于：**Nova 不是让开发者组合组件，而是让开发者拥有一个完整可控的 Agent 基础设施。**

底部是我们当前的进展：Alpha 版本已经发布，核心子包和官方 Coding Agent 都已可用。本轮融资主要用于三件事：把框架做到生产级稳定、建设 `nova-pkg` 生态市场、以及组建商业化团队服务企业客户。"

---

## 六、如何表达国产模型适配、缓存、成本与本地部署优势

这几个点确实是 Nova 的差异化方向，但在投资人面前要**讲机制、讲趋势、讲结果**，避免空喊口号。

### 1. 国产模型适配度高

**真实基础**：Nova 的 `nova_ai` 是统一的 LLM 提供商抽象层，当前已有 OpenAI 完整实现，并预留了 Volcengine 等国产模型的数据结构。架构上支持多厂商统一接入。

**建议表达**：

> 在国产替代趋势下，企业需要不绑定 OpenAI/Anthropic 的 Agent 基础设施。Nova 的模型抽象层让国产模型可以被标准化接入，降低迁移成本。

**注意**：不要承诺"已支持所有国产模型"，应强调**架构上的国产友好性**和**适配成本更低**。

### 2. 本地部署大模型适配

**真实基础**：`nova_ai` 的抽象层可以对接本地推理服务（如 vLLM、Ollama、Xinference 等），`nova_harness` 的 JSON-RPC 模式也支持本地运行。

**建议表达**：

> 对于金融、政务、医疗等对数据主权敏感的行业，Nova 支持本地部署大模型，Agent 数据不出内网，满足合规要求。

**商业价值**：本地部署 = 数据安全 + 长期成本可控 + 避开海外 API 访问限制。

### 3. 成本低

**真实基础**：成本低不是 Nova 本身便宜，而是 Nova 让用户有更多低价选择：

- 接入国产模型（如豆包、文心、通义、智谱）通常比 GPT-4 便宜一个数量级。
- 本地部署省去按 Token 计费的开销。
- 事件驱动架构便于做请求级缓存与复用。

**建议表达**：

> 通过模型层解耦，Nova 让企业可以按需选择国产模型或本地模型，实际运行成本可比纯海外模型方案降低 50%-90%。

**注意**：具体数字需要基于实际测试，路演时可用"显著降低"代替未验证的精确比例。

### 4. 缓存命中高

**真实基础**：`nova_harness` 的会话树与上下文压缩机制，使历史上下文可以被结构化复用。事件驱动模型也方便在层间插入缓存逻辑。

**建议表达**：

> Nova 的会话树和上下文压缩机制，让重复请求可以命中已有上下文，减少无效 LLM 调用，进一步降低 token 消耗。

**注意**："缓存命中率高"需要实际数据支撑。在 Alpha 阶段，建议说成**"具备更好的缓存优化潜力"**或**"架构天然支持缓存分层"**。

### 一页 PPT 中的整合建议

把这四点合并成一条核心卖点，放在右侧"解法"中：

> **模型层解耦**：统一接入国产模型与本地部署模型，成本低、合规灵活、不被云厂商绑定。

口述时补充：

> "底层模型层我们做了统一抽象，这意味着客户既可以用便宜的国产模型，也可以在本地部署私有模型。对敏感行业这是刚需，对普通开发者这意味着成本可以低一个数量级。"

---

## 七、视觉设计建议

### 配色

- **主色**：深海军蓝 `#0A1628` 或 `#0F172A`
- **强调色**：电青色 `#06B6D4` 或 `#22D3EE`
- **辅助色**：白色 `#FFFFFF`、浅灰 `#E2E8F0`

### 字体

- 标题：粗体无衬线（如 Inter、SF Pro、阿里巴巴普惠体）
- 正文：常规无衬线，保持可读性
- 数字/证据：可加粗或放大处理

### 图标

- 左侧问题：使用警示、复杂、碎片化的图标
- 右侧解法：使用分层、连接、包裹/分发的图标
- 底部进展：使用火箭、增长、团队的图标

### 避免

- 不要放架构图细节
- 不要放代码片段
- 不要使用超过 3 种颜色
- 不要堆砌文字，每点不超过一行

---

## 八、AI 图像生成提示词（一页 PPT 背景）

```
A premium minimalist pitch deck background for a one-page AI agent framework slide, 
deep navy blue canvas with subtle glowing cyan neural network nodes on the right side, 
clean geometric layers fading into the background, 
large empty negative space on the left and center for white title text, 
professional, futuristic, high-tech, cinematic soft lighting, 
no text, no logos, no humans, 8K, ultra clean.
```

**中文参考翻译**：
高端极简的一页式 AI Agent 框架路演PPT背景，深海军蓝画布，右侧带有微妙的电青色神经网络发光节点，干净的几何层次融入背景，左侧和中央留有大量负空间用于白色标题文字，专业、未来感、科技感，电影级柔光，无文字、无 Logo、无人物，8K，极度简洁。

---

## 九、一句话总结

**一页 PPT 的目标不是讲完所有事，而是让投资人记住一个核心问题和一个核心答案。**

对于 Nova：

- **问题**：Agent 框架要么太重，要么太碎，且难以产品化。
- **答案**：Nova 提供分层、可控、bundle 化、模型层解耦的 Agent 基础设施，让 Agent 成为可商业化、低成本、合规灵活的产品。
