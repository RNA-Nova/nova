# nova 项目移交书

> 移交日期：2026-09-01　|　移交范围：`/Users/liujinming/agent/nova` 全仓
> 本文档自包含——不依赖任何外部记忆/会话记录即可接手。

---

## ⚠️ 移交首要事项（先读这里）

1. **工作树有 450+ 个文件未提交**。本移交周期（约 2026-08-30 ~ 09-01）的全部重构成果都在未提交状态，任何误操作（checkout/clean）都是不可逆损失。**接手后第一件事：审阅并分逻辑块提交**（建议分块方案见 §6）。提交规范沿用仓库现有风格：中文 + `sync:` / `fix:` 前缀。
2. `packages/nova_executor/crates/executor-otel/` 下有 **3 个 `.rs` 文件的改动**（`config.rs` / `otlp.rs` / `trace_context.rs`）——这是移交前就存在的独立工作（executor-otel 对齐 codex 上游），**不属于本移交周期的重构**，提交时应单独成块或与作者确认。
3. 根目录的 `项目移交书.md`（本文件）与 `packages/nova_server/2026-09-01-*.txt`（会话导出）是新产物；会话导出 .txt 落在包目录里属于临时位置，建议移到 `docs/` 或不入库。

---

## 1. 项目概览

nova 是一个 **Agent 框架 monorepo**，架构上对标两个参考实现：

- **codex**（OpenAI，Rust，位于 `/Users/liujinming/agent/codex`）——**主要对标物**。本周期完成了与 codex 的逐层同构（见 §3）。
- **pi**（TypeScript，位于 `/Users/liujinming/agent/pi`）——session 库层（事件溯源会话树）的移植来源。

### 包结构（pixi workspace monorepo，Python `>=3.12,<3.14`）

| 包 | 角色 | 对标 codex |
|---|---|---|
| `packages/nova_ai` | Provider 层：模型目录、openai 兼容流式、OAuth | — |
| `packages/nova_agent` | 库层 Agent：agent_loop、harness/session（事件溯源会话树，移植自 pi） | `core` 的一部分 |
| `packages/nova_harness` | 高阶组装层：**RuntimeManager**（会话工厂）、AgentSession、config/sessions/resources/package | `core` + `thread-manager` |
| `packages/nova_server` | **协议服务层**：RpcServer、8 域 JSON-RPC 方法表、client 家族（in_process/remote）、exec | `app-server` 家族 + `exec` |
| `packages/nova_client` | TS/Node 侧前端 + pty 脚本（协议消费者） | `tui` 的对应位 |
| `bundles/nova_coding_agent` | 官方 bundle（agents/tools/skills/扩展） | — |
| `packages/nova_executor` | Rust（executor-otel 对齐 codex 上游，独立工作流） | — |

### codex 对位关系（接手必读）

```
codex                          nova
─────────────────────────────────────────────────────────
core/ThreadManager        ⇔   nova_harness/core/runtime_manager/manager.py
core/Session::spawn       ⇔   nova_harness/core/runtime_manager/assembly.py
app-server（服务器本体）   ⇔   nova_server/server.py（RpcServer）
app-server-protocol       ⇔   nova_server/protocol/（8 域方法表+shapes+schema 导出）
app-server-client(InProcess)⇔ nova_server/client/in_process.py
app-server-client(Remote) ⇔   nova_server/client/remote.py
exec（headless，走协议）   ⇔   nova_server/exec/（走 InProcessNovaClient）
tui（终端前端，走协议）    ⇔   nova_client（pty 连 nova-server stdio）
thread-store              ⇔   nova_harness/sessions（SessionManager/JSONL，无 trait 反转——登记项）
sdk/（ts/py 跨语言发行）   ⇔   ❌ 未建（登记项；TS 类型已自动导出）
```

**依赖箭头纪律**（同 codex，单向不可逆）：exec → client → server/protocol → RuntimeManager → core。server/protocol 永不反向 import client/exec。

---

## 2. 环境与常用命令

```bash
# 包管理是 pixi（monorepo 根统一 workspace）；测试/工具必须走 dev 环境
pixi install --environment dev

pixi run -e dev test-ai        # nova_ai        （529 passed）
pixi run -e dev test-agent     # nova_agent     （240 passed）
pixi run -e dev test-harness   # nova_harness   （1321 passed, 44 deselected=integration）
pixi run -e dev test-server    # nova_server    （256 passed）
pixi run -e dev test-coding    # nova_coding_agent（528 passed, 3 skipped）
pixi run -e dev test-all       # 五包连跑（见 §7 怪癖：尾部 exit=4 是既有问题）
pixi run -e dev typecheck      # ⚠️ 只覆盖 nova_ai + nova_agent（pyproject.toml:65）
pixi run format                # black + isort（注意：不含 nova_server，需手动）

# harness/server 的 pyright 要手动跑（无基线文件，靠对比）：
pixi run -e dev pyright packages/nova_harness/src/nova_harness packages/nova_server/src/nova_server
# 记录在案的基线：合计 404 errors（原 405，本周期零新增净减 1）

# 手动单跑某包测试（注意 cwd 必须 cd 到包根，否则 conftest 冲突）：
cd packages/nova_harness && pixi run -e dev pytest tests -m 'not integration' -q
```

- 常驻服务器：`nova-server --listen stdio://`（默认）或 `ws://HOST:PORT`（token 鉴权，连接信息落盘 `~/.nova/agent/rpc-server.json` 0600）
- headless：`nova exec <agent> --task "..." [--json]`（走协议客户端）；console scripts 见各包 pyproject `[tool.poetry.scripts]`
- ⚠️ **两个 pytest 陷阱**：① harness 与 server 的 `tests/conftest.py` 同名，**一次 pytest 不能同时跑两包**（ImportPathMismatchError），必须按包分跑；② shell 工作目录会漂移，跑任何命令前显式 `cd` 到预期目录（本周期多次踩坑）

---

## 3. 本移交周期完成的工作（全部已验证，未提交）

### 3.1 与 codex 的架构同构（主线）

| 工作项 | 内容 | 位置 |
|---|---|---|
| **RuntimeManager 做完善** | 取代已删除的 `sdk.py` 和 `agent_session/factory.py`：四件套 `manager.py`（活跃注册表+生命周期）/`assembly.py`（组装编排）/`factory.py`（per-session 组装件）/`__init__` | `nova_harness/core/runtime_manager/` |
| codex ThreadManager 纪律移植 | registration-last（组装失败零注册零回滚）；session_id 冲突**在位者赢**（新来者销毁+`SessionIdCollisionError`）；`shutdown_all(timeout)` 有界并发扇出、三分桶报告只移除 completed；`asyncio.Lock` 只护注册表；注册表键=open 时刻 id（switch/fork 原地换会后 id 漂移，server 用 `close_runtime` 按身份销毁）；可注入 `runtime_factory`/`session_id_of` | 同上 |
| **server 层接线** | `ServerState` 持 `runtime_manager` 实例；createSession 走 `open_session`；dispose 走 `close_runtime`；exec 走协议客户端 | `nova_server/protocol/methods/{state,session}.py`、`exec/runner.py` |
| **client 家族** | `base.py`（`FrameChannel` 三原语 + `MultiplexedClient`：id 多路复用/有序事件队列/读泵）+ `in_process.py`（进程内起完整 RPC 栈，对位 InProcessAppServerClient）+ `remote.py`（WS + Bearer/`?token=` 双通道鉴权，对位 RemoteAppServerClient） | `nova_server/client/` |
| **exec 走协议** | runner 重写为 `initialize → createSession → prompt（阻塞至回合末）→ 消费 item 帧至 agent_end → shutdown`；`--json` 输出归约 item 帧（与前端严格同源） | `nova_server/exec/runner.py` |
| **契约扩展** | `CreateSessionParams` 新增 `tools`/`excludeTools`/`additionalSkillPaths`/`additionalPromptTemplatePaths`/`trust` 三态（None=UI 决议，True/False=无 UI 覆盖）；schema 工件已重导出 | `protocol/methods/shapes.py`、`session.py` |
| **sdk 消灭** | `sdk.py` 从 harness 层删除（守卫测试 `test_sdk_module_stays_gone`）；唯一组装家是 assembly；`nova_harness/__init__` 直连 assembly；12 个测试文件 import 改线；旧 sdk 测试迁入 `tests/core/runtime_manager/` | 全仓 |

### 3.2 性能修复

| 问题 | 修复 | 数据 |
|---|---|---|
| **O(n²) 流式 JSON 解析**：工具参数逐 delta 全量 `json_repair`（纯 Python ~7µs/字节），14KB×71 delta 实测 3.7s，全在事件循环热路径 | 新增 `StreamingJsonParser`：增量状态扫描（字符串/转义/括号栈，只扫新增片段）+ 最小闭合序列补全 + C 级 `json.loads`；`json_repair` 只在 `finish()` 兜底一次（终值语义与旧 `parse_streaming_json` 完全一致） | 70KB×351 delta：**32ms**（旧实现分钟级超时）；15 个单测 + **种子化差分 fuzz**（400 文档×2 编码×随机切分，逐 delta 断言"与真值前缀一致"不变式 + 300 例截断终值差分对齐旧实现） | `nova_ai/src/nova_ai/utils/json_parser.py`、`tests/unit/test_json_parser.py` |
| F811 清尾 | `timings.py` 的 `def time()` 遮蔽 stdlib → **`NOVA_TIMING=1` 必崩**的潜伏 bug，改 `import time as time_module`；`test_imports.py` 被遮蔽的重复测试复活 | 2 处 |

### 3.3 其他

- nova_harness `AGENTS.md`：目录树/模块职责/导入无环不变量已更新至当前状态
- 根 `AGENTS.md` 数据建模规则全面修订（10 条，含 TypedDict 第四种表示、渐进棘轮）
- pyright 棘轮已启动：harness+server 基线 405→404（零新增）；新写/重写模块要求 0 error

---

## 4. 关键设计决策与理由（防止接手人重新争论）

1. **RuntimeManager 是会话创建的唯一入口**。server/exec 不得直连 assembly。理由：会话创建权收敛工厂（对位 codex ThreadManager::start_thread），生命周期可追踪。
2. **exec 留在 nova_server、走协议客户端**（不是搬回 harness 直驱）。依据：codex `exec/src/lib.rs` 实测 57 处 app_server 引用，经 `InProcessAppServerClient` 发 `thread/start` 协议请求——"codex exec 直驱 core 不走协议"是误判（曾因此两次改方案，最终回到协议路径）。收益：exec 的 `--json` 与前端帧严格同源。
3. **stdio 客户端不建**（与 codex 同决策）。codex 的 Remote 实为"WS 帧跑 TCP/UDS"，stdio 消费者在别的语言侧手搓帧（VS Code 扩展）。抽象跟随消费者，不跟随传输。若将来 nova_client（Python）要库化，从 `client/base.py` 挂一个管道通道即可。
4. **daemon/UDS 不建**。codex 的 `app-server-daemon`（常驻共享服务器+托管升级+远程配对）服务"多前端实例共享"场景，nova_client 目前只会 spawn 私有实例，无服务对象。依赖顺序：WS Remote（已有）→ UDS 传输 → daemon。
5. **settings deepcopy 不修**（实测 10-25µs/次，低频路径，防御性设计保留）。教训已入记忆：性能项登记必须带实测数字。
6. **server 的 createSession"先销毁旧 runtime 再创建"顺序未改**（失败会留下无 runtime 状态）——已登记为后续项，与本周期范围无关。

---

## 5. 登记的后续路线图（按建议优先级）

| 优先级 | 项 | 说明 |
|---|---|---|
| P1 | **deleteSession 策略对齐 codex** | codex `thread/delete`：活跃先收尾→级联子树删→广播 `ThreadDeleted` 通知；nova 现状"活跃即拒绝"。改 server 层 deleteSession + RuntimeManager 配合 + 通知 |
| P1 | 分块提交 | 见 §0 |
| P2 | Responses API api_impl | "增"不"换"：对支持的 provider 该 provider 的参数解析问题自动消失 |
| P2 | operation 模型 / tasks 工单化 | 真正的崩溃续跑（`findOpenOperations` 语义）依赖它；pi 自己也是 stub，nova 与 pi 停在同一进度 |
| P3 | msgspec codec 层 | session JSONL 加载提速（decode 快 6-15x），登记在 AGENTS.md 规则 2 |
| P3 | StdioSpawn SDK / TS client | 对位 codex `sdk/`；TS 类型已自动导出（`nova_client/protocol/nova-wire.gen.ts`），触发条件=出现真实跨语言嵌入用户 |
| P3 | ThreadStore trait 反转 / daemon / UDS | 依赖链见 §4.4 |

---

## 6. 建议的提交分块（450+ 文件）

1. session 库移植（`nova_agent/harness/session/` + conformance 测试）
2. nova_harness 核心重构 + server 独立包拆分（历史大块）
3. RuntimeManager 四件套 + server/exec 接线 + 契约扩展 + schema 重导出
4. client 家族（base/in_process/remote）+ exec 协议化
5. sdk 消灭 + 测试迁移
6. StreamingJsonParser + 差分 fuzz（`fix:`）
7. timings.py 潜伏崩溃修复 + F811 清尾（`fix:`）
8. AGENTS.md 系列文档
9. **单独**：executor-otel 三个 .rs（作者本人确认后）

---

## 7. 已知问题与怪癖

| 问题 | 说明 | 处置 |
|---|---|---|
| `test-all` 尾部 exit=4 | 该 pixi 任务除 depends-on 五套件外自带一条 `pytest tests`，在仓库根执行（根目录无 tests/）→ 报错。**五套件本身全绿**，与代码无关 | 既有怪癖；可修 task 定义去掉自带 cmd |
| `typecheck` task 覆盖不全 | 只查 nova_ai/nova_agent；harness/server 需手动 pyright | 已在 AGENTS.md 注明 |
| 两包 conftest 同名 | 一次 pytest 跑 harness+server 必炸 | 按包分跑（pixi task 就是这么做的）|
| `nova` pixi task（`nova-harness run`） | 指向的 `run` 子命令在 `app/main.py`（help-only 桩）中不存在 → **当前是坏的** | 待办：给 main.py 补子命令结构（当时搬 exec 方案里有现成设计，已随方案作废，可翻计划文件找回思路）|
| monkeypatch 目标随模块迁移 | patch 必须打到持有调用点的模块（`factory.py` 迁移、`PackageManager` patch 都踩过）| 写测试时注意 |

---

## 8. 知识资产位置

- **各包 AGENTS.md**：包级指南（nova_harness 的最全——含 runtime_manager 职责与导入不变量规则）
- **根 AGENTS.md**：数据建模 10 规则、静态检查棘轮
- **计划文件**：`/Users/liujinming/.claude/plans/immutable-toasting-piglet.md`（本周期两个大方案的过程稿）
- **会话导出**：`packages/nova_server/2026-09-01-*.txt`（完整决策对话，含所有"为什么"）
- 本文档 §3/§4/§5 为自包含摘要

---

## 9. 接手验证清单（10 分钟确认环境正常）

```bash
cd /Users/liujinming/agent/nova
pixi run -e dev test-ai && pixi run -e dev test-agent && pixi run -e dev test-coding
cd packages/nova_harness && pixi run -e dev pytest tests -m 'not integration' -q   # 期望 1321 passed
cd ../nova_server && pixi run -e dev pytest tests -q                                # 期望 256 passed
pixi run -e dev python -c "from nova_harness.core.runtime_manager import RuntimeManager; print('ok')"
pixi run -e dev python -c "from nova_server.client.in_process import InProcessNovaClient; print('ok')"
git status --short | wc -l    # 确认未提交体量，开始审阅提交
```

预期数字：529 / 240 / 1321 / 256 / 528(+3 skip)。若对不上，先看 §3 的文件清单 diff，再回看会话导出。
