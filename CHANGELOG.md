# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/lang/zh-CN/).

## [0.1.0] - 2026-09-03

> 首个封版。说明：executor 相关（`nova_executor` / `nova-executor-py` 包与
> bundle 的 executor 接入/`/executor` 切换扩展）已从本发布线移除——执行后端的
> 演进在 main 分支独立进行中。本线为纯本地执行的框架 + 官方 bundle 形态。

### Added
- **入站背压 + 事件循环滞后观测（连接化 P3 收官）**：每连接在飞 handler 上限（`max_inflight` 默认 256）——超限对请求立即回 `-32004 overloaded`（码位避让：-32001 已是 SESSION_NOT_FOUND）、对通知静默丢弃，读泵不再无界派生 task（codex `-32001` 对位，本地可信客户端场景下是防呆而非防攻击）；`RpcServer` 内置 **loop-lag 探针**（周期任务测漂移，超 100ms 打 stderr → `rpc-stderr.log`——单循环的三类卡顿源（CPU 占住/同步阻塞/队头阻塞）全部表现为"周期任务不准时"，一个探针把三类都变成可观测数据），包级 AGENTS.md 同步写入"RPC 循环线程三禁"纪律。
- **事件信封锚点 + 原子同步快照（连接化 P2，codex 读 API 追赶的加强版）**：`agent/event` 信封新增 `seq`（服务器生命周期单调发号，`broadcast_event` 唯一写入点）+ `ts` + `sessionId` 三字段；新增 `syncSession` RPC（session 域第 76 方法）——状态快照 + 条目分页 + 事件高水位**一帧原子拿齐**（handler 同步段无 await，单循环天然原子），取代"getSessionState + getSessionEntries 两发"旧路径（两发之间的增量事件可能既进快照又进事件流被重复应用）；前端 `syncFromBackend` 改走 syncSession 逐页翻完（500 条/页，末页 state/entries/eventSeq 同一时点），事件汇丢弃 `seq <= eventSeq` 的增量——快照/增量重复应用竞态清零；旧后端（无 syncSession）自动回退两发路径（-32601 检测），MINOR 差放行语义下混合版本可用。`getSessionEntries` 新增 `offset`/`limit` 分页（缺省全量向后兼容，`total` 供翻页终止判定）——大会话全量 dump 不再单帧冻结循环。契约 MINOR 升 1.3（加法变更），`nova-wire.schema.json`/`nova-wire.gen.ts` 重新导出，conformance 新增锚点用例（水位后事件 seq 严格大于水位 + 分页逐页拼接等于全量）。

### Changed
- **RPC 层连接化重构（学习 codex app-server 的 asyncio 译本，为 WS 多客户端铺路）**：`core/rpc/` 从"单 transport 单读循环"重构为连接一等公民——新增 `connection.py`（`Connection`：进程级 id、`initialized` 状态机、按连接 UI 能力集、在飞请求表、有界出站队列 + 独立写泵；`ConnectionOrigin` stdio/websocket/memory 背压分流：可信来源阻塞等位、网络来源慢消费者断连；`current_connection` contextvar 让 handler 任务链全程可取来源连接）；`server.py` 重写为 `RpcServer`（连接注册表 + 每连接读泵；`add_connection` 唯一接入口；stdio 以 `exit_on_close=True` 保留"连接关即进程退"的 TUI 子进程语义）；事件桥改广播 + initialize 门（握手完成才收 `agent/event`），无界 `create_task` 发帧改为各连接队列 FIFO 入队（全局写锁删除——慢写不再队头阻塞全局）；反向原语改 `RoutingUIContext`（从 `protocol/` 升至 `core/rpc/ui_context.py`）按连接寻址——发起方连接优先、无归属广播 + 首响应胜出 + 败者收 `ui/cancel`，连接关闭即按 cancelled 收尾其独家持有的 pending（广播件只摘地址）；`cancelRequest` 按连接隔离（复合键 `(connId, reqId)`——两客户端同 id 不再互相误取消）；`ServerState.request_tasks` 随之下放 `Connection`。文件组织：删除 `modes/rpc` 的两个死兼容壳（旧名 `NovaRpcServer`/`RpcUIContext` 零引用实证）与 `modes/rpc/output_guard.py` re-export，`NovaServer` 正名 `RpcServer`。线上协议词汇零变更——TUI 前端无感。
- **conformance 黑盒套件读侧修复**：`select`+`TextIOWrapper.readline` 的预读缓冲陷阱（多帧合并到达时后到的行卡用户态缓冲、select 永不再报可读）改为 reader 线程 + 队列排干；套件纪律补 initialize 握手（连接化后它是事件/UI 的门，真实客户端同款）。新增多连接专项（`tests/core/rpc/server/test_connections.py`：双客户端同 id 隔离、事件 initialize 门、cancelRequest 连接隔离、exit_on_close 关停、断连取消在飞 handler）。
- **伞目录归并（纯搬家，零语义变更）**：`packages/nova_harness` → `packages/nova-harness/backend`、`packages/nova-client` → `packages/nova-harness/frontend`——运行时两半区同居一个上级目录（与 bundle 的 backend/frontend/ 三段式同构 dogfood）。Python import 名（`nova_harness`）与 npm 包名（`nova-client`）不变；pixi workspace/poetry path 依赖/全局 `nova` 包装器/wire 契约导出路径全部跟随。
- **user/project 两级目录前后端分治（设计定案 §9 落地，`packages/nova-harness/frontend/docs/frontend-backend-separation.md`）**：
  - **后端散养资源归 `backend/` 半区**：顶层自动发现扫描根改为 `<base>/backend/{extensions,skills,prompts,personas}`（user 级 base = `~/.nova/agent`，项目级 base = `<cwd>/.nova`），`agents/` 两半共享平级保留；包内约定发现（manifest 缺省目录）不受影响；tools/user_tools 本就不做顶层自动发现；
  - **前端域归 `frontend/tui/` 半区**：UISettings（`~/.nova/agent/ui-settings.json` → `frontend/tui/settings.json`）、UIStateStore（`ui-state/` → `state/`）、自定义主题（`themes/` → `themes/`）、键位表（`~/.nova/agent/keybindings.json` 与 `.nova/keybindings.json` → 两级 `frontend/tui/keybindings.json`）、/debug dump（→ `frontend/tui/debug/`）；
  - **启动自动迁移**：后端在 `AgentSessionServices.create`（`core/config/migration.py`）、前端在 TUI 装配根构造期（`src/migration.ts`）各管各的域——mv 语义（只搬不删）、幂等、新位已有内容不合并不覆盖（记诊断提示人工处理）；
  - **散养前端资产扫描根（新能力）**：`~/.nova/agent/frontend/tui/` 与 `<cwd>/.nova/frontend/tui/` 直挂 `tools/<tool>.ts` 渲染器、`dialogs/<name>.ts` 对话框（散养专属文件约定，默认导出 DialogFactory）与 `index.ts` 扩展入口；user 级永远可信，project 级未信任不 stat 不 import；注册顺序即覆盖优先级（project > user > package > builtin），碰撞诊断复用既有机制。

### Added
- **扩展能力全量开放（前后端严格分离）**：
  - 后端（事实/持久化）：新增 `appendEntry` RPC（session 域——任意包形态经 `invoke` 产生持久化 custom 条目，`entry_appended` 事件实时进转录；B 型纯 TS 包的 `registerEntryRenderer` 自此闭环）；新增 `getSessionAgents`/`getPersonas`/`setPersonaOverride` RPC；快照加 `agentName`/`personaOverride`；`session_info_changed` 事件扩为三字段全量值（改名/换角色/换 persona 三处发射，前端快照直写）；
  - 前端（Node 宿主原语，ctx 新增）：`events.on`（事件观察口——bus 只读桥、异常隔离、通配 `*`，包重载自动注销；B 型包可做宿主同级响应式 UI）、`runInteractive`（TUI 挂起/恢复终端让位，自 interactive-shell 对话框上移）、`setTitle`（终端标题覆盖，与宿主自动标题协调）、`notifyDesktop`（OSC 桌面通知）、`setFooter`（footer 整件替换——pi `ctx.ui.setFooter` 对位：env 回灌 git branch/扩展状态/快照/invoke，undefined 恢复默认，异常回退默认渲染）；footer 内建行显示当前角色与 persona override（`角色·persona · model · thinking`）；
  - **OSC 8 链接的边界定案（与 pi 同哲学）**：assistant markdown 链接可点（pi-tui Markdown 内建 OSC 8，vscode/iTerm/kitty 等探测即发射——PTY 真实 LLM 轮次验证）；**工具卡片刻意保持纯文本**——工具输出是过程证据，逐行链接在密集输出下是视觉噪音（OSC 8 在 VS Code 常驻下划线），可点性归 assistant 汇总层承载。`scripts/pty-links.py` 双向断言（markdown 必发射 + 工具卡片 file:// 零发射的回归守卫）。曾实现过工具卡片全量链接化（标题/grep/find/read），评审后按"密度-价值"原则回退，仅留此记录备查。
  - **/compact 空会话三连 bug**：①`ctx.compact` action 原来返回 None（fire-and-forget `create_task`）——命令 handler 的 `await ctx.compact()` 直接崩 "NoneType can't be used in 'await' expression"，且后台任务把真实错误（"Nothing to compact"）静默吞掉；改为返回 `asyncio.ensure_future` 的 Task 且异常在无 on_error 回调时传播（await 方拿真实错误，fire-and-forget 在任务日志可见）；②mirror 的 `compaction_end`/`auto_compaction_end` 无条件置 `working`——按"压缩后必有 run 续跑"的错误假设，失败的手动压缩把前端状态永远卡在工作态（后端 idle 但 spinner 永不停、Esc 无效）；改为 `will_retry ? working : idle`（overflow 错误恢复才续跑）；③`_compact` 命令补 try/except——失败经 `_reply` 给真实错误（"压缩失败: Nothing to compact (session too small)"），不再出现误导性 NoneType 扩展错误。PTY 验证：空会话 /compact 立即报错无 spinner；happy path 真实摘要生成（2244 字）。
  - **/agent 切换语义定型为"角色初始态全量重建 + session_start 重放"（每个角色权利平等）**：①change_agent 不再携带旧角色激活集（携带会击穿角色能力边界）；②补发 `session_start(reason="agent_change")`——此前切换不重放启动钩子（save_agent/reload 都重放），扩展的条目恢复不跑；agent 条目恢复在 agent_change 下跳过（否则分支旧角色条目把刚切的角色切回去）；③tool-panel 条目角色标签 + agent_change 重放组合出"每角色面板记忆"（切回某角色恢复该角色上次的开关集）；④前端 `session_info_changed` 触发 refreshSnapshot 全量对账（/agent 切换后 activeTools 快照不刷新的根因）。change_agent 改异步（重放需 await），调用点同步更新。
  - **open/strict 视野切换全链路修通（PTY 验证 `scripts/pty-role-boundary.py` 全过）**：①tool-panel 会话条目加 `role` 标签——面板 delta 归属角色上下文，恢复时仅当条目角色匹配才应用；②设置面板搜索失效的根因——`SearchableSelector` 把 placeholder 塞进了输入值（每个字符都拼在占位文本后，必无匹配），改 PlaceholderInput 包装（空查询时渲染 dim 占位、不污染值）；③`updateGlobalSettings` 未知键守卫只认 snake 名，camel 键（面板唯一发法）全部被 `-32602` 静默拒绝——**设置面板所有 camel 项的持久化从未生效**，守卫改为 snake+alias 双收；④资源管控键（tools/role_boundary 等）写入后此前不重解析（视野不变），updateSettings 现对这类键自动触发会话 reload。
- **/model 与模型循环交互修复（PTY 全覆盖验证，`scripts/pty-model-matrix.py` 14 断言）**：
- **/model 与模型循环交互修复（PTY 全覆盖验证，`scripts/pty-model-matrix.py` 14 断言）**：
- **pi 细粒度 UI 控制面对平**：`setHeader`（启动区整件替换）、loader 三旋钮（`setWorkingMessage`/`setWorkingIndicator`（frames/intervalMs，frames:[] 隐藏帧）/`setWorkingVisible`）、`widgetBelow` 区域（编辑器下方 placement——pi setWidget 双 placement 对位）、`registerCommand` 支持 `getArgumentCompletions`（slot 命令参数补全，经 pi-tui SlashCommand 通道进编辑器补全）。
  - 纪律写死在 `extension-api.ts`：ctx 只收"后端够不着的宿主原语"，后端方法的访问面就是 `invoke`（生成方法表带类型、全量、零维护），不手写包装域。

### Changed
- **资源与权限体系重构（四层漏斗终态落地，设计见 `packages/nova_harness/examples/resource-permission-refactor.md`）**：
  - **名单代数统一**：新建 `core/utils/name_sets.py`（名字级）与既有 `apply_patterns`（路径级）同一语法两个作用域——三态（缺席=全放 / 空=全禁 / 名单）+ 四级条目（plain/`!`/`+`/`-`，单名单内"宽排除+精确豁免"语义，与 pi 逐条对齐）+ 零命中诊断 helper；
  - **agent.yaml 三态化**：`tools`/`extensions`/`user_tools`/`commands`/`skills` 五字段 Optional 化（**行为变更**：`tools` 未声明从"无工具"变"全放"；显式 `[]` 从"全放"变"全禁"），支持 `!` 排除；删除从未生效的 `subagents` 死字段；补 `source_info`（resolver provenance 透传，七类资源来源跟踪齐平）；
  - **ToolsManager 裁决单点化**：散闸 if 链删除，替换为 settings pattern（用户终裁）→ SDK allow/exclude（宿主硬闸）→ yaml 名单（`role_boundary=strict` 时入注册表）的单点求值；新增 **`role_boundary` 设置**（`open` 默认=创造态：yaml 名单只做初始激活集、/tools 面板可见全池；`strict`=硬边界）；
  - **CapabilitySelection 报告**：yaml 点名项的裁决结果（ok/missing/disabled_by_settings/disabled_by_sdk）随 `getSessionState` 快照透出（仅非 ok 项），TUI 启动渲染为通知——"角色为什么少个工具"有确定性答案；
  - **settings 层补齐**：新增 `tools`/`user_tools`（名字 pattern）与 `personas`（路径+pattern）三键 + SettingsManager 类型化 setter；新增意图级 RPC `excludeResource`/`includeResource`（增量语义：只增删单条 `!name`，写后自动重解析注册表 + 重算系统提示词）；
  - **CLI 补 SDK 投影**：`nova-harness run` 新增 `--tools/-t` 与 `--exclude-tools/-xt`（CLI 与 SDK 同一层，纯透传）。
- **persona 升格为资源类目**：`[tool.nova]` 新增 `personas` 类目（B 型包同语义）；三源发现（包 + `~/.nova/agent/personas` + `.nova/personas`，project 源 trust 门控）；命名=相对 personas 根去扩展名；新建 **PersonaManager**（装配从 agent_config loader 乔迁：路径引用 + 包根收敛校验 + 注册名解析；override 旋钮）；`/persona` 命令（选择器/直切/default 清除，会话条目持久化 + 分支恢复）；`AgentConfig.sections` 不再加载期固化（纯声明快照）。
- **AgentManager（新）**：角色旋钮归位（从 SystemPromptManager 迁出）、默认解析链、可委派视图；**`/agent` 命令**（选择器切换/直切）+ **`/agent save`/`save-as`**（当前生效状态物化回 yaml——包来源影子写 user 级、user/project 就地写回、写后 reload 生效）；`saveAgent` RPC（方法表 70→71）。
- **subagent 工具重设计（消灭 subagents 概念）**：工具消费会话 AgentConfig 注册表（`ToolExecContext.agents` 注入），删除工具侧三源发现/`agent_scope`/独立 trust 判定（重复管线归零）；新扩展 `subagent_gate.py` 执行前确认（自治权检查点：允许一次/本会话始终允许（会话条目持久化、分支恢复）/取消；无 UI 放行）；系统提示词新增 `# Available Agents` 动态菜单段（激活含 subagent 时，name+description+source 标签）。

### Fixed
- **/resume（及 /new、/fork、/tree、clone、import）切换后 transcript 不更新（"无法真的切换"的根因）**：后端会话切换一直成功，但前端只在启动时全量同步一次 transcript——会话替换后没有任何事件通知前端，界面永远停留在旧会话。修复：新增 Bus 2 事件 `session_replaced`（`SessionReplacedEvent`，带 `reason`），发射点覆盖全部九处替换路径——runtime 重建（new/resume/fork/import，`_finish_session_replacement` 统一收口，**rebind 之后发射**，否则 RPC 事件桥还订在旧 session 上）、AgentSession 原地切换（new/resume/fork/clone/import）与树导航（navigate）；前端 `app.ts` 监听后全量重同步（快照 + 历史条目）并重绘 transcript、刷新 footer/resources、对账包 UI 贡献（cwd/trust 可能随切换变化）。线上契约同步再生成；新增回归测试（事件类型/reason/rebind 先于发射的顺序断言）+ 常驻 PTY 验证脚本 `scripts/pty-resume-switch.py`（命名会话 + 标记消息 → /new → /resume 选回，断言增量输出回显标记与会话名，实测通过）。

### Added
- **pi examples 必要拓展五件补齐**（工具类归 tools、交互类归 extensions 的纪律不变）：
  - **question 工具多问版**（pi questionnaire 对位，Claude Code AskUserQuestion 同能力）：schema 新增 `questions` 数组（1~4 问，`anyOf` 两形态），多问优先/非法即错/单问路径逐字等价；dialog 单框升级 `QuestionnaireDialog`——tab 条分页（✓ 已答标记、答完自动跳页、←/→/tab 切页、全答完 Enter 提交、Esc 取消），降级路径逐问串行两步；渲染器多问形态（等待中逐问高亮 + 进度 `waiting for answer… (1/3)`，完结逐问 ✓）；
  - **`/tools` 工具开关面板**（新扩展 `tools_panel.py`，pi tools.ts 对位）：`dialog:tools` 复选面板（Space 切换/Enter 提交/Esc 取消，`{active: [...]}` 绝对集 `set_active_tools`）+ 无 UI 文本回退；`tool-panel` 条目持久化，session_start/session_tree 从分支最新条目恢复（分支安全）；
  - **交互式 shell 终端让位**（新扩展 `interactive_shell.py` + `dialog:interactive-shell` 组件，pi interactive-shell.ts 对位）：user_bash 拦截 14 程序集（vi/vim/nvim/nano/emacs/htop/top/less/more/man/ssh/tig/lazygit/watch）或 `i ` 前缀强制——组件经 `env.tui` 挂起 TUI（setImmediate 避开工厂同步窗口陷阱）、清屏、`spawnSync(stdio: inherit)` 跑真实 TTY、恢复后 `{exitCode}` 回执替换执行；无能力/无 UI 回 `(interactive commands require TUI)` 防挂死；**零 nova-client 改动**（dialog 工厂 env 本就有 tui 句柄）；
  - **confirm-destructive**（新扩展 `confirm_destructive.py`，pi 同名对位）：`session_before_switch`/`session_before_fork` 确认门——有 UI 且会话非空时 confirm 弹窗，选否/取消经类型化结果 `cancel=True` 取消切换（runtime 读 `result.cancel`，纯 dict 会被 getattr 读丢——契约核实后的必要选型）；headless 放行；
  - **turn 结束桌面通知**（nova-client `terminal.ts`，pi notify.ts 对位）：`bus.onDerived('turn:ended')` 精确触发，OSC 9/777/99 三序列并发（writer 可注入 + 控制字符剥离），`desktop_notify` 设置项进设置面板（17 项，默认开）；附带修复 `MirrorStore` 缺失的 `emit('status')` 死路径（状态迁移时补 emit 且不重复——OSC 9;4 进度与状态指示器自此接通）。
- 测试：backend 356（+34）、bundle frontend 178（+31）、nova-client 308（+6）全绿。

### Fixed
- **命令反馈泄漏进 LLM 上下文（纪律违例全面审计与收拢）**：审计发现 harness 的 `convert_to_llm` 本就 pi 对齐（custom→user 文本）——所以 `/login`、`/export`、`/trust` 等反馈消息此前全部以 user 身份喂给了模型（噪音），而 plan_mode 的上下文注入其实一直正常（早前"注入是哑的"的判断系误读 nova_agent 默认过滤器，工厂实际注入了 harness 转换器——在案订正）。按"custom 消息只留上下文注入"纪律迁移：session_commands 36 处 + tools_panel 2 处 + plan_mode 完成卡 1 处，全部改走 `append_entry("command_result"/"plan-complete")`（转录卡片 + 持久化 + 不进上下文）；plan_mode 的 `deliverAs=followUp` 真注入保留。探针验证：/trust 反馈不再进 LLM 上下文，custom→user 转换对注入类消息正常。
- **auth.json 静默丢凭据（数据丢失级）→ AuthStorage 重写为 pi 式哑 JSON 映射**：根因是存储层做了逐条 pydantic 校验且静默丢弃失败条目，重写时丢失（登出 A 连带抹掉写法不标准的 B）。pi 的做法是存储层零校验（JSON.parse 直进直出）、校验在消费点——对齐重写：`_data` 存原始 dict，读写原文保留一切未知/损坏条目；`_materialize` 仅在 `read`/`modify` 回调入参物化类型化 Credential（失败按未配置处理，可正常 /logout）。同时订正一个误判：auth.json 的 `key` **支持** `$VAR`/`!cmd` 引用（`AuthStorage.read` 经 `resolve_config_value` 在读取时解析，与 models.json 同语义——昨日"$VAR 会 401"的说法不成立，实测 `$VOLCENGINE_API_KEY` 正确解析）。
- **/login //logout 边界语义补全**：①已配置凭据的 provider 重登前弹确认（"重新登录将覆盖现有凭据"，选否不动）；②登出未配置 provider 此前假报"已登出"（delete 对不存在键静默 no-op）——现按 `get_provider_auth_status` 给准话：未配置→"无需登出"；凭据来自环境变量/models.json（不在 auth.json）→拒绝并给指引；③实测写穿验证：stored 凭据登出即时从 auth.json 移除（含文件级回归探针，用户的 volcengine/kimi/kimi-coding 凭据测后原样恢复）。
- **取消反馈归一（Esc 取消登录等 slash 命令只出现一次）**：分析三条取消路径后定调——反馈归**前端**（取消由 Esc 前端发起、即时可靠；后端任务被取消后 send_message 的 await 不可靠，且 headless 下 /login 入口即拒、无受众）。`runSlashCommand` 的 AbortError 保留 `addInfo('已取消')`，后端 `/login` 的重复"登录已取消"消息移除。PTY 验证：确认 → 授权框 → Esc，恰好一条"已取消"。
- **/login 两处瑕疵**：①授权 URL 不自动打开浏览器——`UIAuthInteraction` 在 auth_url/device_code 事件上 `webbrowser.open`（同 URL 每流程去重、失败静默，URL 仍在等待框可点）；②"Waiting for authentication..." 残留——根因是 device_code 事件除等待框通知外**另发了一条冗余 progress 通知**，轮询期每次事件重发、登录结束后仍常驻状态行；直接删除该冗余通知（等待框自带等待文案与取消入口，一处呈现），辅以"空 progress = 清除状态槽"语义与 `/login` finally 清除兜底。PTY 验证：取消后末屏无残留。
- **prompt 模板与真命令无法区分**：`/refactor`（展开后发给 LLM）和 `/model`（本地动作）在 UI 里外观一致。线上 `getCommands` 本就带 `source: extension/prompt/skill`，目录合并时丢失——现保留为 `kind` 字段：补全描述前缀标注"提示词模板 · /技能 ·"，`/help` 目录按"后端命令 / 提示词模板（展开后发给模型）/ 本地命令 / 扩展命令"分组。
- **RPC 出参裸奔（Esc 中止崩 TUI 的根因，系统性契约洞）**：router 只在入参侧校验，`result_model` 形同虚设——handler 返回 snake dict 直接上线，前端按生成的 camel 契约消费等于读 undefined。`clearQueue` 的 `follow_up` 在 Esc 中止时把 `followUp is not iterable` 砸成未处理 rejection 杀进程。修复：dispatch 对声明了 result_model 的出参做 `model_validate + dump_wire`（RootModel 走 `model_dump(by_alias=True)`）；校验失败透传原值并告警（契约违约可见但不掐断响应）。10 个断言旧裸奔形状的测试修正为线上 camel 契约；顺带发现 `listPromptTemplates` 的 `path` 是契约从未承诺的死字段（在案，可选清理）。
- **空会话 `/clone` 抛裸 ENOENT**：与 `/export` 同类——文件未落盘时给可读错误（"当前会话还没有内容，无法克隆"）。
- **PTY 冒烟矩阵扩至 45 例（34 个命令全覆盖）**：补齐 clone/compact 族/new（确认框）/plan 开合/packages 面板/login/logout 选择器/share 降级/prompt 模板展开与 Esc 中止/真实 LLM 一轮/quit 进程退出判定；关闭类用例改"Esc 后 /debug 探针证明控制交还"（pi-tui 差分渲染不保证文本帧，像素断言不可靠）。
- **/help 只见半个世界（后端 26 个 vs 实际可用 34 个）**：本地命令（/theme /settings /copy /hotkeys /share /changelog /quit）与 slot 命令（/packages 等）不进后端注册表，后端 /help 只列 26 个且会误导读本（如列出被本地遮蔽的 /debug prompt）。修复：抽 `commands/directory` 三源合并为单一事实源（补全目录与 /help 共用，按名去重、覆盖优先级=分发现实、白名单同判、null 名消毒）；/help 宿主本地化（分发现实本就走本地段）——新 HelpViewer 模态按"后端/本地/扩展"分组展示全量 34 个；后端 /help 保留作 headless 回退。PTY 矩阵 +2（目录打开/关闭），共 30/30 全绿。
- **custom 消息全链路不可见（"命令没反应"体感的最大来源，四层断裂）**：①后端边界错位——扩展惯例 `{"type","text"}` 与 `send_custom_message` 期望的 `{"customType","content"}` 不匹配，所有命令反馈消息创建时即为空壳（归一：两族键名都收）；②序列化剥壳——`AgentMessage` 联合里无字段的 `CustomAgentMessage` 基类把 `CustomMessage`/`BashExecutionMessage` 子类剥成 `{}`（nova_agent 与 harness 两套 MessageStart/Update/EndEvent 均加运行时类型 field_serializer，线上 custom 载荷从 `{}` 变全量）；③mirror `message_end` 只认 user/assistant——新增 custom 分支（role=custom 按 customType 建条目、display=false 上下文注入不进转录、具名 role 按 role 建条目）；④bash 用户工具流式聚合——`user_tool` chunk 事件按 callId 累积单卡片、终态消息定稿合并（不再逐块堆空卡），`BashExecutionView` 改读线上 camel 键（exitCode 等此前静默全丢）。
- **扩展错误静默吞噬（silent failure 的系统性根源）**：`extension_error` 事件此前只进事件流不进转录——handler 崩溃在用户眼里就是"没反应"。mirror 新增 `extension_error → error notice` 映射；该事件补入 `AgentSessionEvent` 联合与线上信封（schema 再生成）。本批已靠它现场抓获两个真 bug：`/help` 的 `SlashCommandInfo.resolved_name` 属性误用（真实类型只有 `.name`——替身测试掩盖）、空会话 `/export` 的 ENOENT（改抛可读错误"当前会话还没有内容可导出"）。
- **PTY 冒烟矩阵（`scripts/pty-smoke.py`）**：真实 TTY 逐命令断言 29 例全绿——slash 命令（help/session/todos/tools/scoped-models/model/resume/tree/fork/name/export/trust/untrust/reload/import/debug）+ 本地命令（hotkeys/changelog/settings/theme/copy）+ 用户 bash 流式渲染。
- **TUI 启动从不读 trust.json（"信任过下次还问" 的最终根因）**：`AgentSessionServices` 的信任决议委托给 `resolve_project_trust` 回调，但**只有 print 模式接线**——RPC/TUI 启动永远默认不信任、不查记录。修复：抽共享回调工厂 `make_resolve_project_trust_callback`（print 无 UI 形态 + RPC 有 UI 形态共用同一条决议链：trust.json → `default_project_trust` 设置 → 启动信任框），`createSession` 正式接线；print 模式同厂重构（行为不变，杜绝两份决议逻辑漂移）。
- **后端 stderr 杂讯直喷 TUI 画面（/trust 触发 reload 时集中爆发）**：三层治理——①总闸门：RPC 后端启动时 stderr 重定向到 `~/.nova/agent/logs/rpc-stderr.log`（fd 级全覆盖）；**接管条件是 stderr 为进程原生对象（`sys.stderr is sys.__stderr__`）而非 isatty**——后端的 stderr 其实是管道（Node 端 `stdio: pipe` + 转发到终端，正是污染路径），而 pytest 会替换 sys.stderr（dup2 劫持其 fd 会破坏宿主进程全部用例，首版 isatty 门两处全错）；②消息类型注册幂等化：同类重复注册静默跳过（reload 重注册是常态非冲突），真碰撞（异类同 role）保留警告；③manifest "path 依赖解析出包根" 警告进程级去重（pkgList/启动/reload 反复读同一 manifest 不再刷屏，测试侧配 autouse 清表夹具）。
- **`/trust` 形同虚设（三级断链）**：①决策只写进程内存、不落 trust.json——重启即忘；②信任翻转后不重载资源——项目级工具/扩展要重启才进出场；③后端重载前端无感知——包 UI 贡献（渲染器/对话框/slot 命令）要重启才刷新。修复：`trust_project` 持久化 `trust.json` + 进程内即时翻转；`/trust` `/untrust` 处理器自动 `ctx.reload()`；新增 `session_reloaded` 会话总线事件（`session.reload()` 末尾发射，线上契约同步再生成），前端收到后先 `refreshSnapshot()` 对账（`projectTrusted` 是 trust 过滤的判据，不先刷新会把刚信任的项目包继续误杀）再 `refreshPackages()` 整体重载 slots。验证：SDK 级回归（持久化/翻转/事件三断言）+ 真实 TUI dump（`/trust` 后快照 `projectTrusted: true`、25 槽全数在场）。
- **pkgList 合并去重在未信任项目内吃掉 user 包（bundle 前端贡献全灭的根因）**：`list_with_resources(local=False)` 合并 user+project 时 project 按身份优先去重且**不看信任**——cwd 位于未信任项目（如仓库内 `tmp/`）时，project 副本挤掉 user 副本后被前端 trust 门过滤，出现"包装着但 slots 只剩 builtin、/tools 等全不弹"的窗口。修复：项目不被信任时合并视图整组剔除 project 级（与 resolver 读取门控、前端 partitionByTrust 同语义），user 副本正常浮出水面；`local=True` 管理视图不受影响（装/卸是主动行为）。harness 三向回归测试。
- **first-time 引导框每次启动都弹出并吞掉输入（"命令不能用"的元凶）**：判定门读 `settings.default_model`，但 getSettings 线上经 `dump_wire()` 是 camel（`defaultModel`）——永远读到 undefined，每次启动都弹"先配置模型"，用户输入的 `/model` 全进了引导框过滤栏（显示"无匹配"）。修复后已配置默认模型不再弹；PTY 全链验证（选择器打开 → setModel → footer 即时更新）。
- **settings 消费侧 snake/camel 系统错位**：同一病灶波及 `quietStartup`（安静启动从不生效）、`doubleEscapeAction`（自定义回退默认）、`hideThinkingBlock`（跨会话恢复丢失）、设置面板 5 个后端持久化项 id——统一为线上 camel 键（写入本就走 `populate_by_name` 双收，只读侧断）。
- **refreshPackages 静默吞错**：catch 空实现会把真实加载失败藏成"0 packages"假象——改为 `store.addNotice('error', ...)` 透出。
- **补全目录三源混乱（同名重复 + 占位描述）**：slash 补全此前把后端 RPC（26）+ 硬编码本地（8）+ slot（6）平铺入表——tree/todos/model 等同名出现两次且 slot 条目只有通用"扩展命令"占位。改为按名去重（覆盖优先级 = 分发现实：本地 > slot > RPC），`registerCommand` 注册时把 description 附着函数对象供补全消费；同名 `/debug` 注明本地镜像 dump 遮蔽后端 prompt 模板（pi 对位）。去重后目录约 32 条、条条有真实描述。
- **getCommands RPC 命令名异常（两级根因）**：①扩展命令序列化用 `invocation_name`（未重命名时 None），线上变 null——前端补全 `value.startsWith` 未处理 rejection 杀进程；②修复时又踩 `RegisteredCommand.resolved_name` 是普通方法而非 property（与兄弟 `extension_path` 不一致的陷阱），把 bound method 序列化上屏。**根治**：`resolved_name` 改 `@property`（与 `extension_path` 对齐），全部调用点统一为属性访问（agent.py、loaders/extensions.py、runner 测试）；前端 `setupAutocomplete` 加输入边界消毒（非字符串名直接丢弃）。真实 RPC 端到端探针验证：26 个命令名全非空字符串。
- **命令白名单可被程序化入口绕过**：`runCommand`（ctrl+l、双 Esc 导航、--resume 启动、first-time 引导）此前不查 `isCommandEnabled`——agent.yaml `commands` 允许集裁掉的命令经热键仍能打开包自持 UI。修复后与编辑器输入分发同判（快照 `allowedCommands`/`disabledCommands` 门控，被拦提示"已被当前 agent 配置或用户设置禁用"），并补测试（允许集排除/放行、disabled 黑名单三态）。

### Changed
- **nova_coding_agent 目录纪律整顿**：测试布局全面镜像化——`backend/tests/` 拍平顶改镜像 `backend/`（`tools/` 10 工具一文件一测、`extensions/`、`user_tools/`、`nova_coding_agent/{bash,subagent,tools_common}/`；拆分 861 行大杂烩 `test_tools.py` 按工具归位、`test_grep_find.py` 拆 `test_grep.py`/`test_find.py`、`test_image_process.py` 改名 `test_image.py` 对齐模块名、`test_session_bash.py` 按引擎/用户工具拆分）；`frontend/tests/` 镜像 `tui/`（`renderers/` 旧名改 `tools/`、`edit-preview` 归 `lib/`、六个 slash 选择器测试补全 `extensions/session_commands/slash/` 前缀路径）；README 目录树重写（此前停在 9 工具/17 命令/2 扩展的旧版）；根 `.gitignore` 补 `.nova/`（本地 dev 安装记录含绝对路径，防误提交）。测试数 356→362（+8 拆分、-2 纯重复）。
- **`/help` 命令**（session_commands 第 19 个命令，pi `/commands` 示例对位）：`ctx.get_commands()` 全量列出已注册命令（含其他扩展注册的），按调用名排序 + 名称列等宽对齐，无描述给占位，附 `!` bash 前缀提示；空注册表明确提示。
- **8 个工具渲染器单测补齐（58 个新用例，渲染器测试覆盖率 2/10 → 10/10）**：`bash`（视觉行折叠/计时滴答/exit code/截断警告/footer 剥离）、`edit`（patch 解析/降级替换/preview 钩子三态）、`read`（折叠零内容 pi 语义/展开 meta 表/图片回执）、`write`（10 行预览/无效参数回执）、`find`/`grep`/`ls`（折叠预览/截断警告/空结果）、`question`（等待中选项列表/完结 ✓/(wrote)/Cancelled 三态）；全部断言经探针脚本核实真实行为，未改渲染器源码。

- **交互式能力大满贯（question 工具 + plan_mode 扩展 + dialog:\* 自定义词汇槽位）**：
  - **question 工具**（pi question.ts 对位，`ToolExecContext.ui` 通道首个消费者）：选项选择 + "Type something." 自由输入；能力门控双路径——`dialog:question` 已注册走包侧单框（选项+内联编辑器同框，组件 `frontend/tui/dialogs/question.ts` 为 pi 单框逐行移植），否则基线两步降级（select_items→input）；`execution_mode="sequential"`；headless 错误回执、取消合法回执（answer=None 非错误）；
  - **dialog:\* 自定义词汇槽位**（pi 后端扩展直驱 UI 的对位出口）：`ExtensionUIAPI.registerDialog(name, factory)`——后端经 `ui.request("dialog:<name>", params)` 调起包侧组件，done(result) 按 `{value}` 应答、undefined 按 cancelled；**注册即触发 `system/capabilities` 能力重宣告**（后端 `has_capability` 实时放行——握手后注册也生效）；ui/cancel 撤销与 abort 竞速与基线对话框同语义；槽位键即线上 componentType，零映射；
  - **plan_mode 扩展**（Claude Code 风只读规划模式，pi plan-mode 逐行移植）：`/plan` 命令 + ctrl+alt+p 快捷键 + `--plan` 旗标；开启时 edit/write 从激活集移除（`set_active_tools`，模型不可见比拦截更硬）、bash 限只读白名单（危险否决+白名单放行双判，tool_call 拦截）；`before_agent_start` 注入规划/执行上下文（display=False 自定义消息）、`context` 事件非规划态滤除过期注入；"Plan:" 编号计划提取 → `agent_end` 询问（执行/留在规划/改进）→ 执行态 [DONE:n] 进度跟踪（`turn_end` 解析）→ 全部完成自动收尾；状态经 `append_entry("plan-mode")` 持久化，session_start/reload 从条目重建；
  - **`set_status` 展示类词汇**（pi `ctx.ui.setStatus` 对位——反向原语第六词）：命名通知 `ui.notify("set_status", {key, text})` 驱动 footer 扩展状态行（key 幂等覆盖、空文本清除），TUI 能力面宣告 + `ui_primitives` 糖库与 `STANDARD_METHODS` 收录；plan_mode 为首个消费者（⏸ plan / 📋 n/m / 清除）。
- **包间依赖声明 `requires`**（包系统终局拼图）：A 型 `[tool.nova] requires = ["包名"]`、B 型 package.json `"nova": {"requires": [...]}` 同一语义——安装时校验被依赖 nova 包已安装（user/project 合并视图），缺失即拒绝（副作用前）并附安装提示；卸载时被其他包 requires 引用的包拒绝卸载并列出依赖方；`PackageMetadata.requires` 透出（`nova-pkg info` 显示）；v1 约束校验不做来源解析。
- **`createSession.noSession` 契约开关**（pi `--no-session` 对位）：内存态 SessionManager 注入（不落盘不进会话列表，与 print 模式同机制）；与 sessionFlag/continueLast/sessionFile 互斥（参数错误先于 runtime 重建）；TUI `--no-session` 旗标直通（摘掉"暂未生效"警告）。
- **启动 `@file` 图片附件**（pi file-processor 对位）：`expandFileArguments` 升级为 `{text, images}`——文本内联 `<file>` 块、图片经魔数嗅探（png/jpeg/gif/webp，与后端 read 支持集一致）转 ImageContent 附件 + 空体引用占位；`initialImages` 经 app → submitText → prompt RPC `images` 参数全链路透出；差异在案：无 auto-resize（无 TS 图像库，压缩归后端 read 路径）。
- **bash 卡片计时滴答**（pi bash Loader 对位）：`RendererEnv.elapsedMs`——running 态由 ToolCardView 持表（500ms interval，done/error 即停），渲染器纯函数只读展示（`Running… X.Xs`）；**防泄漏取舍在案**：重建模型下渲染器产物级 interval 必泄漏，故滴答锚在宿主视图而非渲染器组件。
- **npm dist-tag 引用**（`npm:pkg@beta`）：spec 层非版本语法按 dist-tag 接受（存在性交由 resolver 查询时校验），resolver 命中 dist-tags 直取具体版本；未知 tag 报出可用 tag 清单（取代裸 semver 语法错误——npm 对齐语义）。
- **工具执行期 UI 通道（ToolExecContext.ui / has_ui——pi ctx.ui 对位）**：包工具执行中途可弹确认/选择（反向原语经 RPC 到前端，**Node 只渲染**，工具逻辑不出 Python——与"Node 不做工具执行载体"的定案同向）；
  - **两条纪律收进注入点**（`ScopedUIContext` 承载，工具作者零 signal 代码）：①run abort 竞速——Esc 中止 run 时挂在半空的对话框被 `ui/cancel` 撤掉、按 cancelled 解决（信号源与扩展侧同为 agent 当前 run 的 signal，每次请求现取，idle 自动豁免）；②**弹窗串行锁**——会话级一把 `asyncio.Lock`，一轮内并行工具调用的 UI 请求排队而非互踩（扩展事件 handler 顺序派发天然无此问题，工具并行是真问题）；已 abort 的请求预检直放、不参与排队（自愈：锁释放后由 transport 立即按 cancelled 解决）；
  - **设计定调**：默认绑定 run signal + 显式传参永远优先——pi 靠作者自觉的前提是单进程（忘传 signal 是软失败），跨进程架构下忘传的代价是挂起 + 僵尸对话框，故默认值必须安全；域级信号归事件荷载显式传入（决策权归真正知情方），组件私有信号归作者自持；
  - headless/无 UI 前端时 `has_ui=False` + NoOp 句柄安全降级；`NULL_TOOL_EXEC_CONTEXT` 同步兜底；
  - 测试：harness 1318（串行锁区间无交叠/abort 预检直放/绑定前后 has_ui/共享锁/NULL 兜底 6 新用例）+ coding 286 全绿。
- **bundle 全量对齐 pi（不止 tools）——subagent 深化 + todo 工具 + 工作流内容**：
  - **subagent 三源发现**：`discover_agents` 从"仅 user/project 目录"扩为**包源地板 + user + project** 三源合并（同名覆盖 project > user > package；包源扫描已安装包的 `agents/*.yaml`，与 `list_installed_agents` 同源但载入完整组合声明取 description/model/tools）——此前包内 agent 对 subagent 工具不可见（`Unknown agent` 误报）；
  - **Project Trust 门控**（pi `confirmProjectAgents` 的 nova 对位）：pi 在工具内弹 confirm；nova 工具是纯执行器，复用会话启动的信任裁决——显式不信任（False）时排除项目源并在未知 agent 错误里附 `/trust` 引导；无记录按 headless 默认信任放行（与子进程自己的 trust 解析一致）；
  - **聚合流式回调**：runner 的 on_update 统一为全量结果列表形态（parallel 含 `exit_code=-1` 运行中占位、chain 含已完成+当前步骤），渲染器可见 "n/m done" 总进度（pi 并行流式 details 对位）；
  - **subagent 四件套代理**（`agents/{scout,planner,reviewer,worker}.yaml` + `backend/personas/subagents/` 四份人格）：scout 只读侦察、planner 只读规划、reviewer 评审、worker 全能力执行（显式不含 subagent 防递归）；每代理工具白名单经组合声明裁剪；
  - **工作流提示词三件套**（`backend/prompts/`）：`implement`（scout→planner→worker）/ `scout-and-plan` / `implement-and-review`——`$@` 占位 + `{previous}` 链式传递；
  - **agent yaml `model:` 字段接线**（曾是死字段）：进入初始模型解析链 tier 4（CLI/scoped 之后、settings 默认之前），复用 CLI 解析（模糊匹配 + 自定义模型 id + 警告透出），无鉴权/未知 provider 静默落回——scout 类轻量代理可声明便宜模型；
  - **todo 工具（全量替换语义）**：模型每次发完整清单原子替换（Claude Code TodoWrite 实践），工具零服务端状态——状态单一事实源是会话里最新工具结果的 details，分支/fork/树导航天然正确；`/todos` 命令（后端 headless 文本回退扫 `get_branch` + 前端模态查看器经 `getSessionEntries` 回溯 leaf 自组分支）；
  - **前端渲染器两件套**：`subagent.ts`（三模式折叠/展开、运行中 ⏳ 占位与并行进度、工具调用格式化行、展开态 Markdown 终输出 + 每步/总计 usage 行）、`todo.ts`（进度行 + ○/◐/✓ 清单，折叠前 5 条）；
  - `coding_agent.yaml` 激活 subagent + todo；测试：backend 286（subagent 重写 + todo + 聚合回调 + 信任门）、harness 1312（agent_model 四层用例）、frontend 44（todos 分支回溯 + 渲染器冒烟）；真实冒烟：`nova-harness run scout` 真实 LLM 一轮通过（包内 agent 解析 + persona 装配 + 模型链）。

### Changed
- **四个前端自持选择器整体迁入官方 bundle**（/model /scoped-models /resume /fork——/tree /todos 之后的第二批 dogfood）：组件与编排在 bundle `frontend/tui/extensions/session_commands/slash/{model,scoped-models,resume,fork}/`，nova-client 删除三控制器两专用选择器（宿主只留 runCommand 命令通道 + 通用选择器件）；新增后端 `/scoped-models` headless 回退命令（含 harness `get_scoped_models` 扩展动作一环）；ctrl+l/双 Esc 改推命令；`/tree <id>` 带参形态补齐（直跳 navigateTree，与后端同语义）。
- **nova-client 测试统一外移 `tests/`（37 个测试文件，镜像 src 子路径）+ keymap 管理器上移为运行时能力子系统**（`modes/tui/keymap/` → `src/keymap/`，pi `core/keybindings.ts` 对位）：加载/三级合并机械与保留键位对账归运行时层，宿主经 `create({defaults, reserved})` 注入自家表——**机械上移、方言留下**：TUI 默认键位表（app 级绑定 + pi-tui 内建合并 + 保留清单）留在 `modes/tui/keymap/tables.ts`（ctrl+l 类终端自由的键本就宿主特定）；`presentation/` 保持 pi-tui-free 不动。TS 测试约定与 bundle 统一：包根 `tests/` 镜像 src 子路径（`tsx --test "tests/**/*.test.ts"`），src 内零测试文件。

### Removed
- **`nova-coding-agent-ui`（B 型纯 TS 示范包）整体移除**（仓库包 + 已安装副本一并清理）：渲染器已由官方 bundle 全部自持，示范包只剩历史价值；**B 型包形态保留**（package.json 身份证 + npm 阶段 + 发现/加载链路不动，测试夹具 `test_b_type_package.py` 在），`requires` 等能力继续对 B 型生效。
- **RPC WebSocket 传输移除**（本发布线为 TUI-only，前端经 stdio 子进程挂载）：`transport/websocket.py`（`WebSocketTransport`/`WebSocketAcceptor`）、`nova-harness-rpc` 的 `--listen/--token/--token-file/--allow-origin` 旗标与 `rpc-server.json` 监听信息落盘、`ConnectionOrigin.WEBSOCKET`、conformance 套件 WS 参数化腿与 `test_websocket.py` 一并移除；连接化架构保留（stdio/memory 同为连接来源）。多客户端/远程前端的演进在 main 分支。

### Fixed
- **subagent usage 统计静默丢失（snake/camel 键名错位）**：runner 的 `_apply_event_payload` 照 pi 读 camel 键（`cacheRead`/`totalTokens`/`stopReason`/`errorMessage`），但 nova print 模式 JSON 流是 `model_dump` 原生 snake 形态——cache/totalTokens/stop_reason/error_message 全部丢失。改读 snake 键 + 回归测试。

### Changed
- **skills 名单语义翻转：空名单 = 全放（含包内），白名单收敛为纯 opt-in 人格裁剪**：
  - **动机**：旧默认（空名单 = 包内 skill 全灭）与 tools/extensions/user_tools 的空名单语义全部相反，且造成"用户装了 skill 包、agent.yaml 没点名就静默不可见"的经典困惑；安全职责本就不在白名单（项目级归 project trust，包安装是用户主动信任行为），skills 名单裁的只是**自动注入面**——包内 skill 的 name+description 每轮自动进系统提示附录（token 卫生 + 人格聚焦）；
  - **新模型（资源两类分治）**：能力类（tools/extensions/user_tools）= 门控后加载完全集 + 全来源白名单；文本类（skills/prompts）= 随时可加，skills 最多包内名单（空=全放）、prompts 无名单（零自动注入面，模板只在 `/name` 显式展开时进上下文）；
  - **实现**：`filter_skills_by_whitelist` 空名单直接全放；加载层死参数 `allowed_names` 删除（`load_skills`/`load_skills_from_dir`——名单过滤本就只在消费侧，src 零调用）；`AgentConfig.skills` 字段注释、`agent.py`/`system_prompt/manager.py` docstring 同步；
  - **测试**：`test_whitelist.py` 重写（空名单含包全放/非空裁包内/混源非包全放 5 用例）；`test_source_semantics.py` 翻转 + 新增未命中裁剪用例（真装包 + 真 SDK 会话）；system_prompt manager 用例翻转；
  - 文档同步：根 AGENTS.md（agent.yaml 字段行）、nova_harness/AGENTS.md（yaml 示例注释 + 字段说明 + 过滤链路描述）、examples/resource_loader_outputs.md。
- **线上契约全量 camelCase 化（LSP/FastAPI 惯例——Python 内部保持 snake，转换收敛在序列化边界）**：
  - **基类**：`NovaBaseModel` 加 `alias_generator=to_camel` + `populate_by_name=True`（反序列化双收）+ `dump_wire()`（线上形态统一出口）；
  - **RPC 边界全站 dump_wire**：serialize_event（事件广播）+ methods 全部结果点 + `to_json_safe` 兜底；**handler 的 params 读取保持 snake**（router 校验后 `model_dump()` 字段名——camel 只存在于线上 JSON 与契约工件，不进 harness 内部代码）；
  - **事件系统 66 个 dataclass 迁移 NovaBaseModel**（"跨进程用 Pydantic"纪律的补课——dataclass 无 alias 概念会造线上分裂）；迁移红利（Pydantic 校验逮出的真问题）：`SessionStartEvent.reason` 的 Literal 漏 'clone'/'import'（dataclass 时代静默越界）、`SetApiKeyParams` 双写字段（apiKey/api_key 并存——alias 撞车）、before_provider_headers 的原地修改语义需 `model_construct` 保引用（Pydantic 构造默认拷贝 dict）；
  - **shapes/类型层命名统一**：历史手写 camel 字段（customInstructions/authType/apiKey/entryId 等）全部归 snake；nova_ai 的 `CredentialInfo.providerId` 等 camel 残留（39 处）归 provider_id；
  - **契约导出按 alias**：schema_export 的 `_fields_of` 读 alias + `_has_default` 按 alias 反查（可选性标记修复）；gen.ts/schema.json 重导全 camel；
  - **前端消费点全量迁移**（TS 编译器引导，~60 文件）：runtime/mirror/export/controllers/测试断言；**export-html 的 mapKeysDeep 映射层删除**（线上 camel 后与 pi 三件套天然同构——零映射）；
  - **持久化保持 snake**（会话 JSONL/settings/auth/trust/dist-info 的 `model_dump()` 默认形态）——存量磁盘格式零迁移；
  - 全量绿：nova_ai 458、nova_agent 106、harness 1236、coding 228+12、nova-client 192（含一致性套件真实子进程 schema 校验线上帧）；**真实端到端冒烟**（`scripts/smoke-e2e.ts`——真后端 + 真 LLM 完整 turn：握手/快照/事件流/条目/stats 全 camel 零 snake 残留）。

### Fixed
- **命名纪律全仓审计与修复**（Python 纯 snake / TS 纯 camel / 线上纯 camel）：
  - **活 bug**：`packages/registry.ts` 按 `install_path`（snake）解析线上 pkgList——camel 化后恒解析失败（ui/ 资产发现静默全灭），改 `installPath`；
  - **线上 snake 泄漏三处**：`SourceInfo`（dataclass 漏网——迁 NovaBaseModel，gen 契约最后一个 snake 键清除）；`ui/request` 反向帧的 `componentType`（ui_context 裸 dict + bridge 消费点同步）；`getContextUsage` 的 `contextWindow`（stats 裸 dict + footer 同步 + 补 `GetContextUsageResult` result_model 纳入契约）；
  - **陈旧声明/测试 fixture**：mirror 的 HistoryEntry/ContentBlock 陈旧 snake 声明、`previous_model`/`model_id`/`context_window` fixture 修正；
  - **审计结论在案**：Python 侧零违规——扩展 API camel 方法名（pi 对齐）、RPC handler 函数名=线上方法名（自文档）、nova_ai auth 层 TS 移植对齐三集群确认为刻意设计；`exclude_from_context`（用户工具参数面）保持 pi 对齐豁免；
  - 全量绿 + 真实端到端冒烟复跑通过。
- **项目上下文文件边界修正（恢复原设计 + 补 trust 门控）**：`context_files.py` 曾在一个未提交的工作区改动中被改成"上溯到文件系统根（对齐 TS）"——会把 `~/AGENTS.md` 这类项目外文件拼进系统提示；git 历史证实**原设计就是 git 仓库根封顶**（104058c）。本次恢复并强化：①**git root 封顶**（无 git root 只读 cwd，不上溯）；②**trust 门控**（新增）——项目不被信任时项目链上下文文件不读（上下文进系统提示=注入面），全局 `agent_dir` 的用户级文件不受门控；loader 调用点经 `settings_manager.is_project_trusted()` 传真实决议。测试按新语义重写（封顶/顺序/门控/无 git root/去重 6 用例 + loader 门控用例反转）。harness 1227 全绿。

### Added
- **工具能力与渲染全量对齐 pi（批 2+3）**：
  - **渲染器四件套补齐**（write/find/grep/ls——此前通用回退）：折叠预览行数对齐 pi（write 前 10 行、find/ls 前 20、grep 前 15）+ 截断警告；`RendererEnv` 新增 `expanded`（ctrl+o 全局展开态透出——折叠语义渲染器的数据源）；
  - **bash/read 渲染语义对齐**：bash——折叠态末 5 个**视觉行**（wrapTextWithAnsi 宽度折行）+ 隐藏行数提示 + `Took X.Xs` 计时行 + 剥 full-output footer + 截断带完整路径 + 非零退出码行；read——**未展开时内容区为空**（pi 语义）+ 展开全量 Markdown 高亮 + 截断提示带 `(50KB limit)`；
  - **工具功能逐项 audit 对齐**（pi 源码逐行对照）：edit——`access(R_OK|W_OK)` fail-fast（ENOENT/EACCES 带 errno）；read——魔数嗅探 MIME 优先（修真 bug：wav/avi 曾被 RIFF 前缀误判为图片、JPEG 字节配 .png 扩展名张冠李戴）+ 首行超预算提示带实际大小；write——文件锁范围对齐 pi（mutation 全包在锁内 + 三点 abort 检查）；find/grep/ls——截断只按 50KB 字节（修真 bug：2000 行上限先触发且误报字节限）、fd 部分产出容忍 + `\r` 清理、ls 跳过不可 stat 条目（悬空软链）+ 达 limit 即停；五工具补 `prompt_snippet`/`prompt_guidelines`（pi 原文）；
  - 测试：新增 35+ 用例（含假 fd 脚本的行为级用例）；全量绿：coding 270、nova-client 300、冒烟复跑通过。
- **agent 组合层重构（三层模型定稿：素材/组合/运行）+ 命令级选配**：
  - **agent = 单 yaml 组合声明**（`agents/<name>.yaml`——纯选配零内容附着）：`persona`（人格素材路径列表，文件逐列/目录递归字典序展开，顺序即组装顺序）+ `tools/extensions/user_tools/commands/skills/subagents` 名单 + 元数据（name 缺省=文件名）；**退役**：description.md 及 frontmatter 合并、sections/ 硬约定、setup.md；
  - **人格文本素材独立成类**：`backend/personas/`（与 prompts/ 用户模板分源——身份文本 vs 命令宏不同概念）；官方 bundle 落地 `agents/coding_agent.yaml` + `backend/personas/coding/{role,guide}.md`；
  - **发现链**：`is_agent_file` 取代 `is_agent_dir`；manifest `[tool.nova] agents` 支持 yaml 文件或装 yamls 的目录（`agents = ["./agents/"]`）；自动发现扫 `<scope>/agents/*.yaml`；项目级 `.nova/agents/<name>.yaml` 同构；SDK `list_installed_agents` 按文件名 stem；
  - **命令级选配**：`agent.yaml` 新增 `commands` 允许集（空=全放）+ settings 新增 `disabled_commands` 排除集——求交生效（允许集 ∩ 非排除集）；执行点三处：后端 `get_commands` 过滤 + 调用守卫（被滤命令消费不执行 + UI 通知）+ 快照透出（`allowedCommands`/`disabledCommands`，契约重导）→ 前端 Node 命令分发与补全过滤；skill/prompt 模板不进（文本类纪律）；
  - 测试翻新：agent_config 全套重写（persona 装配/目录展开/顺序/诊断）+ 发现/装载/会话 fixtures 迁新模型；全量绿。
- **包结构两段式终局落地（backend/ + frontend/ 自含半区）+ 渲染器契约双形态**：
  - **目录模型定稿**：官方 bundle 重构为 `backend/`（Python 半区：可导入包 + tools/user_tools/extensions/agents/prompts + pytest）+ `frontend/`（TS 半区：package.json/tsconfig + tui/ 宿主段 + tests/）；B 型纯前端包**根即前端半区**（`tui/` 直接在根下）；poetry `from="backend"`、pixi editable 重链实测 import 正常；
  - **镜像发现约定**：前端段镜像后端资源类型目录（`tui/tools/<name>.ts`、`tui/user_tools/<name>.ts`、`tui/extensions/<name>/...`——位置即语义，后端 taxonomy 即前端 taxonomy）；`themes/` 宿主无关；discovery 按"半区根 + 宿主段"统一 A/B 型（A 型 frontend/，B 型包根）；
  - **渲染器契约双形态**：`NovaRenderer` 返回 `NovaBlock[] | Component`（判别在消费点——数组走块适配层可过网，带 render 方法的对象直挂为活组件）；`RendererInput` 新增 `env`（cwd/colors/markdownTheme——组件形态的主题通道，第三方免 deep-import）；`isComponentOutput` 判别导出；
  - **官方渲染器组件化**：bash/edit/read 三个渲染器改写为返回活 pi-tui 组件（复用宿主 renderDiffLines/renderTableLines 纯函数保视觉一致）；tree 选择器归位 `tui/extensions/session_commands/slash/tree/`（镜像约定首个实例）；edit-preview 引擎归 `tui/lib/`；
  - **安装器同步**：npm 阶段探测点改 A 型 `frontend/package.json`（包根遗留不触发，回归测试钉死）/ B 型包根；B 型合法判定 `ui/` → `tui/`；
  - **jiti 别名锚定宿主 pi-tui**（前条已记——本批依托它实现包组件与宿主同实例）；
  - 测试：discovery 8 用例（A/B 型、无双轨回归）+ loader 14 + bundle frontend 30 + B 型 12；全量绿：harness 1304、nova-client 300、coding 236+30；冒烟复跑通过；dogfood 复验：新结构发现→加载→command:tree 注册 + bash 渲染器返回活组件（6 行渲染输出）。
- **Node 扩展运行时补全（pi ExtensionUIContext 对位——UI 向，dogfood 终局落地）**：
  - **`ExtensionCommandContext` 更名 `ExtensionUIContext`**（名副其实：内容是扩展的 UI 运行时动作面，命令/快捷键只是消费点）；装载期 `ExtensionUIAPI` 与运行期 `ExtensionUIContext` 的两段式命名对齐 pi；
  - **模态对话框宿主原语 `ctx.custom`**（pi ctx.ui.custom 对位——逃生舱核心件）：工厂产出 pi-tui 组件挂载进编辑器槽位（或 overlay 浮层），`done(result)` 交还结果关框（幂等），工厂抛错/非组件产物按取消解决不挂起扩展；宿主侧 `DialogController.customLocal` + 本地四件套 `selectLocal/confirmLocal/inputLocal/editorLocal`（多行编辑器框 EditorDialog：enter 提交/esc 取消）；
  - **编辑器与终端通道**：`getEditorText/setEditorText/pasteToEditor`（fork 回填类场景）、`writeClipboard`（OSC52/平台）、`setStatus`（footer 扩展状态行——key 幂等覆盖、按 key 排序、单行截断）、`onTerminalInput`（原始终端输入拦截，keymap 之前优先消费、异常静默、返回反注册）、`getToolsExpanded/setToolsExpanded`（ctrl+o 展开态）、主题访问器三件（getTheme/getAllThemes/setTheme）、`invokeCancellable`（长 RPC 取消）、`registerForegroundTask`（Esc 域级路由登记）；
  - **自动补全 provider 钩子**：`registerAutocompleteProvider`（`autocomplete:*` slot 族）+ `CompositeAutocompleteProvider`（扩展源在前、按 label 去重、applyCompletion 按来源路由回原 provider）；
  - **jiti 别名锚定宿主 pi-tui**（`'@earendil-works/pi-tui'` → 宿主副本——原生 ESM 缓存共享，getKeybindings 等模块级单例与宿主同实例，实测验证；双包分裂会破坏键位匹配）；nova-client package.json 开放 `./modes/tui/*` 子路径导出（宿主 TUI 部件成为 UI 包的正式消费面）；
  - **dogfood 验收：/tree 迁入官方 bundle**（`nova_coding_agent/ui/`——tree/selector.ts 组件 + tree/controller.ts 编排 + ui/index.ts 扩展入口 registerCommand）：命令 UI 与后端命令同包同生命周期；宿主编辑器的 /tree 拦截删除，改走"扩展命令 > 前端导航 > 后端 slash 回退"分发链（双 Esc 同路）；bundle 缺席时后端 /tree（反向原语版）自动接管；**真实管线验证**：discoverUIAssets → loadUIAssets（jiti + 别名）→ 工厂执行 → `command:tree` 注册成功、零诊断；
  - 测试：dialogs 宿主语义 5 用例 + bundle 侧 tree 纯函数 18 用例随迁；全量绿：nova-client 296、coding 30+236、冒烟复跑通过。
- **pi 深度对账清零批（TUI 能力面全量核对后的弱点更进）**：
  - **启动 CLI flags 与恢复工作流**（pi cli/args 对位）：`--resume`（启动打开会话选择器）、`--session <file|id>`（恢复指定会话——后端 createSession 新增 `sessionFile` 参数：绝对路径直用/裸 id 在 cwd 会话目录解析/与 continueLast、sessionFlag 互斥校验先于 runtime 创建）、`-n/--name`、`--thinking`（契约七级校验，非法警告忽略）、`--no-session`（契约无开关，警告挂账）、`@file` 初始消息文本展开（`<file name>` 块拼接，空文件跳过、缺失/目录清晰报错）；**顺带修复存量真 bug**：main.ts 的 commander action 参数顺序写反（`cwd/--continue` 等 flags 从未真正到达 app，恒 undefined）；启动编排 StartupController（未信任项目横幅 → "Session compacted N times" 提示 → --name 命名 → resume 选择器）；
  - **进程健壮性三件套**：`utils/signals.ts` installSignalHandlers——SIGINT/SIGTERM/SIGHUP 优雅退出（runtime.stop 容错 → tui.stop → exit）、死终端 EIO/EPIPE/ENOTCONN 应急退出 129、uncaughtException/unhandledRejection 恢复 cooked 模式后打印堆栈退出；重复信号幂等 + 监听器可卸载；
  - **设置面板与终端集成（E 流）**：
    - 设置面板 5 → 16 项：steering_mode/followup_mode（setSteeringMode/setFollowUpMode）、auto_compaction（setAutoCompactionEnabled）、thinking_level（快照 availableThinkingLevels 驱动子菜单，不支持禁用文案）、default_project_trust（updateSettings）、tree_filter_mode、branch_summary_skip_prompt、editor_padding/autocomplete_max_items/clear_on_shrink（pi-tui setter 即时生效）、terminal_progress；持久化三通道分流（后端 updateSettings / 会话态 RPC / 前端 ui-settings.json 新存储 utils/tui-settings.ts）；
    - OSC 0 终端标题（updateTitle——basename + 控制字符净化，快照联动）、OSC 9;4 任务栏进度（working/compacting 置位 + 1s keepalive，设置门控）、OSC 133 prompt 分区（user/assistant 首尾行注入，终端跳转）；
    - automatic 双主题跟随终端（COLORFGBG 取值 + pi-tui color scheme 通知同步；/theme 与 /settings 首项）；主题文件 watcher 热更新（用户目录 + 包主题登记，100ms 去抖，命中当前主题全量重渲，坏文件回退 dark）；tmux < 3.1 extended-keys 版本层告警（选项层盲区补齐）；
    - 工具结果图片内联渲染（result.content 的 image 块经 pi-tui Image 组件；终端不支持协议回退 `[图片: <mimeType>]` 文本行；两渲染路径共用）；
    - /changelog（copy-assets 拷仓库根 → dist 资产，renderChangelogEntry 渲染 Unreleased/最新段——transcript 新增 addMarkdown 出口）+ What's New（ui-state lastSeenVersion，首装不弹/前进弹一次，替换旧 settings 通道实现）；
  - **/model 与 /scoped-models 前端自持**（pi model-selector/scoped-models-selector 对位）：/model 当前模型置顶带 ✓ + Tab 切 all/scoped 作用域 + provider 分组 + fuzzy + 元信息列（name/reasoning/未配置），选中即 setModel（ctrl+l 同路）；/scoped-models 面板（space 切换启用、alt+↑↓ 循环排序、ctrl+a 全启用（有搜索词仅过滤结果）、ctrl+x 全清、ctrl+s 才写 setScopedModels——"session-only until saved"、esc 放弃、(unsaved) 脏标记）；SearchableSelector 通用增强 `onTab`/`setItems` 两扩展点；后端 /model 保留 headless 回退；
  - **后端 RPC**：`setAutoCompactionEnabled`（对齐 setAutoRetry 形状，方法表 68）；/login 与 /logout 的 provider 选择器状态标签（`✓ configured` 凭据已配 / `env: <VAR名>` 环境变量可得 / 未配置省略——数据源 ModelRuntime.get_provider_auth_status 同步快照）；
  - **tree 收尾**：ctrl+x 复制选中条目全文（entryCopyText——消息全文块拼接/摘要类取 summary，OSC52/平台剪贴板）；shift+t 标签时间戳显隐（formatLabelTimestamp：当天 HH:MM、今年 M/D、跨年 YY/M/D）；commitLabel 同步时间戳；
  - **/quit**（ctrl+c 双击/ctrl+d 空退的命令形态）；
  - 测试：startup 18 + signals 14 + 设置/终端/主题/图片/changelog 68 + scoped-models/tree/model 新增用例 + 后端 8 用例；全量绿：harness 1303、coding 236、nova-client 309；真实端到端冒烟复跑通过。
- **TUI 长尾对齐 pi 收尾批（八项挂账清零）**：
  - **前端自持导航三件套**：`/tree`（TreeSelector——pi TreeList 语义复刻：单子链平级、分支点缩进、隐藏条目后代重挂最近可见祖先、活跃分支优先；←→ 折叠/展开、shift+l 标签编辑（setLabel RPC）、enter 跳转带摘要问询流（不摘要/摘要/自定义指令，Esc 重开树），editorText 回填编辑器）；`/resume`（SessionSelector——ctrl+d 删除确认态（吞键状态机）、ctrl+r 重命名、tab 作用域 current⇄all）；`/fork`（user 消息选择器——fork RPC 的 selectedText 回填编辑器"编辑后重发"）。判决：per-item 动作键反向原语表达不了 → 前端直调 RPC 自渲染，后端同名命令保留 headless 回退；双 Esc 导航同走前端路径；
  - **会话 RPC 三件套（后端）**：`deleteSession`（trash CLI 优先、os.remove 兜底、幂等、当前活跃会话 SESSION_IN_USE 新错误码 -32003 拒绝）、`renameSession`（任意会话文件追加 session_info 条目——当前会话走 live 通道；空名=清除）、`listSessions` 富字段化（messageCount/firstMessage/cwd/parentSessionPath + scope:current|all，listing 层并发限量复用）；`fork` 返回值补 selectedText/editorText（navigateTree 命名对齐）；契约 MINOR 1→2（纯加法）；新错误码与 14 用例测试在案；
  - **form 原语（标准词汇第五件）**：多字段表单——Python 糖 `form()`（ui_primitives.py，空能力优雅降级）+ TUI 原生 FormDialog（tab/↑↓ 切换、enter 下一项末项提交、ctrl+enter 任意提交、esc 取消；预填值首激活光标归末位）；落地决策在案（dialog:* slot 未落地，form 足够通用故原生，不迁回）；ui-primitives.md 同步；
  - **overlay 浮层（registerOverlay）**：扩展 API 新方法——NovaOverlayOptions 纯数据子集（锚点/宽高/边距/nonCapturing，可过网）；TUI 侧 OverlayHost（零高度组件借渲染周期做 slot 对账）经 pi-tui showOverlay 呈现，替换/消失自动 hide；region:overlay 单例键；
  - **/share**：gh CLI 前置检查（未安装/未登录两条明确错误）→ 导出会话 HTML 到临时文件 → gh gist create --public=false → URL 进 transcript（与 pi 偏差在案：无托管 viewer——导出物自包含；无 Esc 取消加载框，挂账）；
  - **npm range 版本**：`^`/`~`/裸部分版本 + 复合语法全集（`*`/通配段 `1.2.x`/`v` 前缀/比较器集 AND/`||` 并集/hyphen range）——自研零依赖 `_semver.py`（Version/NpmRange/NpmRangeUnion/max_satisfying：caret 0.x 锁定、x-range 展开、比较器交集折叠、prerelease 排除规则）；解析期不触网，resolver 选 max satisfying，无匹配报清晰错误；已知边界在案（dist-tag 引用仍报错、hyphen 不混排）；
  - **导出高亮验证销账**：vendored hljs + renderer.code + .hljs-* 主题变量全链路本就已通（挂账为陈旧），装配级验证脚本确认零占位符残留；
  - **残余清零第二波（同一批扫尾）**：
    - **tree filter 五模式 + 搜索 + 水平视口**：ctrl+d/t/u/l/a 直切（default/no-tools/user-only/labeled-only/all，重按回 default）+ ctrl+o/ctrl+shift+o 循环；可打印字符即搜索（token AND，Esc 有查询先清查询并清折叠——pi 语义）；水平视口（TreeRowsView 渲染期锚点左移——sliceByColumn 列裁剪，光标 gutter 固定）；
    - **session 排序与搜索语法**：ctrl+s 循环 threaded/recent/relevance（threaded 按 parentSessionPath 组树缩进，有查询自动回退——pi 同款）；ctrl+n 只看命名、ctrl+p 路径显示；`re:` 正则 / `"phrase"` 精确 / fuzzy 三态搜索；
    - **分支摘要 Esc abort 接线**：`abortBranchSummary` RPC 新增（域级 abort，契约 MINOR 2→3，方法表 67）；`runtime.invokeCancellable`（client.callCancellable 泛型透出）+ **ForegroundTasks 前台任务登记处**（新控制器——Esc 域级路由第四环：对话框 > 前台任务 > run/retry/compacting > 双 Esc）；摘要中 Esc = 本地 AbortError + cancelRequest 上行 + 域级 abort 停 LLM，中止后重开树（pi aborted 语义）；
    - **/share Esc 取消**：同一 ForegroundTasks 通道（kill gist 子进程 + 临时文件清理 + 静默收尾）；顺带修复：全局 ctrl+o 在对话框开着时让路（tree filter 循环不再被全局展开切换截胡）；
    - **npm 复合 range 语法**（子代理流）：`*`/通配段（`1.2.x`）/`v` 前缀/比较器集（`>=1.2 <2.0`）/`||` 并集/hyphen range——`_semver.py` 扩为全集实用子集；
  - 测试：tree 组装/过滤/搜索 15 用例 + SessionSelector 模态与语法 12 用例 + FormDialog 5 用例 + keymap 前台任务优先级等 14 用例 + ui_primitives 7 用例 + 会话 RPC 14 用例 + npm source 53 用例；全量绿：harness 1295、coding 235+12、nova-client 225；真实端到端冒烟复跑通过。
- **export-html（会话导出 HTML——Node 层，pi export-html 复刻）**：
  - **位置决策**：放 nova-client（呈现归 Node 层，零后端改动）——`src/export/` 子系统（宿主无关，主题数据调用方注入；M3 Web 同可用）；
  - **pi 三件套直搬（MIT）**：template.html/css/js + vendored marked/highlight.js（客户端渲染的完整导出物：会话树侧栏/搜索/过滤/工具卡片内建渲染）+ ansi-to-html.ts；`scripts/copy-assets.mjs` 补 tsc 不复制非 ts 资产的构建链；
  - **数据零映射**（camelCase 契约化后的红利）：线上条目与 template.js 消费形状天然一致；SessionStateResult 契约加 `leafId`（minor 加法——template.js 定位当前分支的锚点，契约重导）；
  - **主题 export 段**：ThemeJson 加可选 `export`（pageBg/cardBg/infoBg）+ 内建 dark/light 补 pi 原值 + `getExportThemeData()`（256 索引转 hex、export 段 vars 解析、缺省从 userMessageBg 派生——pi deriveExportColors 对位）；
  - **/export 分叉**：无参数或 `.html` 后缀 → 前端 HTML 导出（默认 `nova-session-<id8>.html`）；`.jsonl` → 后端 JSONL（后端命令描述同步）；**踩坑记录**：`String.replace` 对 minified JS 的 `$` 序列会按特殊模式展开——全部改函数形态替换（真实验证逮出）；
  - 测试：映射器三用例（递归/自由负载保留/数组标量）+ 装配完整性（九占位符零残留/数据 base64 解回/主题注入）+ 真实 dist 产物端到端（267KB 自包含 HTML，含树数据）。nova-client 195、harness 1236、coding 228 全绿。
- **npm registry 包源（`nova-pkg install npm:<name>[@<version>]`——第三种包源落地）**：
  - **spec 解析**：`npm:@scope/pkg@1.2.0` / `npm:pkg`（latest）——版本段取最后一个 `@` 分隔（兼容 scope 前缀）；`^`/`~` range 报明确错误挂账（不静默装错版本）；
  - **resolver `_resolve_npm`**：registry metadata 查询（`NPM_CONFIG_REGISTRY` 环境变量优先，镜像生态惯例）→ dist-tags.latest/精确版本解析 → tarball 下载 → **sha512 integrity 校验** → 解压（剥 `package/` 前缀 + 路径逃逸检查 + `filter="data"` 双保险）→ 缓存即安装态（`packages/npm/<name>/`，同名只留一份、更新即替换；临时目录 + move 防残骸）；只读解析（update=False）不触网，缓存缺失即"未安装"；NOVA_OFFLINE 拒绝下载；
  - **install/updates 链路**：`install_path_for_source` 加 npm 分支（缓存路径即安装路径，物化阶段自动跳过自复制——与 git 同形态）；`PackageUpdate.type` 加 "npm"，更新检查 = registry latest vs 已装版本（精确 pin 不查，pi 语义）；`ProgressEvent.action` 加 "npm"（真实验证逮到的校验缺口）；
  - **真实端到端验证**：真 registry（npmjs.org）装 `pretty-ms@9.2.0`——解析/下载/校验/解压全链通过；非 nova 包被既有资源校验正确拒绝（B 型包放行分支不受影响）；
  - 测试 9 用例（spec 四形态/resolver 五场景——mock 网络层）。harness 1236 全绿。
- **P3-10 终端健壮性（pi 健壮性层对位）**：`utils/terminal-guard.ts`——**死终端应急退出**（stdout/stderr 的 EIO/EPIPE/ENOTCONN → exit(129)，不做恢复写——终端已死写序列会再触发 EIO）；**uncaughtException 崩溃恢复**（先 tui.stop 恢复 cooked 模式/光标再打印退出——防 raw 模式残留逼用户 stty sane）；**SIGINT/SIGTERM/SIGHUP 优雅退出**；**tmux 键位检测**（extended-keys 未开/format=xterm 时给 ~/.tmux.conf 配置指引，查询失败不警告）。新版本通知挂账（包未发布 npm registry——包更新通知 checkPackageUpdates 已有）。测试 2 用例。nova-client 191 全绿。
- **P3-9e 包面板内建扩展（R17 dogfood 验收）+ 扩展 UI 通道补全**：
  - **slotsBootstrap 升格为 ExtensionUIAPI 形态**：内建贡献（官方块适配器 + 包面板）与第三方包扩展拿**同一个 api 对象**（dogfood 终极形态——runtime.createSlots 建 api 注入完整 deps：commandContext/uiSettings/uiState）；registerBuiltinBlocks 改走 `api.registerBlock`（注册表直操作清零）；
  - **ExtensionCommandContext 补两通道**：`select`（选择器原语——Node 扩展的第一个 UI 能力；宿主注入，TUI 经本地框 Promise 化）+ `refreshPackages`（包变更后 slots 重建）；
  - **/packages 内建包面板**（`builtin/package-panel.ts`）：包列表（名称/scope/版本/资源计数 + 更新角标 pkgCheckUpdates）→ 详情/更新/卸载动作（pkgUpdate/pkgUninstall + refresh 联动）+ 用户级/项目级列表切换；无 select 通道优雅降级；
  - 测试 5 用例：注册来源/空列表与无通道降级/更新流程/卸载流程/作用域翻转。nova-client 189 全绿。
- **P3-9d custom-entry 渲染器 + 包内主题资产**：`entry:<customType>` 渲染器（registerEntryRenderer + transcript 消费点——注册即块适配渲染，无注册走 CustomMessageView 兜底；设计 §17 挂账清账）；**包内主题资产**（`ui/themes/*.json`）：discovery 发现 + loader 校验收集（parseThemeJson/vars 解析 + 同名碰撞与坏文件诊断）——**主题契约 theme-json 上移至 presentation 层**（纯数据契约归呈现层，loader 宿主无关可引用；modes/tui/themes/theme-json.ts 改 re-export 兼容出口）；runtime `onPackageThemes` 回调透出宿主，TUI 三源注册（builtin > 用户目录 > 包，全量替换语义与 slots 整体替换配套）。测试：loader 包主题全链用例 + themes 包注册用例 + transcript custom-entry 两用例。nova-client 184 全绿。
- **P3-9c settings/state 子系统（design §7 落地——扩展配置与数据一等公民）**：
  - **`settings/store.ts`**：`UISettings`（设置键注册表——define 声明（schema 走代码）/get 并入默认值/set 类型校验 + 未声明拒绝/异 owner 冲突拒绝/原子写持久化/onChange 发布）+ `UIStateStore`（扩展内部 KV——`ui-state/<包名>.json` 每扩展一个文件，命名空间路径净化防逃逸）；存储归 Node 层（`~/.nova/agent/ui-settings.json` + `ui-state/`）——后端 settings 不背（未知键拒绝 + 前端域，两个文件主权分明）；
  - **API 接线**：`api.settings.define/get/set` + `api.state.get/set/all`（命名空间 = 包名）；设置键冲突复用碰撞诊断通道（`setting:<key>`）；runtime 持有子系统实例（路径可注入隔离测试）→ loader 透传；变更经 bus 派生事件 `settings:changed` 发布；
  - 测试 7 用例：define/get/set 全链与持久化回读/异 owner 冲突/onChange/坏文件降级 + KV 隔离与回读/路径净化 + API 通道（冲突诊断/设置全局共享/state 按包隔离）。nova-client 180 全绿。
- **P3-9b registerCommand/registerShortcut（统一命令表第三源 + restrictOverride 消费）**：
  - **slots 加两族键**：`command:<name>` / `shortcut:<key>`——行为注册复用注册表机制（覆盖/来源/碰撞诊断/包卸载即消失的生命周期）；
  - **`registerCommand(name, {description, handler})`**：Node 扩展命令——submitText slash 分发先查 Node 扩展命令（命中即本地执行，参数透传），未命中再走后端；补全三源合并（后端 getCommands + 前端本地 + Node 扩展）；执行上下文 `ExtensionCommandContext`（框架无关最小面：invoke 直达后端 RPC + notify 消息出口）经 loader ← runtime 注入链透传；
  - **`registerShortcut(key, handler)`**：keymap 最优先路由（pi onExtensionShortcut 对位）；**restrictOverride 落地**：`validateExtensionShortcuts` 对账（slots 替换后由装配根调）——撞保留键位当前绑定键的扩展快捷键剔除 + transcript 诊断；生效表缓存（按键零遍历全表）；
  - 测试 4 用例：扩展命令优先分发（参数透传/不进后端）+ 快捷键命中执行 + 撞保留键位剔除诊断。nova-client 173 全绿。
- **P3-9a 逃生舱泛化（RegionHost 两态宿主 + widget/header/status 消费点）**：
  - **RegionHost 组件**（`components/layout/region-host.ts`）：`region:<name>` slot 两态生产者同组件分流——声明式 producer（每帧调用 + 指纹缓存复用组件，可过网）/ 组件工厂（首次解析建厂一次直挂 pi-tui Component——pi setWidget/setFooter 对位）；首次调用返回形态判别（数组 → 块，否则 → 组件）；slots 整体替换后渲染帧自动重判别（函数引用比对）；部件异常静默；
  - **API 加法**：`registerRegionComponent(region, factory)`——与 `registerRegion` 同键竞争（后注册覆盖、碰撞诊断在案）；RegionEnv = { cwd, tui, colors, markdownTheme }（声明式用 cwd，逃生舱用全套）；
  - **三消费点接线**：widget 区新建（编辑器上方容器——pi setWidget 对位）+ header（welcome 之下）+ status（状态容器）；footer 保持既有附加行形态不动；
  - 测试 5 用例：空态/声明式指纹缓存/工厂建厂一次/替换重判别（组件↔声明式形态切换）/异常静默。nova-client 170 全绿。
- **P3-8 长尾命令收尾**：盘点纠正——/clone /export /import 后端早已真实实现并注册（非缺口）；本批：**/export 无参数默认路径**（cwd 下 `nova-session-<id前8>.jsonl`，pi 默认文件名对位）+ **/debug 前端本地命令**（镜像状态 dump：快照 JSON + 条目 kind 计数 + slots 注册表，写 `~/.nova/agent/debug-<ts>.log`）；挂账在案：/share（gist 依赖 gh CLI）、session 选择器内删除/重命名（定制选择器动作键）、HTML 导出（主题 export 段未做）。测试：export 默认路径/显式路径 2 用例。coding 228、nova-client 165 全绿。
- **P3-7 loaded resources 区 + 启动信息族**：
  - **ResourcesView**（pi loadedResources 对位）：compact 单行计数（`2 skills · 3 prompts · 16 commands · 2 packages`）/ expanded 分组清单（ctrl+o 联动）；数据四 RPC 并发现拉（listSkills/listPromptTemplates/getCommands/pkgList，失败静默降级）；quiet_startup 随 header 区移除；
  - **终端标题**（OSC 0）：`nova - 会话名 - ~cwd`，启动即设 + session_info_changed 联动；
  - **/hotkeys**：键位表全量展示（解析后生效值——用户重绑定即时反映；未绑定标记）；
  - **changelog 启动提示**（v1）：包版本 vs `last_changelog_version` 比对——升级提示一行 + 回写记录（包内无可达 changelog 文件源，/changelog 命令不做——诚实取舍）；
  - **ctrl+o 联动收敛**：KeymapController deps 袋的 expansion/transcript 直操作改为 `toggleExpansion` 回调——app 统一翻转 + transcript/welcome/resources 三视图联动重建；
  - 测试：ResourcesView 四用例（compact 计数/expanded 清单/rebuild 不重拉/全失败降级）。nova-client 165 全绿。
- **P3-6 选择器定制视觉（depth/group 元信息）+ /tree 链路修复**：
  - **定制视觉数据驱动方案**（零协议变更）：`SearchableItem` 加可选 `depth`（树形缩进）/`group`（分段组头）字段；SearchableSelector 渲染 depth 缩进 + group 变化处插 dim 组头行（渲染层插入不占数据索引）；`itemsFromParams` 透传——后端 `select_items` 糖库带上即生效，无 variant 路由（数据驱动更彻底）；
  - **/model provider 分组**：items 加 `group: provider`（选择器按 provider 分段，pi scoped-models 的分组视觉对位）；
  - **/tree 无参数链路修复（原来是断的）**：`navigate_tree("")` 此前直达 `get_entry("")` 抛 Entry not found——补上树选择器：DFS 扁平化（多根/悬空 parent 防御）+ depth 元信息 + current 路径标记（叶→根回溯）+ label 摘要（标签前缀 + role:预览）；选中 `wait_for_idle` 后 `navigate_tree`（当前叶/取消不动作）；折叠/标签编辑等定制交互归后续；
  - 测试：前端选择器三用例（depth 缩进/组头插行去重/普通回归）+ 后端 tree 三用例（扁平化顺序与 depth/current 标记/取消/headless）+ model group 断言。coding 226、nova-client 161 全绿。
- **P3-5 footer 数据补全（pi 行 2 全面对齐）**：`refreshStats` 并发拉 `getSessionStats` + `getContextUsage`（RPC 既有通道）；行 2 左侧补：缓存命中率（cache_read/input 百分比）、`$成本`、**上下文用量 `percent/窗口`**——>70% 黄 >90% 红（pi 阈值同款）+ auto-compact 开时带 `(auto)` 标记（读 currentSettings.compaction.enabled）；窗口 128k 缩写格式化；FooterView 构造加 settings getter（app 传 currentSettings 引用）。测试 2 用例（全量渲染/无 auto 标记）。
- **P3-4 initialMessage 首条消息**（pi 对位）：CLI positional 参数 `nova "问题"` → 启动后立即提交（first-time 引导开着则不抢焦点跳过）；commander argument 接入 + app.run 尾段 `submitText`（全分发管线：bash/slash/普通皆可）。`nova --help` 真实验证。
- **P3-3 小键位族（copy / suspend / 外部编辑器 / resume 提示）**：
  - **ctrl+x + /copy**：复制最后一条 assistant 消息到剪贴板（`writeClipboardText`——clipboard 工具补齐写入方向：macOS pbcopy / Linux wl-copy/xclip；TranscriptController 域方法，键位与本地命令同路径；无消息/失败有反馈）；
  - **ctrl+z 挂起**（pi handleCtrlZ 直搬）：keepAlive interval + SIGINT 忽略 + SIGCONT 恢复（tui.start + 全量重绘）；Windows 提示不支持；
  - **ctrl+g 外部编辑器**（pi openExternalEditor 直搬）：临时文件（`nova-editor-*.md`）→ tui.stop 让位 → spawn stdio:inherit 等退出 → 退出码 0 回写草稿；编辑器命令链：settings `external_editor`（新字段，纯前端 round-trip）→ $VISUAL → $EDITOR → vi；
  - **退出打印恢复命令**：`nova --continue`（pi resume 提示对位）；
  - **KeymapController 构造收敛为 deps 袋**（11 依赖的可读性救星——非行为变更）；
  - 测试：ctrl+x/ctrl+z/ctrl+g 路由用例。nova-client 156 全绿。
- **P3-2 thinking 三件套 + 模型键位**：
  - **shift+tab 循环 thinking 级别**（`cycleThinkingLevel` 后端既有通道——按模型支持面循环）+ **ctrl+p / shift+ctrl+p scoped 池模型循环**（`cycleModel` 后端既有）+ **ctrl+l 模型选择器**（/model 命令路径）——五个键位全注册进键位表（可配置）；
  - **编辑器边框状态色**（pi 同款）：bash 模式（! 开头）→ bashMode 绿；否则 thinking 级别色——主题系统补 thinking 级别色板（dark/light 内建七级，自定义主题的可选 token，缺失回退 borderMuted）；`setEditorBorderColorHook` 渲染帧现取（状态即变，零事件接线）；
  - **ctrl+t thinking 显隐**：`hide_thinking_block` 消费落地——AssistantView 折叠为静态 "Thinking..." 标签（pi 语义），切换即持久化 + transcript 全量重建；/settings 同步收编（第五项）；
  - 测试 8 用例：边框色三级（token 取值/max 回退 xhigh/缺 token 回退）+ thinking 显隐三渲染态 + shift+tab/ctrl+p/ctrl+l 路由。nova-client 155 全绿。
- **P3-1 提交管线状态路由（修 bug 级缺口 + pi 队列交互链对齐）**：
  - **流式中提交路由**（修 bug：后端 `prompt()` 流式中要求显式 `streamingBehavior` 否则抛错，前端此前无脑 prompt——working 时提交必报错）：`runtime.prompt` 改 options 签名（images/streamingBehavior）；`submitText` 统一提交分发——working 时 Enter=**steer**（插入当前 turn）、alt+enter=**followUp**（排队等 run 结束，idle 时等同普通提交）；
  - **队列还原**（pi dequeue/restoreQueuedMessagesToEditor 对位）：`runtime.clearQueue` 通道 + `EditorController.dequeueToEditor`——alt+↑ 全量还原进编辑器（steering+follow_up 时间序，现有草稿附后）；**Esc 中断 run 前先还原队列**（排队内容不丢，pi 语义）；
  - **`!!` 语义**：bash 不进 LLM 上下文（`exclude_from_context`——后端 bash 用户工具参数既有，零后端改动）；`!` 保持进上下文；
  - 键位表注册 `app.message.followUp`(alt+enter)/`app.message.dequeue`(alt+up)（可配置）；
  - 测试 9 用例：submitText 五路由（!!/!/idle/working steer/followUp/slash 分流）+ dequeue 两场景（时间序拼接/空队列不动）+ Esc working 先还原再 abort。nova-client 146 全绿。
- **P2-4 settings 可视化编辑 + skill 条目呈现（设计批次收官）**：
  - **skill 条目**（pi skill-invocation-message 复刻）：`/skill:name` 展开的 XML skill block 从 user 消息拆出——`parseSkillBlock`（pi 正则直搬，后端产物格式同构验证）；`SkillInvocationView` 折叠单行 `[skill] name`（ctrl+o 跟随全局展开态看 markdown 全文，customMessageBg 视觉一致性）；UserMessageView 改造（skill 折叠 + 尾部用户文本正常呈现，OSC133 标记包整体）——XML 全文不再刷屏；
  - **/settings 可视化编辑**（pi settings-selector 对位，前端本地命令）：两级选择器——第一级配置项（label + 当前值 + 描述）、第二级候选值（当前值排第一，Esc 返回上级）；选中即 `currentSettings` 原地更新（getter 消费方即时生效）+ 项级即时动作 + `updateSettings` 持久化；v1 四项（有消费者才收）：theme（复用 ThemeController.applyTheme）、double_escape_action（即时生效——app 的双 Esc 设置随之重构为 currentSettings 派生 getter）、quiet_startup（下次启动生效）、show_cache_miss_notices；DialogController 补 `hasLocalDialog`（本地框重入判定——第二级 Esc 返回第一级）；
  - 测试 12 用例：parseSkillBlock 三形态/UserMessageView 拆分四场景 + settings 五场景（配置项表派生/布尔解析与持久化参数/字符串即时生效/theme 分发与 skipPersist/真实主题单例读取）。nova-client 139 全绿。
- **P2-3 自定义块 + 扩展编辑器（ExtensionUIAPI 全量四方法齐备）**：
  - **`registerBlock(kind, {adapter, validate?})`**：自定义块类型注册（`block:<kind>` slot——与官方块同通道）；`NovaBlock` 词汇开放（union 加 `{kind: string} & Record<string, unknown>` 兜底成员——内建五种窄化/Extract 不受影响，字段错误由运行时 validateBlock 守护的既有定位闭环）；可选 `validate` schema 钩子（slots 旁路存储，消费点适配前调用——issues 非空渲染错误块，适配器不被调用）；
  - **`registerEditor(factory)` + 热替换**：`editor:main` slot 单例键；runtime 新增 `onSlotsReplaced` 通知（refreshPackages 成功路径）→ `EditorController.maybeSwapEditor`——建厂（TUI 环境注入 tui/theme/keybindings）→ EditorComponent 契约防御（缺 getText/setText/handleInput 拒绝 + 错误提示）→ **文本迁移** → 重接线（wire 重挂 onSubmit）→ 槽位换人（对话框开着只换引用，关框恢复即新编辑器）；同工厂幂等不换；
  - **EditorRef 引用盒**：controllers（editor/keymap/dialogs）共享当前编辑器的通道从构造注入实例改为盒子现取——热替换对全体消费方自动生效；EditorComponent 可选方法判空调用（addToHistory/insertTextAtCursor/setAutocompleteProvider——扩展编辑器可实现子集）；
  - 测试 7 用例（归位 blocks/editor 两语义文件）：自定义块注册渲染/validate 拦截（适配器零调用）/无 validate 直通与未注册降级 + 编辑器热替换全链/幂等/契约防御/无注册不动作。nova-client 127 全绿。
- **P2-2 `ui/index.ts` 扩展宿主 + region 区域部件**：
  - **ExtensionUIAPI**（`presentation/extension-api.ts`，框架无关契约）：`ui/index.ts` 默认导出工厂 `(api) => void`——`registerRenderer`（等价 renderers/ 文件约定的编程式形态，可条件注册/共享 helper）+ `registerRegion`（区域部件：纯函数产声明式块，`RegionContext` v1 给 cwd）；边界守住：行为注册（命令/工具/事件）不经此 API，归后端 Python 扩展；
  - **loader 扩展（`loadRenderers` → `loadUIAssets`）**：extension entry 加载（jiti + 工厂函数校验）；**注册统一走 ExtensionUIAPI 通道**（文件约定的注册也改走 api.registerRenderer——同源同路真 dogfood，loader 内重复碰撞逻辑删除）；执行顺序 renderers 文件先、index 工厂后（同包编程式可覆盖文件约定，同源不算碰撞）；缓存层合并（rendererCache→moduleCache：index 缓存工厂函数、每次刷新重新执行注册——slots 整体替换语义配套）；碰撞诊断扩到 region 键；
  - **region:footer 消费点**（FooterView 第三行）：producer 每帧调用（纯函数）+ 输出指纹缓存（不变复用组件——Markdown 构造贵）；部件异常静默不炸主行；discovery 的 `hasExtensionEntry` 布尔升级为 `extensionEntry` 路径字段；
  - 测试 7 用例：工厂注册双贡献/非函数诊断/同包覆盖无碰撞·跨包碰撞诊断/entry 缓存（mtime 命中 + 工厂重执行）+ FooterView 三场景（基线/注册产行、指纹复用与重建、异常静默）。nova-client 120 全绿。
- **P2-1 块注册制（slots 第四族 `block:<kind>` + 官方块 builtin 迁移 + schema 校验）**：
  - **`block:<kind>` slot 族**（slots.ts 四族齐备）：官方五种块适配器（markdown/code/diff/table/json）经 `registerBuiltinBlocks` 走同一 `register` 调用（source=`builtin`，dogfood 无特权——包可注册自定义块类型或覆盖官方适配器，碰撞诊断在案）；
  - **schema 校验**（presentation/blocks.ts `validateBlock`，框架无关契约守护）：内建五 kind 查必需字段（diff 定位到 `hunks[i].lines[j]`），**未知 kind 放行**（开放集归自定义适配器）；非法块渲染为红字错误块（issues 列全——渲染器 bug 可见，不再炸适配器）；未注册 kind 降级 json + dim 提示行（可发现性）；
  - **runtime `slotsBootstrap` 宿主注入**：slots 实例每次整体替换（refreshPackages）后、包渲染器注册前调用——修掉"装配时注册一次、刷新后 builtin 丢失"的隐患；宿主无关层（runtime.ts）保持不认识 pi-tui 组件，TUI 经 `slotsBootstrap: registerBuiltinBlocks` 注入（将来 Web 宿主注 DOM 适配器）；
  - 消费点签名迁移：`blocksToComponents(blocks, slots)`（tool-execution.ts 一处）；包根出口补 `validateBlock`/`blockSlot`；
  - 测试 9 用例：validateBlock 四场景（五合法/缺字段 issues/定位路径/未知 kind 放行与非对象拒绝）+ 注册制五场景（builtin 注册来源/合法-非法-未注册三态/覆盖 dogfood/bootstrap 注入与空态）。nova-client 113 全绿。
- **P1-3 命令交互系统（welcome 启动区 + 双 Esc 导航 + first-time-setup）**：
  - **welcome 启动区**（`components/layout/welcome.ts`，pi builtInHeader 对位）：logo+版本（运行时读包根 package.json）/ 模型（`provider/id`，getter 现取——model_changed 经 store snapshot 订阅自动刷新）/ cwd / 键位提示（compact ↔ expanded 两态跟随 ctrl+o 全局展开态，键位表动态生成）——替换原静态 header 行；`quiet_startup` settings 门控（启动移除）；主题切换随 onThemeChange 重染；
  - **双 Esc 导航**（pi 同款 500ms 窗）：idle + 空编辑器双击 Esc → `/tree` 或 `/fork`——挂在 Esc 域级路由末位（working/retrying/compacting 的域级 abort 优先；对话框/授权框让路优先）；`double_escape_action` 三档设置（tree/fork/none，默认 tree 对齐 pi），settings 启动读取注入 getter；触发走 `runSlashCommand`（编辑器 slash 提交提取的公开路径——可取消 + 取消句柄注册，first-time 引导共用）；
  - **first-time-setup**（无模型引导，设计文档语义——非 pi 的主题/遥测向导）：无 `--model` 且 settings 无 `default_model` 时启动弹引导（SearchableSelector 本地框）——登录 provider（/login）/ 选择默认模型（/model）/ 跳过（Esc 即过；设过默认模型后永不弹）；
  - **settings 统一读取**：run() 启动一次 getSettings 分发四处（主题/quiet_startup/双 Esc/first-time 判定），ThemeController.init 参数化；DialogController 本地框通道复用；
  - 测试：双 Esc 7 用例（时间窗/三档/非空不导航/域级 abort 优先/窗口重置——轻 mock + 全局键位表路径注入隔离）+ welcome 3 用例（两态渲染/模型 refresh）；nova-client 104 全绿。
- **P1-2 主题系统（多主题 + /theme 预览选择器 + settings 持久化）**：
  - **themes/ 重构为运行时**：`theme-json.ts`（主题 JSON 契约——必需 token 46 个（核心 26 + markdown 10 + syntax 9），为 pi theme-schema 消费面子集，**pi 主题文件直接可用**（多余字段容忍）；ColorValue 三形态（hex / `""` 默认色 / 256 索引）+ vars 变量引用（环引用/未定义报错）；缺 token 报错列完整清单）；`builtin-dark/light.ts`（pi 调色板直搬，内联 ts 免构建期 json 复制）；`theme.ts`（`createThemeFace` 四件套生成——colors/markdownTheme/syntaxColors/editorTheme；全局单例 initTheme/setTheme/onThemeChange；失败回退 dark pi 同款语义；COLORFGBG 终端背景检测兜底，OSC 11 挂账）；**自定义主题目录** `~/.nova/agent/themes/*.json`（内建优先去重，坏文件诊断透出）；
  - **动态消费面（组件零改动）**：`colors`/`markdownTheme`/`syntaxColors`/`editorTheme` 出口改 Proxy/活引用转发——import 一次，`setTheme` 后渲染自动取新色；app.ts 内联 editorTheme 删除改从 themes 取；
  - **`/theme` 前端本地命令**（pi 同款定位：主题是纯前端关心的事，不进后端命令表；补全清单前端追加）：SearchableSelector 新增 `onHighlight` 钩子（**移动即预览**，pi preview 语义对位），Enter 确认经 `updateSettings` 持久化，Esc 恢复打开前主题；DialogController 新增本地框通道（`showLocal`/`restoreLocal`，Esc 让路判断合并本地框）；ThemeController 装配（启动读 settings.theme 初始化 + 目录诊断提示 + 切换经 onThemeChange → transcript 全量重建重渲）；
  - **后端**：`Settings` 加 `theme` 字段（纯前端消费 round-trip 区；契约零变更——GetSettingsResult.settings 本是 `Dict[str, Any]`）；
  - 测试：themes 13 用例（parse 五场景/vars 三场景/ThemeFace ansi 序列与 fg-bg 分流（chalk.level=3 强制上色）/单例切换回调与 Proxy 活引用/失败回退/自定义目录发现与内建优先/COLORFGBG 检测）+ harness settings theme round-trip；nova-client 94、harness 1225 全绿。
- **P1-1 键位系统（`keymap/` 落地，pi keybindings.ts 对位 + 三级合并）**：
  - **keymap/ 三件套**：`tables.ts`（app 级键位表——`app.interrupt`(esc)/`app.clear`(ctrl+c)/`app.exit`(ctrl+d)/`app.tools.expand`(ctrl+o)/`app.clipboard.paste`(ctrl+v)，declaration merging 进 pi-tui `Keybindings` 注册表，类型安全）；`loader.ts`（keybindings.json 加载——未知 actionId/非法值/坏 JSON 全部**诊断透出**而非 pi 的静默跳过）；`manager.ts`（`NovaKeybindingsManager` extends pi-tui KeybindingsManager：三级合并 + reload + `isReserved` 查询）；
  - **三级合并（超 pi 一级）**：builtin < `~/.nova/agent/keybindings.json` < 项目 `.nova/keybindings.json`——按 actionId 整体替换（空数组=禁用），合并诊断启动时经 transcript 提示（不阻断）；项目级不做 trust 门控（纯声明式映射无执行面）；
  - **KeymapController 全量迁入键位表**：`matchesKey(字面量)` → `kb.matches(actionId)`——Esc 域级路由、ctrl+c 双击退出、ctrl+d 空退、ctrl+o 展开、ctrl+v 粘贴全部可配置；pi-tui 组件内建键位（编辑器移动/undo/kill-ring/选择器导航）随全局表自动跟随；hints 提示行（P0-1）本就经 `getKeybindings()` 动态生成，改键即变；
  - **restrictOverride 数据模型**：`RESERVED_KEYBINDINGS` 保留清单（app.interrupt/app.clear/app.exit/输入提交/选择器确认取消）——pi 语义对位（管 M4 扩展快捷键竞争，不管用户 JSON——用户对自己的键位有完全控制）；
  - 测试：`keymap/keymap.test.ts` 13 用例（loader 七场景 + 合并 + manager 五场景：三级覆盖/重绑定生效旧键失效/冲突透出/reload/isReserved），临时目录 fixture 路径注入隔离真实 `~/.nova`。nova-client 81 全绿。
- **P0-3 编辑器系统收尾（图片输入 + 粘贴/undo 验证确认）**：
  - **图片输入（pi handleClipboardPaste 对位）**：新建 `modes/tui/utils/clipboard.ts`——macOS 经 osascript（PNGf → JPEG 顺序尝试，零依赖系统内建命令）、Linux 经 wl-paste（Wayland）/ xclip（X11 兜底）、Windows 挂账；Ctrl+V 键位接入（KeymapController 路由 → EditorController.pasteFromClipboard，对话框开着让路）；语义对齐 pi：剪贴板图片写临时文件（`nova-clipboard-<uuid>.png`），**路径以文本进编辑器**，随 prompt 纯文本上送，LLM 经 read 工具读图（read 内建格式归一/EXIF 校正/预算压缩/非视觉模型降级）；无图片时退化为剪贴板文本插入；读取失败静默（无权限场景不打断输入流）。真实验证：osascript 链路（文本剪贴板静默空返 / PNG 剪贴板落盘为合法 PNG）+ build + 68 测试全绿；
  - **粘贴折叠 / undo / kill-ring 验证确认（pi-tui 内建，零改动）**：bracketed paste 大粘贴（>10 行或 >1000 字符）自动折叠为 `[paste #N +M lines]` marker、提交时 `expandPasteMarkers` 展开上送全文；undo（ctrl+-）/ kill-ring（ctrl+y / alt+y）/ deleteWordBackward（ctrl+w）等emacs 族键位均为 pi-tui 内建；
  - **补全描述列验证确认**：SlashCommand.description → SelectList 右列灰色描述（宽度 >40 显示、自动截断）——P0-1 数据链接通后 pi-tui 内建渲染，零改动。
- **P0-2 状态指示器家族（pi status-indicator.ts 复刻）**：
  - `components/status/indicators.ts`：`StatusIndicator` 基类（extends pi-tui Loader）+ 四变体——working（spinner）/ **RetryStatusIndicator**（CountdownTimer 倒计时："Retrying (2/3) in 5s… (esc to cancel)"）/ CompactionStatusIndicator（manual→"Compacting context…"、overflow→"Context overflow detected, Auto-compacting…"）/ BranchSummaryStatusIndicator；文案键位提示经键位表动态生成；
  - **状态详情数据链**：mapping 归约 `retryStatus`（auto_retry_start 的 attempt/maxAttempts/delayMs）与 `compactionReason`（manual/threshold/overflow）随事件记录/清空，store getter 透出；StatusController 按 status+详情创建对应变体（同变体续用防闪烁，切换即 dispose）；
  - **顺带修复**：`AutoCompactionReason` 改 str 子类枚举——线上序列化从 `AutoCompactionReason.OVERFLOW` 丑字符串变为干净的 `"threshold"/"overflow"`（契约重导，gen.ts 同步）；
  - 测试：mapping 状态详情用例（retry/compaction 记录与清空）。nova-client 68 全绿、harness 1224 全绿。
- **TUI 模块化分层（controllers/ 落地）+ 选择器系统（可搜索 + 元信息列）**：
  - **controllers/ 分层**（tui-design.md 的防膨胀地基）：`app.ts` 从 303 行瘦身为 163 行纯装配根——编排逻辑归位六个控制器：`editor.ts`（onSubmit 分发：! 前缀 bash 用户工具 / slash 可取消命令 / 普通 prompt + 补全安装）、`keymap.ts`（全局键位路由：ctrl 族 + Esc 域级分派 + ctrl+o）、`dialogs.ts`（四件套 + auth 等待框管理并入：pendingCommandCancel 句柄 + hasAuthDialog）、`transcript.ts`/`status.ts`（挪位）；`ExpansionState` 抽为独立类型文件（组件不再 import controller）；组件与编排分层各司其职；
  - **选择器系统**：`components/pickers/` 新建——`selector.ts` 基础件（SelectList + DynamicBorder + 标题 + 键位提示 + 可选 CountdownTimer）、`searchable.ts` 可搜索基类（Input 搜索框 + pi-tui fuzzyFilter + 多列元信息渲染 + 滚动窗口 + 计数 + 选中项跟随）、`hints.ts`（键位提示动态生成）；`CountdownTimer` 组件（pi 对位）；
  - **四件套 select 全面升级为可搜索选择器**：DialogController.showSelect 用 SearchableSelector 替代 SelectList 组装——**所有选择器自动获得"输入即过滤"**；糖库新增 `select_items`（结构化 `{value,label,description}` 元信息列，与 select 同通道零契约变更）；
  - **后端三命令接入元信息列**：/model（label=model id、description=provider+current 标记）、/resume（label=会话名、description=消息数+首条预览）、/fork（label=消息文本、description=条目 id）——select_items 直返 value，省掉字符串匹配；
  - 测试：三命令的选择器断言迁移到 items 结构；nova-client 67 全绿、coding 223 全绿。
- **呈现资源层落地（`src/resources/`，M4 方案 Step 1 收拢）+ 注册语义三修正**：
  - **resources/ 四件套**：`types.ts`（ResourceFile/ResourceDiagnostic 含 collision——pi diagnostics 对位，冲突/失败/trust 剔除全部结构化）、`discovery.ts`（ui/ 资产发现上移自 packages/assets.ts）、`trust.ts`（**trust 过滤上移到编排层**——project 级包不被信任时连发现域都不进，pi 编排思想；loader 回归纯加载器）、`loader.ts`（jiti 管线平移自 extensions/loader.ts：mtime 缓存/preview 钩子/耗时观测不变）；packages/assets.ts 瘦身改名 `npm.ts`（纯 npm 自愈）；`extensions/` 目录删除（全量扩展宿主 M4 再立）；
  - **slots 注册表整体替换**：`refreshPackages` 时新建 `SlotRegistry`（对齐后端 ToolsManager.refresh 的原子形态——reload 不再残留卸载包的渲染器；`runtime.slots` 去 readonly，消费者经 runtime 现取）；
  - **覆盖碰撞可见性**：注册时同键异源覆盖生成 collision 诊断（pi ResourceCollision 对位——"我的渲染器被谁顶掉了"从静默变可查），经 diagnostics 透出为 notice；slots 回归纯注册表（碰撞生成归资源层）；
  - **cache miss 提醒（pi `maybeShowCacheMissNotice` 对齐）**：新事件 `cache_miss`（Bus 2，契约 minor 加法）——`EventController` 在 message_end（assistant 正常结束、持久化前）调 `detect_cache_miss`（复用既有 `cache_stats.py`——此前零调用点）+ settings `show_cache_miss_notices` 门控（默认关闭）；前端 mapping 产 notice 条目（pi 文案：显著阈值 2 万 tokens/$0.1、模型切换/超 TTL 空闲标签）；后端 3 用例（发射/门控/噪声地板与首轮）+ mapping 2 用例；
  - 测试平移与修正：loader/discovery 测试归位 resources/、断言迁移到 diagnostics 结构（`errorsOf` 筛选）。nova-client 67 全绿、harness 1224 全绿。
- **渲染器加载结果缓存（pi loadExtensionsCached 对位，mtime 自失效模型）**：`loadRenderers` 新增按（文件路径 + mtimeMs）的结果缓存——`refreshPackages` 重复刷新时未变化的渲染器**零 jiti 编译**直接注册；文件内容变化（mtime 变）自动重载，无需显式失效通道；`force` 选项预留 reload 场景旁路。**加载耗时统计**：`loaded` 项携带 `durationMs`（pi `time()` 对位——包多后排查启动性能的观测点）。**两个实测逮出的实现陷阱**：①jiti 自带 `fsCache/moduleCache` 跨实例共享且不校验 mtime（坏语法同 mtime 仍返回旧产物）——已禁用，外层缓存为唯一缓存层；②测试模拟"同 mtime"时 `new Date(mtimeMs)` 截断亚毫秒导致 utimes 设回值不等——utimes 用浮点秒传参。4 个缓存用例（命中/mtime 失效/force 绕过 + 既有 preview 收集）。包少时收益有限，为生态成型（包多）后的启动性能提前就位。
- **渲染器资产目录纪律修正（发现 bug + 归类修正）**：`discoverUIAssets` 原按扩展名全收 `ui/renderers/*.ts`——辅助模块 `edit-preview.ts`（无默认导出渲染函数）与 `edit-preview.test.ts`（测试）都被当渲染器加载，每次启动产生两条"渲染器加载失败"错误通知。修正三层：①辅助模块归 **`ui/lib/`**（renderers/ 恢复"一文件一工具、默认导出渲染函数"的纯发现域）；②TS 测试汇聚 **`tests/renderers/`**（与 pytest 的 `tests/` 同目录按领域分类，替代 ui/ 内就近 colocated——测试文件不应出现在资产发现域）；③`discoverUIAssets` 跳过 `*.test.ts`（防御放错）。A/B 两型包同步（package.json test script 指向 tests/、tsconfig include 覆盖）；真实验证：发现结果仅 bash/edit/read、edit 双导出正常、两包 npm test + typecheck 全绿。
- **包定名 `nova-client`（纯设计驱动）**：包内容 = 连接分布式后端的客户端运行时 + 客户端产品（TUI 为一种客户端形态，M3 的 Web UI 为另一种）——"client" 让包内每个目录的存在都有解释（wire=客户端传输、mirror=客户端状态投影、modes/tui=一种客户端形态），并与 harness 的 `NovaRpcServer` 形成协议两端的命名呼应。改名链：`nova-ui-runtime` → `nova-ui` → `nova-client` 终态；全量同步目录/package.json/tsconfig `paths` 自引用/jiti 别名（渲染器 import 契约 `from 'nova-client'`）/A-B 型渲染器包 import 与 devDep/harness 契约工件路径/文档全套。验证：build / 74 测试 / bin / tsx 直跑 / 两渲染器包 tsc / harness 1221 / coding 223 全绿。**附随修正**：edit-preview 算法测试从 nova-client 归位渲染器包就近 colocated（`ui/renderers/edit-preview.test.ts`）——nova_coding_agent 与 nova-coding-agent-ui 两包补齐 TS 测试设施（tsx + `npm test` + `typecheck` script），渲染器包自此自含"源码 + 测试 + 类型检查"完整开发面。
- **ui-runtime 结构精修（纯设计驱动）**：`session/` → `mirror/` 改名（名实相符：该目录装的是会话镜像三件套 mapping/store/types，"session" 易误解为会话管理——后端职责）；`modes/tui` 组件的包根 import 从四层级相对路径（`../../../../index.js`）改为 **Node 包内自引用**（`from 'nova-ui-runtime'`——tsconfig `paths` 管编译期、package.json `name`+`exports` 管运行时）——内置宿主与外部渲染器包作者写同一个 import，出口面被宿主 dogfood 验证；包名 `nova-ui-runtime` 保留（它是渲染器 API 的契约名，名实相符；产品身份由 `bin: nova` 承担）。三种执行形态验证：tsc build / node dist / tsx 直跑。
- **RPC stdio 管道健壮性（读侧行限 + 写侧异步化）**：
  - **读侧**：`StreamReader` 显式 `limit=64MB`——asyncio 默认 64KB 行限会拒收合法大帧（带图 prompt 的 base64、大会话全量历史），且 `readline` 超限抛错触发 server 读循环 `break`（连接死亡）。属 latent bug（TUI 未开图片输入故未触发）；
  - **写侧**：同步 `sys.stdout.write+flush` 改为 `connect_write_pipe` 的异步 `StreamWriter`（`drain()` 协作式让出——FlowControlMixin 提供流控钩子）——消除"大帧 + 慢读端时 flush 阻塞整个事件循环"的冻结隐患（abort 应答被队头阻塞）。协议帧直写 stdout fd，不再经 `sys.stdout` 层——OutputGuard 杂散拦截语义自动保持（依赖库 print 仍拦到 stderr，协议帧无需白名单标记），`StdioTransport` 的 `output_guard` 参数随之退役；
  - **测试**：新增 `test_stdio.py` 真实子进程端到端 3 用例（256KB 大帧往返完整、写入期间事件循环活跃度探针、小帧回归）。
- **工具卡片两阶段生命周期（完全复刻 pi）+ edit 执行前 diff 预览**：此前 nova 的卡片等 `tool_execution_start` 才创建（执行开始才可见），pi 的卡片在**参数流式期**就出生（message_update 扫描 assistant content 的 toolCall 块，`updateArgs` 逐段累积）。本次完全对齐——
  - **mapping 重构**（`session/mapping.ts`）：`message_update` 扫描 content 的 toolCall 块即建卡（`streaming` 态，参数逐段累积可见——数据面本来就在线：nova_ai 每个 delta 经 `parse_streaming_json` 增量填充 `arguments`）；`message_end` 正常结束标记 `argsComplete`（"参数完整、执行未开始"窗口，pi `setArgsComplete` 对位）；`tool_execution_start` 复用流式卡片（只转 `running`，不动 args）；R7 abort 收尾扩展覆盖 streaming 态。卡片状态机：`streaming → running → done/error`；
  - **edit 执行前 diff 预览**：渲染器模块新增可选 `preview` 命名导出（`PreviewComputer`——异步只读计算器，渲染器纯函数纪律下包作者的唯一副作用通道）；loader 收集、slots 注册/解析；`ToolCardView` 在 `streaming + argsComplete` 时发起（previewArgsKey 防重算、参数变即作废），产出经 `RendererInput.preview` 回注渲染器；`edit-preview.ts`（A/B 两型包各一份）**逐行复刻 pi 的 edit-diff.ts 匹配链**（精确 → fuzzy 归一两级匹配、多 edit 反向应用、重叠/重复/无变化报错、fuzzy 时保留未改行原始字节），`createTwoFilesPatch` 产标准 patch——渲染器同通道消费（执行后 details.patch 无缝取代预览）。真实验证：jiti 加载 → 只读计算 → diff 块产出 → 匹配失败红字路径；
  - **小差距补齐**：retry 文案（`lastRetryAttempt` 跟踪 auto_retry_start 的 attempt，abort 时组装"第 N 次重试后中止"——卡片收尾与 assistant 错误行同文案）；**`entry_appended` 事件**（后端契约加法：`EntryAppendedEvent` 入 Bus 2 联合，扩展 `append_entry` 追加 custom 条目时实时发射——扩展自定义内容不再等会话重开才进 transcript；mapping 仅消费 custom 类型，对齐 pi）；
  - **测试**：mapping 两阶段 5 用例 + entry_appended/retry 2 用例；loader 的 preview 收集用例；`edit-preview.test.ts` 12 用例（两级匹配/多 edit 语义/只读全链路/fuzzy 命中——import 真实包文件，跨包引用有意为之：预览 diff 必须与执行 diff 一致）；harness `test_entry_appended.py` 2 用例。tsconfig 排除 `*.test.ts` 出产物（顺带修跨包引用的 rootDir 报错）；
  - **cache miss 提醒（pi `maybeShowCacheMissNotice`）挂账**：它是独立的统计通道（cache_waste 在 getSessionStats），非 transcript 归约核心，下一轮随 stats 呈现一起做。
- **nova-tui 并入 nova-ui-runtime（TUI 从独立包变为运行时的一种宿主形态）**：对齐 pi 的结构（`modes/interactive` 就是 coding-agent 包的 mode）。原 `packages/nova-tui` 整体迁入 `packages/nova-ui-runtime/src/modes/tui/`（app.ts 薄壳 + components/ + blocks/ + themes/），包内 import 由包名根导入改为相对路径；`bin.nova` 入口移至 `dist/modes/tui/main.js`（补 shebang）；package.json 合并（终端依赖 pi-tui/chalk/commander/diff 入主包，engines 升至 Node >=22.19.0），版本 0.2.0；新增 `npm run tui`（tsx 直跑）。理由：唯一消费者作为主体的 mode 比独立包更符合消费关系（单向依赖、免版本对齐），且为 M3 WS 宿主（`hosts/`）与将来更多宿主形态留好了平级位置。构建 + 54 单测 + `nova --help` 真实跑通验证。注意：原 nova-tui 的 `smoke/` 伪 TTY 冒烟脚本系未跟踪文件，搬迁时遗漏被误删，待重写。
- **`cancelRequest`（RPC 级调用取消，LSP `$/cancelRequest` 的方法版）+ OAuth 登录 Esc 取消全链路**：此前 `/login` 的 device code 轮询最长 15 分钟不可取消（signal 通道流空：命令态无 run signal；前端无 Esc 入口），且"取消"后浏览器完成授权仍会落盘 credential。方案按"取消走在承载工作的通道自己身上"定调——长 RPC 调用的取消走 RPC 通道，复用 request id 寻址（不发明流程 id）：
  - **harness**：`ServerState.request_tasks` id→task 映射（server `_handle_raw` 登记/清理）；`cancelRequest` 方法（system 域，幂等 `{cancelled}` 应答）；`_handle_raw` 新增 `except CancelledError` 写回 `-32800`（对齐 LSP RequestCancelled）后 re-raise；`TransportUIContext.request` 补 `CancelledError` 收尾（发 `ui/cancel` 撤销帧——修掉 task 取消时前端对话框变僵尸的泄漏）；`UIAuthInteraction` 的 auth_url/device_code 改发结构化 `type:"auth"` 通知（url/userCode 字段，message 文本保留兜底）；契约 MINOR 1.0→1.1，方法面 63→64；
  - **ui-runtime**：`client.callCancellable()` 新增（返回 `{promise, cancel}`——cancel 本地 AbortError 立即收尾 + 发 cancelRequest 帧，幂等）+ `runtime.promptCancellable()` facade（slash 命令专用；普通对话的取消仍走 `abort` 域方法）；
  - **nova-tui**：新组件 `AuthWaitingDialog`（复刻 pi LoginDialogComponent 三态：hyperlink URL + 授权码 + waiting 提示；组件持有 onCancel——pi AbortController 内聚模式的跨进程对位）；`/` 命令走 promptCancellable 并持有取消句柄；`type:"auth"` 通知经 DialogController 分流开框；Esc 路由加等待框让路分支（优先级：四件套 > auth 框 > 状态域级路由）；取消路径本地显示"已取消"；
  - **测试**：cancelRequest 方法级 3 用例（取消在飞 task/幂等/参数校验）+ server 端到端（-32800 应答 + 映射清理 + 已完成调用幂等）+ TransportUIContext task 取消收尾 + ui-runtime callCancellable 2 用例（cat 回显替身）。
- **RPC 域级中止补齐 + Esc 域级路由**：新增 `abortCompaction` 方法（只停上下文压缩，不动 run/retry/用户工具——核心 `AgentSession.abort_compaction` 此前未暴露到 RPC，契约工件重生成，方法面 62→63）；nova-ui-runtime facade 补 `abortRetry()`/`abortCompaction()` 类型化转发；TUI 的 Esc 从"仅 working 可 abort"升级为按 `store.status` 域级分派（working→`abort`、retrying→`abortRetry`、compacting→`abortCompaction`，对话框开着仍优先让路给焦点框）——此前 retrying/compacting 时 Esc 无反应，唯一手段是宽级联 `abort` 误伤进行中的 run。
- **M1 薄 TUI 复刻深化（pi 组件族移植 + 目录定型）**：`src/` 定型为 `components/{transcript,dialogs,pickers,status,editor,layout}` + `blocks/` + `themes/`（对位 pi 结构）。转录族：tool-execution 完整版（args 摘要标题/折叠预览 ctrl+o/状态色）+ 条目族（BashExecutionView 边框卡片/CompactionSummaryView/BranchSummaryView/CustomMessageView 均复刻 pi 视觉）+ 排队消息区（steer/follow-up 快照字段驱动）+ diff 块升级（pi renderDiff 行内词级高亮，`diff` 包）+ bash 渲染器长输出截断（PREVIEW_LINES=20 对齐 pi）。状态区：FooterView 完整版（cwd+git branch（Node 本地读 .git/HEAD+watch）+ 会话名 + token 统计（getSessionStats）+ model/thinking）。**mirror 补齐**：历史 sync 新增 bashExecution/custom/compaction/branch_summary/custom_message 条目映射（此前全部漏掉——重开会话这些内容消失）+ assistant 条目透出 stopReason/errorMessage（abort/error 红字呈现）。**`!` 前缀路由**：编辑器检测 `!cmd` → invokeUserTool('bash')（pi bash 模式对位）。**主题**：pi dark.json 语义色直搬（chalk.hex/bgHex 真彩色，toolPendingBg 等暗色背景）。
- **RPC 序列化修复（冒烟逮出）**：`SourceInfo`（dataclass）混入 getCommands 结果打爆 `json.dumps`——两层修：system.py 三个方法改用 `serialize_source_info`（resources.py 助手公开复用）；`NovaServer._write` 出站帧统一过 `to_json_safe`（serialize.py 公共出口）兜底——单个坏结果不再打爆整个写入路径。
- **M1 薄 TUI 落地（nova-tui 全删重写为 NovaUIRuntime + pi-tui 薄壳）**：旧 nova-tui（前 runtime 时代、自带 RPC 客户端、消费已删除的旧映射协议）整体删除重写。槽位布局（header/chat/status/editor/footer）；transcript 三视图**复刻 pi interactive 组件主体**（UserMessageView=Box 背景+Markdown、AssistantView=thinking 独立斜体暗区块、ToolCardView=状态背景色卡+slots 渲染器）；**thinking 与正文在 mirror 层分离**（assistant 条目新增 `thinking` 字段，事件流与历史回放同构）；对话框四件套（pi-tui `SelectList`/`Input` 现成件 + editor 槽位替换法）+ **`ui/cancel` 撤销帧全链路**（后端 `TransportUIContext.request` 加 signal 竞速、abort 即向桥发 cancel；bridge 路由；前端关框应答 cancelled）+ 诚实能力宣告（对话框就位才宣告四件套）；键位（Esc abort/ctrl-c 双击/ctrl-d 空退）+ slash 命令补全（`CombinedAutocompleteProvider` 经 RPC getCommands）；状态区（pi-tui `Loader` 驱动于 store.status + footer cwd/model/thinking）。端到端验收（伪 TTY 冒烟）：真实后端对话流式 + thinking 渲染 + /fork 选择器弹窗可答。`NOVA_PYTHON` 环境变量指定后端解释器（dev 用 pixi python）。
- **真实 LLM 集成测试套件（`tests/integration/`，38 用例全真 API、零 mock 核心链路）**：
  - `test_real_agent_loop.py`（13）：双 provider 基本对话（含 provider/model 出处戳）、工具调用全链路 ×2（事件链 + toolResult + 工具结果后必须有总结消息）、流式 `message_update` 与思考块、模型切换出处戳、follow_up idle 开新一轮、user tool 上下文注入、散装 `.agents` skill 标记验证、多工具调用、abort 恢复、会话 JSONL 持久化、compact 边界；
  - `test_real_tools.py`（7）：write/read 磁盘往返、edit diff、grep/find/ls 三件套、bash 非零退出 is_error、read 不存在文件、流式输出事件、并行双调用；
  - `test_real_compaction.py`（4）：**真实超窗压缩双路径**（多轮大粘贴把上下文推过 keep_recent_tokens 窗口——手动 compact（关 auto）→ 事件链 + 结果 + 消息数下降 + 续聊可用；**auto-compaction 真实触发**（超窗自动压缩 + 二次边界））、二次压缩边界、compaction 条目持久化；
  - `test_real_session.py`（7）：`switch_session` 恢复历史并续聊、new_session 干净态、fork 前缀保持、setSessionName 事件、export JSONL、steer 进行中真实插入、扩展命令 `/session` 零 LLM 执行；
  - `test_real_features.py`（8）：thinking off/high 对照、`/debug` 模板生效、setActiveTools 禁用/恢复、queue_update 与 drain、retry 开关、trust=False 附录门控、abort_user_tool。
  凭证走 auth.json/环境变量，缺失自动 skip；`-m integration` 按需运行（默认排除）。
- **SDK 示例 notebook**：`examples/02-sdk-tour.ipynb`——遍历 SDK 能力面（kimi OAuth 主鉴权、volcengine ↔ kimi 模型切换、工具调用、项目级 `.nova/skills/` + `/skill:<name>` 注入（标记验证）、`invoke_user_tool` 上下文注入、`follow_up`、`compact` 边界、`dispose`）；演示用项目级自定义 agent（`.nova/agents/` + skills 白名单）。**全部 cell 经 jupyter 内核真实执行验证**（kimi OAuth + volcengine 真实 LLM 调用 + 真实 bash 工具调用）。新增 `_exec_nb.py`（基于 jupyter_client 的 notebook 忠实执行器，校验用）。
- **`permission_gate` 扩展（对齐 pi 的 permission-gate.ts + protected-paths.ts，合并为一）**：`nova_coding_agent/extensions/permission_gate.py`（单文件形态）——`tool_call` 拦截：bash 危险命令（`rm -rf`/`sudo`/`chmod|chown 777`，pi 同款三条正则）有 UI 经 `ui.select` 询问（Yes/Always/No，拒绝或取消即 block）、无 UI（headless/print）直接 block（fail-closed）；write/edit 写保护路径（`.env`/`.git/`/`node_modules/`，pi 同款）命中直接 block + notify（不询问）；超集一项：Always 会话级记忆（精确命令串，扩展实例生命周期内不再问，reload 即失效）。block reason 经错误工具结果回给 LLM 可调整方案。规则硬编码对齐 pi；登记入 `[tool.nova] extensions` 与 coding_agent 白名单。12 个单测（UI/无 UI/Always 记忆/保护路径/无关工具/防御分支）+ 框架 `ExtensionLoader` 真实加载冒烟。bundle 两个扩展（含既有 `session_commands`）同步统一为单文件形态，扩展形态纪律写入 AGENTS.md（单文件优先、目录按需，与 tools 同一纪律）。
- **`session_commands` 扩展补强（对齐 pi 的 slash 命令面与选择器 UX）**：`/fork` 与 `/model` 无参数时经 `ui.select` 弹选择器（fork 拉会话条目筛 user 消息、最新在前、选中按 `position="at"` 分叉——对齐 pi `getUserMessagesForForking` 语义；model 列可用模型并标记 current），无 UI 时退化为参数用法/当前值展示；新增 `/resume`（会话目录列表选择器 + `switch_session`，排除当前、按修改时间倒序）、`/login`（复用模型运行时 OAuth 登录联动，`UIAuthInteraction` 桥接扩展上下文 UI，取消/失败优雅降级为消息）、`/logout`（删除 credential 联动模型刷新）三命令。15 个行为测试（选择器选取/取消/空候选/headless 退化/登录取消与失败/登出失败等）。

### Fixed
- **read 工具图片处理深度对齐 pi（7 项）**：`process_image` 挪 `asyncio.to_thread`（不再同步阻塞事件循环）；EXIF 方向校正（`ImageOps.exif_transpose`）；bmp 等 API 不接受格式转 PNG + 转换提示（落 JPEG 时提示目标格式，对齐 pi）；**字节预算压缩链**（base64 ≤4.5MB：PNG → JPEG 质量 80/85/70/55/40 → 尺寸 ×0.75 递减至 1x1，PNG/JPEG 择优）；缩放坐标映射提示（`Multiply coordinates by X.XX to map...`，供模型换算）；**失败语义对齐 pi**（无法解码/压不进预算给提示文本让模型继续，不标 is_error，真正读取异常才标）；macOS 路径变体四级重试（初始 Unicode 空格/@前缀归一 + AM/PM 窄空格、直引→弯引、NFD、NFD+弯引组合——以 pi path-utils 源码方向为准）。新增 29 个单测（image 管线 17 + 路径变体 12），另修复旧 `_TINY_PNG` 坏样本。
- **subagent parallel 模式 is_error**：任一子任务失败即整体标记（对齐 single/chain）。
- **LLM/session 工具对 pi 的六域审计与真缺口修复**（亲读双方代码的功能对账，结论：总体不弱于 pi 且多处反超，修复以下真缺口）：
  - **edit CRLF 换行符保真实质 bug**：`tools_common/operations.py` 用文本模式 `open()` 读写（universal newlines 读时吃掉 `\r\n`），`detect_line_ending`/`restore_line_endings` 成死代码——CRLF 文件被静默改写 LF、Windows 反向改写。edit/write I/O 改二进制 + 显式编解码（原字节保真）；补字节级往返单测；
  - **grep/find 子进程退出码不检查**：坏正则/坏 glob/路径不存在静默误报"No matches found"。新增前置 Path not found 检查（三后端统一）；rg/fd 非预期退出透出 stderr 为 `is_error=True`（实测托管 fd 错误码为 1，与 rg 的 2 不同，注释注明）；连带修复 finally 无条件 kill 抹掉真实退出码；
  - **file_queue 写锁**：锁键改 realpath 归一（软链/别名共享锁）+ 引用计数回收（防内存累积与唤醒窗口竞态）；
  - **bash 工具三对齐 pi**：description 补截断/落盘语义（2000 行/50KB、全量存临时文件可续读）+ `prompt_snippet`；timeout 入参校验（非有限/≤0/超上限显式报错，文案与 pi 逐字一致）+ **默认无超时**（pi 同款，不再 60s 误杀长命令）；预 spawn 中止检查（已中止不启动进程）；
  - **find**：`limit` 参数补进 schema；find/ls 输出补 50KB 字节截断；
  - **subagent**：single/chain/unknown-agent 失败补 `is_error`；`_result_to_dict` 保留 `messages`（子 agent 过程历史）与独立 `stderr` 字段（并删除 runner 内同名过期副本，防双源漂移）；project agents 发现改祖先上溯（对齐 pi，到 git root）；
  - **`user_bash` 扩展拦截事件接线**（pi 对齐）：runner 已定义但零调用点的事件落地——会话 bash 执行前发射，扩展可返回 result 整体替换执行或注入自定义 operations；`UserToolDefinition` 新增可选 `build_result_message` 转换器（bash 消息类型归包分发，泛化层不反向依赖）；7 个拦截单测 + 3 个转换器单测；
  - **渲染器 import 去 monorepo 相对路径**：`nova_coding_agent/ui/renderers/*.ts` 与 `nova-coding-agent-ui/ui/renderers/*.ts` 的 `../../../nova-ui-runtime/src/...` 相对导入（copy 模式安装即断）全部改为包名根导入 `from 'nova-ui-runtime'`（jiti 别名解析，冒烟验证六渲染器加载渲染正常）；两包 `package.json` 加 `devDependencies: file:../nova-ui-runtime` 供 tsc 解析（devDep 不进生产安装，npm stage `--omit=dev` 不受影响）；
- **工具结果错误语义对齐 pi（`AgentToolResult.is_error` 加法）**：pi 对失败（非零退出/文件不存在）**抛异常**驱动 `isError`，而我们此前工具对预期内失败只返回错误文本、`is_error` 恒 False（LLM 与 UI 错误卡片无信号）。修复：`AgentToolResult` 新增 `is_error: bool = False` 字段（结果级错误标记，异常路径由执行框架另行标记）；`_finalize_executed_tool_call` 采纳（`executed.is_error or result.is_error`，afterToolCall 钩子保留最终覆盖权）；bundle 工具全面设置——bash（非零退出/超时/取消/基础设施失败）、read（参数错误/文件不存在/非文件/读取异常）、edit/grep/find/write/ls 各错误分支。集成测试 `test_bash_nonzero_exit_is_error` / `test_read_missing_file_is_error` 真实钉住。
- **kimi-coding provider 兼容性：`developer` 角色被拒**：`_OPENAI_COMPAT` 未声明 `supports_developer_role`（None → 探测为 True），reasoning 模型的 system prompt 被以 `developer` 角色发送，kimi coding API 返回 `400: role 'developer' is not allowed`。修复为 `supports_developer_role=False`（kimi API 只接受 `system`）。k2p7 实测对话/工具调用正常。
- **三个"仅真实运行可见"的致命链路 bug**（测试全绿但真实运行必现——FakeSession/无 LLM 测试结构的共同盲区，系统性解法归挂账的"假模型通道"）：
  - **`events.py` message_end 传参错位**：`EventController` 把裸消息传给 `runner.emit_message_end`（收 `MessageEndEvent` 事件对象），真实运行首个消息即 `AttributeError` 崩溃（print/RPC 全线）。修复为构造事件对象传入；新增 `test_events_controller.py` 两条回归（真实 ExtensionRunner：零扩展穿透 + 扩展改写原地生效）；
  - **`ToolController.refresh_registry` 未同步工具**：启动路径只刷新 ToolsManager 注册表，从不调用 `_sync_to_agent()`（此前仅 `set_active_by_name` 调用）——`Agent.state.tools` 恒为空，LLM 收不到工具定义，模型只能"口述"工具调用（从不产生 `tool_execution` 事件）。修复为刷新后同步；新增 `test_tools_controller.py` 回归；
  - **`should_stop_after_turn` 钩子返回 dataclass 恒 truthy**：钩子直接返回 `ShouldStopAfterTurnEventResult` 对象（dataclass 实例恒 truthy），`agent_loop` 的 `bool(...)` 恒为 True——装载任意扩展（如官方 bundle 的 session_commands）后**每个 turn 都被误判停止**，工具结果永远送不回 LLM（turn 在 toolResult 后即终结，无总结消息）。修复为取 `.stop` 字段；
  - **`follow_up` idle 语义缺失**：`follow_up` 只入队不驱动——idle 时消息永久滞留（turn 末 drain 只覆盖"turn 进行中排队"场景）。修复为对齐 pi 语义：idle 且已有历史时经 `continue_` 立即 drain 开新一轮；首轮 follow_up 路由为 `prompt`（避免重复展开）。
- **nova-ui-runtime 契约红利收割与活性修缮（A1/A2/A3 + facade 纯度）**：
  - **A1 RPC 全面类型化**：`client.call` / `runtime.invoke` 泛型化（`call<M>(method, params?) → Promise<WireResult<M>>`，方法名/params/result 走 `NovaWireMethodMap`）；`schema_export` walker 新增 `map_params`——params 模型带默认值字段在 TS 标记 `?`（与后端 pydantic 默认值语义同源，结果模型不受影响仍全量）；runtime 的 `as` 裸断言全部清除，`setThinkingLevel`/`setSteeringMode` 等参数直接用契约枚举类型；
  - **A3 后端死亡不静默**：子进程退出时在飞调用全部 reject（此前 Promise 永久悬挂）+ `closeSink` 经 `RuntimeHost.onClose` 浮现给前端（M3 断线重连的挂点）；
  - **A2 快照活性**：生命周期布尔（`is_compacting`/`is_retrying`）随事件直写（事件即迁移本身，非推测）；命令响应回写通道 `store.updateSnapshot(patch)`——`setActiveTools`/`setAutoRetry`/`setSteeringMode`/`setFollowUpMode` 的响应事实顺手回写；`updateSettings` 后经 `refreshSnapshot()` 全量重拉对账；`is_streaming` 刻意不推导（spinner 语义归 `store.status`）；
  - **facade 纯度**：包更新提醒的产品逻辑（查什么/怎么格式化/失败静默）落位 `packages/updates.ts`（设计 §8 既定位置），runtime 只剩"文本 → notice"一行编排；
  - **测试**：bridge 路由 6 用例（ui/request/notify 帧→词汇翻译、respond/capabilities 帧形状、未知反向方法静默）；store 生命周期布尔 + updateSnapshot 用例；bridge 构造参数收敛为 `ReverseChannel` 结构接口（可测替身）。50 个 node:test 全绿。
- **nova-ui-runtime 按 v3.1 设计一次重写成型（六子系统骨架 + R6/R7/R8 契约修缮）**：
  - **结构重写**：`wire/`（client 传输 + capabilities 握手与能力位 + bridge 反向原语路由，帧形状对齐 `UIContext`）、`bus/`（观察式脊柱：mirror 特权订阅按序恰好一次且异常响亮冒泡、观察者异常隔离、派生便利事件 `session:synced`/`turn:*`）、`session/`（mirror 三件套）、`presentation/`（blocks + slots 三族统一注册表，后注册覆盖同键、来源在案、空态即通用回退）、`packages/`（pkgList 索引 + ui/ 资产发现 + npm 自愈）、`extensions/loader`（jiti 加载 `ui/renderers/*.ts` → slot 注册，`nova-ui-runtime` 别名锚定 import 契约）、`hosts` 不立目录：`RuntimeHost` 接口与进程内实现同居 `runtime.ts`（M3 WS 宿主落地时再拆，index.ts 公共面不变）；旧 `client/` 目录删除；
  - **R6 契约版本 major/minor**：`CONTRACT_VERSION` → `CONTRACT_VERSION_MAJOR=1/MINOR=0`（schema 工件 + `initialize` + TS 常量三处同源）——major 不等硬拒、minor 加法放行（能力位与未知事件忽略降级）；语义注释写入 schema_export；
  - **R7 abort 卡片收尾**：`message_end(stop_reason=aborted|error)` 把未完结工具卡片标 error（错误文本取 `error_message`），`agent_end` 兜底——pi 的 message_end 收尾模式；
  - **R8 快照双源合并**：`session/types.ts` 手写 `SessionSnapshot` 删除，re-export 生成类型 `SessionStateResult`；
  - **快照四事件直写**：`model_changed`（防御性窄化 ModelRef，形状异常不写）/`thinking_level_changed`（null→"off"，与后端同语义）/`session_info_changed`/`queue_update`（独立 `queue` 变更区）直写快照，其余字段不猜增量语义；
  - **渲染器加载链路真实跑通**：对 editable 安装的官方 bundle（A 型）与 `nova-coding-agent-ui`（B 型）完成 发现 → jiti 加载 → slot 注册 → 真实渲染产出块；project 级包经 `project_trusted` 门控（jiti import 会执行包代码，与 Python 扩展同一门控）；node_modules 缺失时 npm 自愈补装；
  - **契约加法（minor 机制首批 dogfood）**：`PackageView.install_path/scope`（B 型纯 UI 包此前无任何路径信息，Node ui/ 发现的唯一通道）、`SessionStateResult.project_trusted`（会话信任决议透出）；
  - 测试：40 个 node:test 单测全绿（mapping 7+R7 新增 4、store 5、bus 6、capabilities 5、slots 4、assets 4、loader 5）；jiti 成为首个运行时依赖。
- **B 型纯 TS 包支持（package.json 身份证）+ 两种包形态 dogfood**：
  - `core/package/manifest.py`：`read_manifest` 在 pyproject.toml 缺席时回退包根 `package.json`（name 去 npm scope 前缀、version/description/author）；`load_package_json` 新增；install 入口与 `validate` 对"无能力类目但 package.json + ui/ 目录"的包放行（拒绝消息同步更新）；B 型自然跳过 pip 阶段与自安装，npm 阶段照跑；
  - **一体版（A 型复合包）**：`nova_coding_agent` 升级为复合包——根 `package.json`（`pretty-ms` 依赖 + typescript devDep）+ `ui/renderers/`（`bash.ts` 终端风 / `edit.ts` diff 风（消费引擎预生成 patch，零重算）/ `read.ts` 文件风）+ `tsconfig.json`；旧时代残留 `ui_blocks/`（renderer.json + schema.py 双文件）删除；
  - **拆分版（B 型）**：新包 `packages/nova-coding-agent-ui/`——package.json 身份证（无 pyproject），独立提供同工具渲染器（bash 的 meta 改用 json 块，示范独立作者风格），前后端解耦开发/发布的参考实现；
  - **v1 声明式块词汇落地**：`nova-ui-runtime/src/presentation/blocks.ts`——diff/markdown/code/json/table 五种内建块 + `RendererInput`/`NovaRenderer` 契约 + detailsOf/extractText 共用工具（M2 渲染体系的第一块砖）；
  - 验证：两形态 editable 安装真实跑通 npm 阶段（pretty-ms 真实落 node_modules）、`tsc --noEmit` 双包零错误、`nova-pkg validate` 双通过、`test_b_type_package.py` 五用例 + 全量绿。
- **npm 依赖阶段（nova-pkg install 第 4 阶段，复合包双生态编排落地）**：包根存在 `package.json` 时自动装配 npm 依赖——有 `package-lock.json` 用 `npm ci --omit=dev`、无则 `npm install --omit=dev`（`--no-audit --no-fund`，600s 超时）；copy 模式在安装副本根执行，editable 在源目录根执行（与 pip -e 同语义）；npm 缺失/NOVA_OFFLINE/执行失败仅警告不阻断（TS 资产降级、能力部分不受影响，Node 层加载时自愈补装）。目录设计定案：**package.json 永远在包根**（A/B 型一条规则，JupyterLab 扩展同款 polyglot 形态），`ui/` 退化为纯源码与资产目录。`tests/core/package/test_npm_stage.py` 七用例锁定（检测/命令选择/目录选择/跳过条件/失败解耦）。
- **启动更新提醒（pi 同款拉模式）+ package 域定位修正**：
  - `pkgCheckUpdates` 只读方法（package 域第 6 个方法）——复用现有 `PackageManager.check_for_available_updates()`（git 源远程比对、并发、path/pin 跳过），`NOVA_OFFLINE`/失败静默返回空列表；形状进 `shapes.py`，一致性套件补离线断言（tmp 环境返回空且形状合法）；
  - nova-ui-runtime `start()` 完成同步后顺手拉取（`checkPackageUpdates()`，非空以 notice 条目进 transcript，`SessionStore.addNotice`）——"要不要打扰用户"的决策留在前端，不加 Bus 2 事件、不加后端推送任务；
  - package 域正式定位为 **operator-facing optional domain**（运营可选域）：服务应用内管理面板与远程前端，`nova-pkg` CLI 始终是开发者主通道，其他语言后端可不宣告（文档同步）；
  - 契约版本 2 → 3（方法表加法变更）。
- **契约加固四件套（多后端可插拔的机器化根基）**：
  - **方法形状（①）**：`methods/shapes.py` 为全部 61 个 RPC 方法声明域/params/result 模型——`MethodRegistry.register(domain=, params_model=, result_model=)` 携带形状，分派前 pydantic 校验（缺参/类型错误 → `INVALID_PARAMS`，布尔参数用 `StrictBool` 防 `"yes"` 宽松强转），`schema_export` 汇总为 schema `methods` 根 + TS `NovaWireMethodMap`（含 `NovaWireMethod` 联合），校验/导出/能力位三方同源；`getSessionState`/`initialize` handler 改为直接构造形状模型（实现与契约零漂移）；`_collect_method_shapes()` 从注册表实况收集，方法表即注册表；
  - **能力位真实化（②）**：`initialize` 返回 `capabilities.domains/methods`（注册表实况，8 域 61 方法），替代硬编码 `{streaming, tools, sessions}`——方法域成为可选宣告，其他语言后端可只实现部分域，前端按位降级；
  - **契约版本（③）**：`CONTRACT_VERSION`（当前 2）——schema 工件、`initialize` 握手、TS `NOVA_CONTRACT_VERSION` 三处同源，nova-ui-runtime 握手时比对，不一致响亮报错；
  - **黑盒一致性套件（④ v1）**：`tests/conformance/test_wire_conformance.py`——spawn 真实后端进程，经 stdio NDJSON 全双工驱动（单线程 Wire 客户端：响应配对/事件捕获/`ui/request` 自动应答），`jsonschema` 校验 getSessionState/listSessions/getSessionEntries 响应形状与 `thinking_level_changed`/`session_info_changed` 事件形状、非法参数错误码、反向原语往返（tmp agentDir 注入探针扩展）、关停语义；`createSession` 新增 `agentDir` 参数（隔离测试根）；LLM 事件覆盖待假模型通道；
  - **spawn 泛化（⑤）**：rpc-client 的 `pythonPath/serverModule` 改为 `command: string[]`（默认 `['python3','-m','nova_harness.modes.rpc.cli']`），任意语言后端二进制可接。
- **线上契约构建期类型导出（哑管道的类型管道）**：`nova_harness.core.rpc.protocol.schema_export`——单一 annotation walker（复刻 `serialize.py` 的落线语义：pydantic `model_dump(mode="json")`、dataclass `vars` 递归、str-Enum 取 value/其余取 `str(member)`、不可建模降级 unknown）从 `AgentSessionEvent`（Bus 2 全集 21 个事件）与 `SessionEntry`/`SessionHeader` 生成双工件：`packages/nova-ui-runtime/protocol/nova-wire.schema.json`（语言中立契约快照）+ `src/protocol/nova-wire.gen.ts`（Literal 判别联合 + `NovaEventEnvelope` 信封联合 + `AgentMessage` 具名别名）；两工件入仓，`--check` 模式 + pytest 漂移测试保证类型变更必须重新导出。nova-ui-runtime 全面采用生成类型：`RuntimeEvent = NovaEventEnvelope`、mapping.ts 改为判别联合驱动（compaction/retry 收敛为显式 case，联合外类型向前兼容静默忽略）、store.ts 快照事件名修正（`model_select`/`thinking_level_select` → 线上真实的 `model_changed`/`thinking_level_changed`，M1 潜伏 bug）、`SessionSnapshot` 补齐 `supports_thinking`/`available_thinking_levels`/`auto_retry_enabled`/`auto_compaction_enabled` 字段。零新增 npm 依赖（TS 由导出器直接发射，不经 json-schema-to-typescript）。
- **RPC 协议面第三轮（会话管理补面 + 思考级别 + 扩展快捷键通道，注册方法 56 → 61）**：
  - session 域补 `switchSession`（path 绝对路径或 sessionId 解析切换既有会话，无需二次 `createSession` 全量重建 runtime）、`cloneSession`、`exportSession` / `importSession`；`getSessionState` 快照补 `supports_thinking` / `available_thinking_levels` / `auto_compaction_enabled`；`listModels` 补 `reasoning` 能力位；
  - model 域补 `cycleThinkingLevel`——`ModelController.cycle_thinking_level` 改为真异步（删除"同步函数 + `asyncio.create_task` fire-and-forget"补丁）与 `_supports_thinking` 兼容别名；
  - system 域补 `getShortcuts` / `invokeShortcut`——扩展快捷键按架构 2.0 分层接线：运行时持**注册表与 handler**（`ExtensionRunner.get_shortcuts()` 只做扩展间冲突裁决、`invoke_shortcut()` 分发执行，错误走扩展错误事件）；键位捕获、内置键位表、用户自定义与冲突裁决归前端，后端永远不知道用户按的是哪个物理键；
  - settings/auth 域免会话：`getSettings` / `updateSettings`（无会话时经懒加载 fallback `SettingsManager`）与 `getAuthStatus` 在 `createSession` 前后均可用（settings/auth 是 agent_dir 绑定而非会话绑定）。
- **`nova-ui-runtime`：Node 扩展运行时（新 TS 包，架构 2.0 第二层，M1）**——TUI / Web UI 共享的厚应用层：spawn/接管 `nova-harness-rpc` 子进程；消费哑管道事件流（`{type, data}` 直通信封）经纯函数呈现映射（`session/mapping.ts`，含 node:test 单测）构建 transcript / 工具卡片 / spinner 状态模型；连接/恢复时经 `getSessionState` + `getSessionEntries` 全量重建；命令 API（prompt/abort/steer/followUp/setModel/cycleModel/compact/...）薄转发；反向原语（`ui/request`）转交前端 + `ui/response` 应答 + `system/capabilities` 上报。里程碑（README）：M2 渲染器注册表（按 tool_name/role）、M3 WebSocket 接入 + 多客户端扇出、M4 UI 扩展宿主（复合包 `ui/` 段）。
- **RPC 协议面补齐（对标 pi 前端能力面）**：
  - `methods/model.py`（新域）：`listModels`（全量模型 + 可用性标记）、`setModel`、`setThinkingLevel`、`cycleModel`、`listScopedModels` / `setScopedModels`；`resolve_model` helper 归位本域；
  - `methods/auth.py`（新域）：`getAuthStatus`（credential 元信息，不含密钥本体）、`setApiKey`、`logout`（OAuth 交互流程走反向原语，另行接线）；
  - `methods/resources.py`（新域）：`listPromptTemplates`、`listSkills`（前端 slash 菜单数据源）；
  - session 域补：`setSessionName`、`setSteeringMode` / `setFollowUpMode`（持久化到 settings）、`clearQueue`、`setLabel`（entry 标签）；`getSessionStats` 并入 `cache_waste`（cache-stats 的 RPC 暴露，定价源取会话 ModelRuntime）；`AgentSession` 新增公开方法 `set_label` / `get_cache_waste`；
  - system 域补：`getExtensionFlags`（定义 + 当前值）、`setExtensionFlag`（未知名拒绝）。
- **RPC 协议面第二轮（auth 交互登录 + settings 域 + retry/reload 入口）**：
  - auth 域补 `login`（OAuth device code / ApiKey prompt 统一入口，交互经 `UIAuthInteraction` → 反向原语 `ui/select`/`ui/input`；长命令，结果走 `ModelRuntime.login` 的模型刷新联动）；`logout`/`setApiKey` 修正为 `ModelRuntime` 联动路径（此前直接摸 AuthStorage，credential 变更后可用性快照漂移）；
  - `methods/settings.py`（新域）：`getSettings`（合并生效配置）、`updateSettings`（全局层部分更新——`Settings` 模型校验 + 未知键显式拒绝 + `deep_merge_settings` 的 fields_set 语义，显式 null 清除）；`SettingsManager` 新增 `get_settings()` / `update_global_settings()`；
  - session 域补 `abortRetry` / `setAutoRetry`（retry 手动控制）与 `reload`（资源/扩展/settings 热重载）；`getSessionState` 快照补 `auto_retry_enabled`；
  - `ModelRuntimeProtocol` 补 `refresh` / `login` / `logout` 成员。
- nova_harness：包 LLM 工具的上下文双通道（`core/types/resources/tools.py`）——构造期 `ToolContext`（不变量：`cwd` 值 + `settings` `ToolSettingsView` 只读 Protocol，SettingsManager 天然满足）经 `DefaultResourceLoaderOptions.tool_context` → `ToolLoader` → `Tool(context)` 注入；执行期 `ToolExecContext`（冻结值对象，当前为 `model` 字段）作为 `execute` 第 5 参由 `DynamicTool` 经 `context_provider` 每次调用现取注入（对齐 pi `wrapToolDefinition` 的 `ctxFactory`），provider 链路 `AgentSession.get_tool_exec_context` → `ToolController.refresh_registry` → `ToolsManager.refresh(context_provider=)`。
- 工具形态升级为**单文件优先**：`tools/<name>.py` 即工具（对齐 pi 的 `tools/bash.ts` 心智），`tools/<name>/executor.py` 目录形态保留给需同目录资产的场景；`ToolLoader` / `UserToolLoader` / `validation.py` / `resolve/discovery.py` 全部支持双形态。`Tool` 类属性补齐 `label` / `execution_mode` / `prepare_arguments`（与框架 `ToolDefinition` 同宽）。
- nova_harness：`agent_config.load_agents(base_dir)`——扫描基目录直接子目录加载全部 agent 配置。
- 初始化仓库结构：nova_ai、nova_agent、nova_harness、nova_team 四个子包。
- 完善各子包的 `pyproject.toml`、`.gitignore` 与 `README.md`。
- nova_harness：用户工具（user tool）包分发通道（阶段 4）——`[tool.nova]` 第六资源类目 `user_tools`（目录仅含 `executor.py`，暴露 `UserTool` 类：元数据为类属性、`__init__(session)` 注入会话、可选 `MESSAGE_TYPES` 类属性）；`resources/loaders/user_tools.py` 装载器（与 ToolLoader 同型）；agent.yaml 白名单 `user_tools`（未声明 = 全部允许）；与 tools 同一纪律——只来自已安装包，不走顶层自动发现/settings 条目。
- nova_harness：消息类型回载注册表（`core/harness/session/message_types.py`）——包级用户工具消息按 `role` 注册，JSONL 解析在静态 union 前拦截复原；包缺席（或注册类校验失败）降级为 `OpaqueUserToolMessage`（payload 全量保留、默认不进上下文、幂等再解析）；`SessionMessageEntry.message` 改为 `Union[Message, CustomMessage, SerializeAsAny[CustomAgentMessage]]` + 裸 dict 守卫（防空基类静默吞数据）。
- nova_coding_agent：`bash` 用户工具（`user_tools/bash/`），`src/nova_coding_agent/bash/` 子包收纳执行引擎（`engine.py`）、消息类型（`message.py` 的 `BashExecutionMessage`）与用户工具工厂（`user_tool.py`）；LLM bash 工具 executor 重写为共享引擎驱动（进程 spawn/清洗/截断/进程组 kill 全部收敛到引擎，本层只留超时、throttled on_update 与结果格式化），自此两个 bash 消费端共享同一引擎。
- nova_harness：用户工具（user tool）子系统——`UserToolManager` 注册表、`UserToolController`（pending/flush、abort 级联、消息记录）、RPC 方法 `listUserTools` / `invokeUserTool` / `abortUserTool`。设计见 `packages/nova_harness/examples/user_tools_design.md`。
- nova_harness：`core/utils/child_process.py`（detached 子进程跟踪 + `kill_process_tree` 跨平台，对齐 pi `utils/shell.ts`）；`core/utils/shell.py` 增加 `get_shell_config` 跨平台 shell 解析（Git Bash / WSL stdin 传输 / sh 兜底）；扩展注册的 `bash_spawn_hooks` 经 `ToolsManager` 注入 LLM 工具链执行体（`SpawnHookAware` 协议），与会话 bash 共用同一批 hook；`nova-harness-rpc` / print 模式在信号与退出路径清场被跟踪的后台子进程。
- 二进制依赖体系：`[tool.nova]` 新增 `binary_dependencies`（wheel 可装，如 `rg = "ripgrep==15.1.0"`，随 pip 依赖安装）、`binary_managed_dependencies`（框架注册表自管理——**注册表只收 PyPI 覆盖不了的官方必需二进制，当前仅 fd**；pin 版本 + sha256 下载到 `~/.nova/agent/bin/`，`core/package/binaries/registry.json` 数据文件驱动，stdlib tarfile/zipfile 解压 + staging/rename 原子安装 + 下载锁 + `NOVA_OFFLINE` 离线语义）与 `binary_system_dependencies`（系统要求，装时校验仅警告并附 brew/apt 安装指引）；`core/utils/binaries.py` 提供 `resolve_binary` 三级解析（env bin → nova bin → PATH，托管优先，PATH 层识别 `fdfind` 等发行版别名）；spawn 子进程 env 自动前置 nova bin + env bin；`nova_coding_agent` grep/find 改为三级兜底（fd → rg → 纯 Python），find 的 fd 缺位时由 `rg --files` 顶替（实测性能打平），目录查找经注册表自管理的 fd 加速。

### Changed
- **`agent_settled` 扩展事件（对齐 pi `_emitAgentSettled`）**：整个 run（含续话/队列 drain）终结后的"彻底安静"时刻——`_run_agent_prompt` 的 finally 中双发 Bus 2 与扩展面（正常结束/abort/异常路径均发射，run 终结即 settled）；事件加入 `AgentSessionEvent` union 并进 wire schema（漂移测试登记为 Bus 2 第 22 个事件）。4 条测试（双通道发射 / 时序在 prompt 完成后 / 异常路径照常 / 无 runner 仅 Bus 2）。
- **`before_provider_headers` 扩展事件（对齐 pi）**：provider 请求派发前的请求头改写通道——`BeforeProviderHeadersEvent`（handler **原地改** headers、返回值忽略、串行可见、异常 fail-open 转 error 事件）；`ExtensionRunner.emit_before_provider_headers`；接线经 nova_ai 既有的 `StreamOptions.transform_headers` 挂点（`create_stream_fn` 新增 `extension_runner_ref` 参数，与调用方既有 transform 链接而非覆盖）。8 条测试（runner 语义 4：原地改/链式可见/fail-open/无 handler；接线 4：安装/无 ref 不装/链接不覆盖/无 handler 透传）。
- **`session_info_changed` 双发到扩展面（对齐 pi setSessionName）**：`AgentSession.set_session_name` 此前只发 Bus 2（前端呈现），扩展订阅该事件永不触发（有常量有类型、唯独没接 runner）。补齐第二发：同步方法内经 `create_task` fire-and-forget 调度 `runner.emit`（pi 同款 `void emit`），`has_handlers` 前置过滤。3 条测试（扩展收到事件且字段正确 / 无 runner 不报错 / 无 handler 不调度）。
- **`tool_call` 事件原地改参契约文档化并钉测试（对齐 pi 的 input 原地改参）**：扩展 handler 可原地修改 `ToolCallEvent.args` 直送工具执行（事件对象与 loop 校验后的执行参数共享同一 dict——校验时已 deepcopy，不污染原始 tool_call；DynamicTool 包装层同样引用透传）；多 handler 串行后者可见前者修改；**改后不再二次 schema 校验**（handler 是受信代码）。此前该行为仅靠共享引用碰巧可用、未文档化未测试——任何防御性拷贝重构都会让改参类扩展（sandbox 式命令包装）静默失效。契约写入 `ToolCallEvent` docstring；两条链路测试钉死：nova_agent 环级（hook 改 `ctx.args` → 工具收到改后参数）+ harness 扩展级（改 `event.args` → 调用方同一 dict 被改、后续 handler 可见前者修改）。
- **Esc 两级路由（对齐 pi 的对话框优先语义）**：此前全局键位在焦点路由前吃掉 Esc——对话框开着时 Esc 也会 abort 整个 run（用户只想"拒绝这次询问"却停了全部）。修正：对话框活动（`DialogController.isActive`）时 Esc 归焦点对话框（本地取消=拒绝，run 继续）；无对话框且流式中才 abort run。配合 ScopedUIContext 的 ui/cancel 兜底（abort 来自他处时撤框）。
- **反向原语 abort/超时链路补全**：`ScopedUIContext`（`types/ui/scoped.py`）——**注入点织入 abort 竞速**：runner 的 `create_context` 注入的 ui 现在按当前 run 的 abort signal 作用域化（每次请求现取 signal；abort → transport 发 `ui/cancel` 撤销前端对话框 + cancelled 返回）。修复审查发现的真洞：扩展 handler 的 ui 调用此前不接 signal，Esc abort 会被无人应答的对话框卡住（挂到应答或 300s 超时）；`TransportUIContext.request` 支持 `params.timeout_ms`（pi 对话框 timeout 语义对位：后端强制超时，前端可自渲倒计时）。7 条测试（作用域竞速/显式 signal 覆盖/idle 不竞速/timeout_ms 覆盖默认超时/断线收尾）。
- **ExtensionContext 契约硬化（双源清理 + 响亮失败）**：① `get_model` callable 字段删除——与 `model` property 双源同义（底层同一个 `_get_model`），对齐 pi 的 `ctx.model` getter 语义（属性、访问时现取、单入口）；调用点（session_commands 的 `/model`）迁移完成。② 全部字段改**构造必选**（`@dataclass(kw_only=True)` 无默认值）——此前字段默认 `_noop`，`create_context` 漏接新字段时 handler 调用静默无效果；现在漏接线构造即报错。
- **TransportUIContext 断线 pending 收尾**：新增 `abort_pending()`——传输断开（前端崩溃/断开）时所有在飞 `ui/request` 立即按 cancelled 解决（此前挂到 300s 全局超时，后端协程干等 5 分钟）；`NovaServer.run()` 关闭路径统一调用。2 条测试（在飞请求即刻 cancelled + 空/重复调用安全）。
- **UIContext 泛型化：harness 退为纯 transport，词汇定义权归包**：`UIContext` 删除全部便捷方法（`select`/`confirm`/`input`/`notify_message`），只保留 `capabilities`/`has_capability`/`request(method, params)`/`notify(method, params)`——method 为自由字符串，harness 零词汇；`modes/rpc/primitives.py`（原语 schema + `STANDARD_UI_METHODS`）与 `modes/rpc/types.py`（re-export，零消费者）删除。标准词汇移入官方 bundle：`nova_coding_agent.ui_primitives`（基线四件套定义 + 糖库 `select`/`confirm`/`input`/`notify_message`）；harness 内部消费同步改造（`UIAuthInteraction` 登录交互、project trust 询问改走泛型 `request`）；bundle 两个扩展改喝糖库。设计定案见 `nova-ui-runtime/docs/ui-primitives.md`（request/notify 时序二分即完备；函数不可过线故传函数型 UI 永归 TS 逃生舱；依赖方向决定词汇归属）。conformance 探针（真实后端 select 往返）同步更新并通过。
- **RPC `login` 能力门槛**：`oauth` 需前端宣告 `notify`（展示设备码/授权 URL）、`api_key` 需 `input`（输入密钥）——未满足直接报错拒绝，否则 device-code 流在无 prompt 关卡时会轮询到超时（约 15 分钟）。3 条测试钉住（缺 notify 拒绝且不启动 / 缺 input 拒绝 / 有 notify 放行）。
- **nova-ui-runtime 反向通道地基两件**：`ReverseBridge` 对无 handler 的 `ui/request` **自动应答 cancelled**（NoOp 等价物——后端 300s 超时不再成为常规路径）；`NovaUIRuntimeOptions.capabilities` 默认从四件套全支持改为**空集**（诚实宣告——默认全支持会让后端把 headless 该走的拦截/拒绝错走成"发问后挂起"；M1 前端实现对话框后显式传入，M4 经 `dialog:*` slot 自动宣告）。
- **扩展 API 面收窄为装载期注册面（动作面单源到 ctx）**：`NovaExtensionAPI` / `ExtensionAPI` 协议删除 14 个动作委托方法（`sendMessage`/`sendUserMessage`/`appendEntry`/`setSessionName`/`getSessionName`/`setLabel`/`exec`/`getActiveTools`/`getAllTools`/`setActiveTools`/`getCommands`/`setModel`/`getThinkingLevel`/`setThinkingLevel`——全库零消费者，真实扩展全部走 `ctx`）及配套 `_schedule` 调度器。分工判据按**生命周期**切（替代 pi 按"能力类型"切的轴）：注册不依赖会话（装载期申报）归 `nova`；动作没有活会话不成立（发给谁/改谁的注册表）归代表活会话的 `ctx`——pi 自破规则的例外（`ctx.abort`/`compact`/`shutdown` 恰是动作）在本切法下名正言顺。规则一句话：工厂里只有注册，handler 里只有 ctx。
- **`tool_call` 扩展钩子改 fail-closed（对齐 pi 的安全姿态）**：`ExtensionRunner.emit_tool_call` 此前把 handler 异常吞掉转 error 事件后**放行执行**——拦截类扩展（permission gate 等）崩溃时危险操作会径直穿过门禁。pi 对 tool_call 单独 fail-closed（runner 不捕获、agent-session 重抛为 "Extension failed, blocking execution"），其余事件 fail-open。对齐：tool_call handler 异常现转为 `ToolCallEventResult(block=True, reason="Extension failed, blocking execution: ...")` 并短路剩余 handler，error 事件照常发出便于观测；其他事件类型语义不变。2 个回归测试钉住（异常即 block、短路后续 handler）。
- **skills 白名单改为来源分治（所有权错位修复）**：`agent.yaml` 的 `skills:` 白名单从此**只约束包内 skill**（`source_info.origin == "package"`，agent 作者的人格面）；用户级（`~/.agents`、`~/.nova/agent`）、项目级（祖先 `.agents`、`.nova`）与 CLI/SDK 显式路径的 skill **始终放行**——用户/团队的技能库不再需要包作者授权，项目级安全边界归 project trust。此前"未声明=零"的一刀切让 `.agents` 发现链路名存实亡、coding_agent（`skills: []`）下 skill 系统整体死亡、用户自装/agent 自学 skill 永远不可用。实现收敛为 `core/harness/skills.py` 的 `filter_skills_by_whitelist`（`is_package_skill` 判定），`_get_allowed_skills` 与 `_filter_skills_by_config` 两处统一调用；真实场景验证（coding_agent + 项目级 `.agents/skills` 发现+展开通过）；**全场景真实测试 `tests/core/harness/skills/test_source_semantics.py` 12 用例**（真实文件系统 + `install_and_persist` 真装包持久化 + 真 SDK 会话：四源发现与 scope 归属、trust 只拦 project、包内空名单拦截/命中放行、CLI 显式路径、附录含与不含、`/skill:` 真实正文展开、`disable_model_invocation` 附录排除但可显式调用、同名碰撞 project 胜、包内 agent 白名单解锁同包 skill）+ `test_whitelist.py` 4 用例 + system_prompt manager 三用例重写。
- **`ExtensionMode` 类型全量移除**：`print`/`rpc`/`tui`/`json` 运行模式类型及其传播链（`ExtensionContext.mode`、`ProjectTrustContext.mode`、`ExtensionRunner.mode`、`AgentSession._extension_mode` 绑定链、`bind_extensions` 的 `mode` 键）一并拆除——pi 侧该字段的读者全是 UI widget 扩展拿它当 `hasUI` 用，Python 纯运行时没有这类消费者，且原接线存在"factory 设置后被 `bind_extensions` 覆盖为 `None`"的半接线缺陷。扩展只需两个信号：`has_ui`（有没有前端挂在通道上，构造期即知，trust 早期裁决用它）与 `ui.capabilities`（前端支持哪些原语，连接后协商）。
- **settings 分层：展示偏好保留 schema、删除运行时死访问器**：纯前端消费字段（`terminal`/`markdown`/`hide_thinking_block`/`editor_padding_x`/`autocomplete_max_visible`/`show_hardware_cursor`/`double_escape_action`/`quiet_startup`/`collapse_changelog`/`last_changelog_version` 等）保留在 schema 供 `getSettings`/`updateSettings` 泛型通道 round-trip（运行时只存储不解释）；删除运行时零消费的 typed accessor 约 20 个（`get/set_show_images`、`get/set_clear_on_shrink`、`get/set_double_escape_action`、`get/set_editor_padding_x`、`get/set_autocomplete_max_visible`、`get/set_show_hardware_cursor`、`get_code_block_indent`、`get/set_hide_thinking_block`、`get/set_quiet_startup`、`get/set_collapse_changelog`、`get/set_last_changelog_version` 及配套默认常量）；保留真实消费的 `get/set_block_images`（factory 图片过滤）与 `get/set_image_auto_resize`（read 工具）。
- **analytics 遥测死臂删除**：`is_analytics_enabled`/`report_analytics_event`（零调用方）与 schema 字段 `enable_analytics`/`tracking_id` 及访问器一并移除；install telemetry（`report_install_telemetry`，installer/attribution 真实消费）保留。
- **`SettingsManager.migrate_settings` 整删**：pydantic 加载时忽略未知键，历史/已删字段自然丢弃——TS 时代 camelCase 迁移（`queueMode`/skills 对象格式/`maxDelayMs`）与已删字段 pop 均为第一版不需要的版本迁移代码（加载与保存两个调用点同步移除）。
- **键位体系按架构 2.0 分层**：`core/config/keybindings.py` 与 `core/types/config/keybindings.py`（内置键位表 `DEFAULT_KEYBINDINGS`、keybindings.json 解析、与内置键的冲突检测）删除——键位捕获/绑定/用户自定义/冲突裁决归前端；运行时只保留扩展 shortcut 注册表与 handler（见 Added 的 `getShortcuts`/`invokeShortcut`）。
- **RPC 事件面重构为哑管道（bus2 直通，架构 2.0 落地）**：事件桥不再做呈现加工——`ui_events.py`（整套 UIEvent 类型）、`event_mapper.py`（挑字段/造显示块/发明 spinner 状态与英文文案）、`content.py`（ContentBlock 词汇）与 `listBlockTypes` 方法全部删除；Bus 2 事件经 `rpc/protocol/serialize.py` 直通为 `{type, data}` 信封（全量、不重不漏，仅 JSON 安全兜底）。`getSessionMessages` 升级为 `getSessionEntries`——全保真条目（`id`/`parent_id`/`type`/原始 payload），成为 transcript 重建、树导航（entryId）、标签与 compaction 条目的统一数据源，替代旧的呈现裁剪序列化（只留 text/thinking、丢 tool call、无 entry id）。线上契约 = 运行时事件类型本身，类型发现走构建期 schema 导出（nova-ui-runtime）；呈现模型（delta diff、工具卡片、状态机、渲染器）全部归 Node 层。**注意：nova-tui 消费的是旧映射协议，本次不同步修复（将由 nova-ui-runtime 取代）**。
- **`ui_blocks` 概念全量清除（执行架构 2.0 第 1 步判决）**：该概念是被 2.0 作废的"Python 后端 + RPC 声明式 UI"旧路线残留，Python 纯运行时不应持有任何 UI 词汇。声明层（`Tool`/`UserTool` 类属性、`ToolDefinition`/`DynamicTool`/`ToolInfo`/`UserToolDefinition`/`UserToolResource`/`UserToolInfo` 字段、两个 loader 的 `_normalize_ui_blocks` 与 `_OPTIONAL_ATTRS` 条目、RPC catalog）与数据层（bash/edit 结果 `details["ui_blocks"]` 块形包装、`event_mapper` 的直通分支）一并拆除；bash/edit 结果改为**平铺结构化 details**（`command`/`stdout`/`stderr`/`exit_code`/`duration_ms`/`truncated`/`full_output_path`；edit 增加 `old`/`new` 原文数据，数据零丢失），bash details 的 `returncode`/`exit_code` 双名统一为 `exit_code`。工具心智回归"写 Python、写 details、完事"，自定义渲染归将来复合包 `ui/` 段（Node 层资产）。两份旧 UI 设计稿（`nova_ui_architecture_design.md` / `nova_ui_layer_redesign.md`）加作废碑。
- **LLM 工具哨兵类 `ToolExecutor` → `Tool`**：卸下 executor.py 目录时代的命名包袱，与 `UserTool` 对仗（无限定词 = LLM 工具，限定词只标例外方）；`ToolLoader` 的哨兵查找、加载期诊断消息、bundle 8 个工具与全库测试/文档同步更名。`Tool` 与 `nova_ai.Tool`（LLM schema 类型）处在不同层，工具作者无导入交集。
- **工具上下文注入对齐 pi 调用期语义**：`ToolContext` 拆为构造期（`cwd` + `settings`，不变量）与执行期 `ToolExecContext`（`execute` 第 5 参，冻结值对象每次调用现造）——`get_model()` 访问器与 sdk 的 `model_provider` 后绑定删除，`AgentSessionServices.tool_context` 字段同步移除；**loader 契约改为只产出 `ToolDefinition`**（对齐 pi"加载层定义、会话层包装"），`ToolLoader.load_tools()` 返回 `{name: ToolDefinition}`、不再包装 `DynamicTool`（顺带削除 loader → harness 层的反向依赖），包装与 `context_provider` 注入统一收拢到 `ToolsManager.refresh` 单点，三个来源（包 / SDK custom / override 合成）一视同仁；bundle 8 个工具 `execute` 签名统一加 `ctx` 参数（read 的视觉模型判定改从 `ctx.model` 读取）。
- **会话工具子系统更名为用户工具（user tools）**：`session_tools` → `user_tools` 全库重命名——`[tool.nova]` 类目键、`agent.yaml` 白名单键、目录与模块（`harness/user_tools/`、`loaders/user_tools.py`、`types/resources/user_tools.py`、`agent_session/controllers/user_tools.py`、`rpc/protocol/methods/user_tools.py`）、类名（`UserTool` / `UserToolManager` / `UserToolDefinition` / `UserToolLoader` / `UserToolController`）、RPC 方法（`listUserTools` / `invokeUserTool` / `abortUserTool`）、事件常量（`USER_TOOL`）、消息回载（`register_user_tool_message_types` / `OpaqueUserToolMessage`）。定名对齐 pi 的 `user_bash` 血统："LLM 工具（模型调用）vs 用户工具（用户/前端调用）"一眼二分；会话条目中的消息 role 字符串（如 `bashExecution`）属持久化格式，保持不变。
- **spawn hook 契约抬到 process 层**：`types/extensions/bash.py` → `types/extensions/process.py`（`SpawnContext` / `SpawnHook` / `SpawnHookAware`），扩展 API `registerBashSpawnHook` → `registerSpawnHook`，runtime 字段 `bash_spawn_hooks` → `spawn_hooks`——框架只认识"子进程"这一 OS 抽象，不认识 bash 工具；任何 spawn 类工具实现 `SpawnHookAware` 即可接入。`ToolsManager._inject_spawn_hooks` 鸭子类型遍历不变。
- **bundle 8 个工具迁移到单文件形态并接入 ToolContext**：`tools/<name>/executor.py` → `tools/<name>.py`，`__init__(context)` 签名统一；bash 构造期消费 settings 的 `shell_path` / `shell_command_prefix`（此前 settings 存在但无人消费）；read 经 `get_model()` 实现非视觉模型图片省略提示（对齐 pi `getNonVisionImageNote`）、`auto_resize` 设置接入 `process_image(resize=...)`；全部文件工具路径解析改以 `context.cwd` 为基准（不再隐式依赖进程 cwd）；`user_tools/bash/executor.py` → `user_tools/bash.py`。
- **core 目录归属整理（nova_harness）**：新建 `core/model/` 模型域，收编原 `core/config/model_runtime/`（store/composer/runtime/helpers）、原顶层散文件 `core/model_resolver.py`（→ `model/resolver.py`）与 `core/provider_attribution.py`（→ `model/attribution.py`），`config/` 收敛回真配置；`core/keybindings.py` 归入 `core/config/`；`config/telemetry.py`、`config/http_idle_timeout.py` 两个非配置项移入 `core/utils/`。类型层同步镜像：`types/config/model_runtime.py` → `types/model.py`，`types/bash.py` → `types/extensions/bash.py`（spawn hook 契约归扩展 API 面），`types/keybindings.py` → `types/config/keybindings.py`；`config/__init__.py` 不再转发 `ModelRuntime`（统一从 `core.model` 导入）。
- **工具定义形态统一为"工具即代码"**：LLM 工具的 `schema.json` 与用户工具的 `user_tool.yaml` 全部废除——`tools/<name>/executor.py` 的 `Tool` 类、`user_tools/<name>/executor.py` 的 `UserTool` 类直接以类属性声明元数据（`name` / `description` / `parameters`，可选 `prompt_snippet` / `prompt_guidelines` / `MESSAGE_TYPES`）；用户工具改由 `__init__(session)` 构造注入会话上下文（替代 `create_user_tool` 工厂约定）；两个 loader 的元数据读取改为 import 后读类属性并做加载期校验（缺失/类型错误走诊断通道），消除元数据文件与代码之间的双源真相。
- **框架不内置任何用户工具**：bash 用户工具从 `nova_harness` 外移到 `nova_coding_agent` bundle——`harness/user_tools/bash.py` 删除，`BashExecutionMessage` 移入 bundle 的 `bash/message.py`（随包分发、经注册表复原）；`UserToolManager` 构造时为空注册表，全部定义来自包加载；`AgentSession.execute_bash` / `abort_bash` typed 便捷方法与 RPC 的 `bash` / `abort_bash` 别名同步删除（泛型 `invoke_user_tool` 系列取代）；`types/bash.py` 只保留扩展 spawn hook 契约（`BashSpawnContext` / `BashSpawnHook` / `SpawnHookAware`），`BashResult` / `BashOperations` 随引擎入 bundle；compaction 的 `_is_turn_start_message` 不再硬编码 `bashExecution` role，改为 `ContextInjectable` 多态判定。
- nova_harness：`convert_to_llm` 的自定义消息翻译改为多态（`ContextInjectable` 协议 + `to_context_text()`），原静态分派函数移除。
- 截断与输出累加能力随 bash 引擎外移回归 `nova_coding_agent.tools_common`（`truncate.py`、`output_accumulator.py`、`shell.py`——引擎离开 harness 后 core 对这三个模块零生产消费）；`child_process.py` 留在 `nova_harness.core.utils`（rpc/print 两个 mode 消费，bundle → harness 方向正确）。
- `nova_coding_agent` bash 工具执行器复用 core 的 `get_shell_config` / `kill_process_tree` / detached 跟踪，并新增 `set_spawn_hook` 接收扩展 hook 注入。

### Fixed
- nova_harness：`ServerState.dispose_runtime` 从未真正释放 runtime——`AgentSessionRuntime.dispose` 是 async 方法却被同步调用（coroutine 从未 await，扩展 `session_shutdown` 事件与 `session.dispose()` 从不执行，黑盒一致性套件经 RuntimeWarning 逮出）；`dispose_runtime` 改 async 并兼容同步实现，全部调用点（createSession/dispose/shutdown/rpc cli 退出路径）同步修正。
- nova_harness：RPC 层三处修复——`methods/ui.py` 死占位删除（`ui/response`/`system/capabilities` 由 `NovaServer._handle_ui_inbound` 分派前直管，占位 handler 永不可达）；字符串 `setModel` 不再绕过会话 `model_runtime`（原实现每次新建 `AuthStorage`+`ModelRuntime` 从磁盘解析，扩展注册的 provider 查不到）；`setThinkingLevel` 非法级别从静默归一为 medium 改为显式枚举校验报错。
- nova_harness：package RPC 修复——`_package_manager` 的 `local` 死参移除；缺参从裸 `KeyError` 改为 `INVALID_PARAMS`；`pkgInstall`/`pkgList`/`pkgUninstall`/`pkgInfo` 经 `asyncio.to_thread` 执行，修复同步阻塞导致 pip 安装期间 abort/反向原语应答全冻结的问题。
- nova_harness：`AgentSession.abort()` 补 `abort_branch_summary`——navigateTree 触发的分支摘要此前无任何取消入口。
- nova_harness：扩展系统 `registerMessageRenderer` 死链清除——注册 API → `Extension.message_renderers` 字段 → `runner.get_message_renderer()` 访问器全程 write-only 零消费者（ui_blocks 同类的 UI 残留）；`ResourceCollision.resource_type` 的 `"theme"` 死值同步移除。
- nova_harness：RPC `getCommands` 的 skills 合并修复——原代码调用 `resource_loader.getSkills().skills`（TS 驼峰残留 + dict 属性访问），会话激活时该方法必抛 AttributeError；改为 `get_skills().get("skills", {})` 并补三源合并回归测试。
- nova_coding_agent：read 工具图片路径修复——`ImageContent.data` 需要 base64 字符串，此前直接传 bytes 导致图片读取必然校验失败（该路径此前无真实图片测试覆盖）。
- nova_coding_agent：subagent 项目级 agent 发现双重修复——目录从 `<cwd>/.nova/agent/agents` 纠正为 `<cwd>/.nova/agents`（`.nova/agent/` 中缀只属于全局目录）；`runner.py` 延迟导入的 `load_agents` 原本不存在（调用即 ImportError），已在 `agent_config.py` 补齐实现并附 scope 语义回归测试。
- nova_harness：会话 bash 执行器质量修复——截断方向改为保留尾部、超长输出写入临时文件并回填 `full_output_path`、abort 时 kill 整个进程组。
- nova_harness：会话 bash 对齐 pi bash-executor 剩余差距——进程退出检测改为进程级（`proc.returncode` 轮询），修复 asyncio `proc.wait()` 连管道一起等导致后台孙进程继承管道时调用悬挂的问题（对齐 pi `waitForChildProcess` 的空闲宽限语义，放弃读取时显式关闭 transport）；abort 升级为 SIGTERM → 宽限 → SIGKILL（trap 进程不再变僵尸）；新增 cwd 预检。
- nova_harness：二进制管理修正——仅声明 `binary_managed_dependencies`/`binary_system_dependencies`（无 pip 依赖）的包不再被跳过（Phase 1 进入条件补齐）；spawn env 的 PATH 前置顺序与 `resolve_binary` 解析优先级对齐（env bin → nova bin）；自管理注册表补 Linux musl 资产与 libc 检测（Alpine 等 musl 发行版可用）。
- `nova_coding_agent` 工具链全面对齐 pi（edit/grep/find/ls/read/write）：
  - **edit**：重写编辑语义为 pi 形态（新增 `tools_common/edit_engine.py`）——`oldText` 必须在原文唯一（多处出现报错）、全部 edit 针对原文匹配且重叠拒绝、找不到/空 oldText/无变化整个调用报错不写盘（原子性）、fuzzy 匹配兜底（NFKC/智能引号/破折号/行尾空白归一，未触及行保留原始字节）、补 `prepareEditArguments` 鲁棒性（edits 为 JSON 字符串、旧式顶层 oldText/newText）；替换旧的"多次出现全部静默替换 + 顺序应用 + 部分失败静默"危险实现；
  - **grep**：参数面对齐 pi（`pattern`/`path`/`glob`/`ignoreCase`/`literal`/`context`/`limit`，默认区分大小写）；修复 schema 与 executor 漂移（`literal`/`context`/`limit` 此前不可见）；context 由"传 `rg --context` 但解析丢弃"改为按行号自渲染（匹配行 `path:N:`、上下文行 `path-N-`）；补长行截断（500 字符）与总输出 50KB 截断（此前 minified 文件单条匹配可打爆上下文）；`--hidden` 包含隐藏文件；
  - **find**：fd 调用修复——带 `/` 的 pattern 走 `--full-path`（此前 `src/**/*.spec.ts` 静默失配）、`--hidden`、`--no-require-git`（仓库外）、空 pattern 不再把搜索根误当正则；输出相对搜索根（posix）替代绝对路径；保留 Nova 超集（`type=directory` 目录查找 + rg/Python 兜底）；
  - **abort 全线接线**：edit/write 步骤间检查、find/grep 子进程 abort kill、ls/read 入口检查（此前全部不响应中止）；
  - **read**：图片经 Pillow 缩放到 2000x2000 内（对齐 pi `processImage`，新增 `Pillow` 依赖与 `tools_common/image.py`）；
  - **ls**：schema default 修正为 500（与代码一致）、空目录输出 `(empty directory)`；
  - **write**：`content` 缺参显式报错（此前静默写入空串）；
  - 删除 `is_path_traversal` 半透检查（挡正当的 `../sibling` 却放绝对路径，pi 不做限制）；`resolve_path` 支持 `~` 展开。

### Fixed
- **nova_harness：auto-compaction 重试路径崩溃修复**——压缩重建状态时，已落盘的截断回复（`stop_reason="length"`）可能随保留窗口还原为 assistant 尾，随后 `continue_()` 拒从 assistant 尾续跑抛 `RuntimeError`；收尾守卫由只剥 `error` 尾扩为 `error`/`length` 同剥。该 bug 此前潜伏（1M 窗口模型下阈值压缩实际不触发），由集成测试小窗口克隆模型暴露。配套加固：集成测试修三个前提腐烂（`prepare_next_turn` hook 签名跟上 `(ctx, signal)` 契约、超窗用例克隆小窗口模型不再硬灌 1M token、/debug 模板断言对齐英文模板 + 具体案例）；新增 `length` 尾剥离回归单测；新增两个 PTY 端到端加固脚本（`pty-auto-compaction.py`：沙盒小窗口模型真实触发压缩并验证续跑；`pty-auto-retry.py`：死端点 provider 验证重试倒计时与耗尽收尾）。
- **nova_harness：无摘要导航的 SessionTreeEvent 校验崩溃修复**——`navigate` 未生成摘要时给 `from_extension`（`bool`）传 `None`，pydantic 校验爆炸导致导航半完成（leaf 已迁移、状态已重建，但事件发射崩溃、RPC 返回 -32603、前端转录不同步且后续提交被吞）；改为传 `False`（语义等价：非扩展摘要）。由新增 PTY 脚本 `pty-tree-navigate.py` 实逮（/tree 真实导航 + 编辑器回填 + 分支续写 + /fork 分叉全链路）；新增 `TreeNavigator` 编排层单测套件 12 例（此前仅 1 例）。

### Changed
- **官方 bundle 拆分为 `nova-base` + `nova-coding-agent`**（基础设施 / 执行能力分层）：`nova-base` 收会话产品基础设施（`session_commands` 21 命令、`tools_panel`、`confirm_destructive`、`question`/`todo` 工具、`ui_primitives` 原语糖库、6 个 slash 命令 UI 与 question/tools 对话框、todo/question 渲染器）；`nova-coding-agent` 收编程执行（8 工具、子代理、persona/prompts、四个 gate 扩展、工具渲染器与 interactive-shell 对话框），经 `requires = ["nova-base"]` 声明包间依赖（装/卸自动校验）。安装基线变为双包：`nova-pkg install` 两个包。

### Added
- **打包形态基建（二进制分发的后端四件）**：①统一入口分发器 `cli/backend.py`——`nova-server [rpc|run|pkg]`（裸跑缺省 rpc；pip 渠道的 `nova-harness`/`nova-harness-rpc`/`nova-pkg` 三启动器不受影响，挂同一批 main）；②子代理冻结兼容——`sys.frozen` 时自调从"解释器 + `-m` 模块调用"切换为"二进制自身 + `run` 子命令"；③内建官方包通道（`core/package/builtin.py`）——冻结形态首启把随包携带的 nova-base 落地到 `~/.nova/agent/builtin/nova_base/` 并登记进 settings 包清单（幂等、版本升级重落地；nova-base 受基础包守护不可卸载——卸载即失去会话基础设施），之后与正常 path 包同机制；产品定案：壳（nova-base）内建、编程能力（nova-coding-agent）按需 `nova-server pkg install`；④冻结形态依赖落点（`.site/`）——`FrozenSiteBackend`（`pip install --target <base>/packages/.site/`，宿主 python 经 `NOVA_PYTHON` > 系统 python3（校验大版本 + pip 可用）探测，无宿主时装包不阻断、警告带指引）+ `runtime_paths.ensure_package_paths`（装配时把 `.site/` 与各包 `backend/` 挂进内嵌解释器 sys.path，append 序——包不能遮蔽 nova_* 内建模块）。
- **前端二进制可编译性（bun --compile 适配，spike 实证 62MB/冷启 0.04s）**：`resources/loader.ts`——jiti 改 `jiti/static` 导入（静态引入 babel transform，否则打包丢掉）；顶层 `createRequire().resolve` 惰性化 + `isBunBinary` 检测；bun 形态 jiti 走 `virtualModules` 直供宿主模块（pi-tui + nova-tui 根 + 6 个 `modes/tui/*` 子路径的静态 import 锚点），非 bun 形态保持 dist 别名锚定不变。
- **后端发现链（`wire/backend-command.ts` 的 `resolveBackendCommand`）**：`NOVA_BACKEND` 显式指定 > 同目录 `runtime/nova-server[.exe]`（打包形态随行后端）> `NOVA_PYTHON` > `python3 -m nova_harness.modes.rpc.cli`；TUI spawn 点改走该解析。
- **npm 自愈加固**：`packages/npm.ts` 重写——后台任务不阻塞加载路径（loader 本轮诊断降级"依赖补装中"，完成经 `onNpmHealed` 回调由 runtime 刷新 + 转录通知）、同目录 in-flight 去重、有 lockfile 走 `npm ci --omit=dev`（可复现）否则 `npm install --omit=dev`、`NOVA_OFFLINE` 跳过；时长归 npm 自身 fetch 超时/重试管（不设强制外层超时，防误杀大包）。

### Fixed
- **/packages 面板卸载/更新参数键名错位修复**：面板发 {name}，后端严格形状要 nameOrSource（router 类型化契约收紧后的遗留失配）——面板卸载/更新此前必败（-32602 字段缺失）；改按线上契约发 camelCase，新增 PTY 端到端覆盖（pty-packages-panel.py：双包列表 → 面板卸载 → 通知 + 列表摘除）。
- **官方基础包卸载守护**：nova-base 不可卸载（卸载即失去 21 个 slash 命令/question/todo 工具/UI 原语——会话基础设施），PackageManager.uninstall 基础包守护（先于 requires 守护）+ /packages 面板对其不提供卸载动作。

### Fixed
- **运行中工具卡片 running 双行去重**：通用回退在宿主计时行（ElapsedLine）在场时不再输出 running… 占位（此前叠出 running… + Running… Ns 两行同义文本）；subagent 渲染器自渲的 running… 行同步摘除（渲染器纪律：运行态由自身内容表达——图标/进度，计时归宿主 chrome），question 的 waiting for answer… 保留（等用户作答是语义信息非运行指示）。新增工具卡片真实渲染矩阵 PTY（pty-tool-cards.py：真实模型逐工具调用验卡片签名）。

### 早期雏形（2026-04，当时未封版）

### Added
- nova_ai：LLM 统一抽象层、流式接口、模型注册表、内置厂商支持（OpenAI、Anthropic、Google、Volcengine）。
- nova_agent（nova_agent）：Agent 核心、事件系统、工具校验、异步循环。
- nova_harness：AgentSession、会话树管理、上下文压缩、资源加载。
