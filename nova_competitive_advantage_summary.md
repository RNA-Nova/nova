# Nova 竞争优势分析总结

> 本文档精炼总结 Nova 与主流 Agent 框架/平台的竞争优势，便于路演和投资人沟通。

---

## 一、市场格局：Nova 在哪里？

```
通用框架 ──┬── LangChain / LangGraph
           ├── AutoGen（微软，对话式多 Agent）
           ├── CrewAI（角色扮演多 Agent）
           ├── OpenAI Agents SDK（handoff 编排）
           ├── Google ADK（层次化 Agent 树）
           └── Semantic Kernel（微软企业级）

数据/RAG ──┬── LlamaIndex

应用平台 ──┬── Dify / Coze / FastGPT

Coding Agent ──┬── Kimi Code CLI
               ├── Codex CLI
               ├── Codex Desktop App
               └── Claude Code

个人助理 ──┬── OpenClaw

企业级科学计算 Agent 运行时 ──┬── **Nova**
```

**Nova 的独特位置**：不是通用框架，不是模型应用，不是低代码平台，而是**面向生物科技等企业科学计算场景的专用 Agent 运行时**。

---

## 二、Nova 的六大核心优势

### 优势 1：自研可控的 Agent 运行时

**什么含义**：Nova 从零自研 `agent_loop`、事件系统、工具执行、状态管理，不是基于 LangChain/Kimi/Codex 包装。

**为什么突出**：
- 核心代码在手里，可按业务需求任意修改
- 不会被第三方框架版本变化绑架
- 对生物研发的高风险、高合规场景至关重要

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| LangChain | 抽象层次深，定制困难 |
| Kimi/Codex | 黑盒应用，无法控制内部逻辑 |
| Dify | 低代码平台，灵活性受限 |
| AutoGen/CrewAI | 通用框架，不是为垂直场景设计 |

**一句话**：
> 别人在现有框架上改装，我们在自研发动机。

---

### 优势 2：场景化上下文压缩

**什么含义**：按任务类型定制上下文保留策略，coding、文献调研、实验记录、序列分析各有不同的压缩方式。

**为什么突出**：
- 生物研发天然长上下文，token 成本高
- 通用压缩会丢失关键信息
- 已验证 coding 场景，可向生物场景迁移

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| LangChain | 简单 Memory，复杂压缩需自行实现 |
| Codex Desktop | 依赖 OpenAI 长上下文 + 项目记忆，非场景化 |
| AutoGen/CrewAI | 无专门长流程压缩 |
| Google ADK | 依赖 Gemini 长上下文 |

**一句话**：
> 通用框架一刀切压缩，Nova 按场景决定保留什么、丢弃什么。

---

### 优势 3：Bundle 化领域专家定制

**什么含义**：生物信息学家、实验专家用 `agent.yaml` + `schema.json` 就能定义专业 Agent 和工具，无需懂框架底层。

**为什么突出**：
- 真正懂生物的人能造更专业的 Agent
- 研发 know-how 沉淀为公司数字资产
- 框架可被模仿，但积累的 bundle 难以复制

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| LangChain | 需要工程师写代码 |
| Dify | 低代码，但灵活性不足 |
| OpenClaw | Skill 偏个人助理，不适合科学计算 |
| MetaGPT | 特化于软件开发 |

**一句话**：
> Nova 让专业的人干专业的事，把领域知识变成可复用的 Agent。

---

### 优势 4：模型中立 + 本地部署

**什么含义**：`nova_ai` 抽象层支持 OpenAI、国产模型、本地私有化模型，数据不出内网。

**为什么突出**：
- 生物数据敏感，不能上云
- 国产模型成本更低
- 避免被单一海外厂商锁定

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| Kimi Code | 绑定 Moonshot |
| Codex CLI/Desktop | 绑定 OpenAI，云端处理 |
| Claude Code | 绑定 Anthropic |
| Google ADK | 绑定 Gemini/Vertex AI |
| Semantic Kernel | 绑定 Azure/OpenAI |

**一句话**：
> 别人的缓存和优化只服务自家模型，Nova 的架构对所有模型都友好。

---

### 优势 5：事件驱动全链路可观测

**什么含义**：Agent 的每一步操作都发出事件，可实时观测、干预、审计。

**为什么突出**：
- 生物实验室需要严格监控 Agent 行为
- 工具调用前可拦截危险操作
- 满足合规审计要求

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| LangChain | LangSmith 是付费外部工具 |
| Codex Desktop | OpenAI Traces，数据在云端 |
| AutoGen | 消息日志，可观测性有限 |
| Dify | 平台内日志，不够灵活 |

**一句话**：
> Nova 的运行时是透明的，别人的运行时是黑盒。

---

### 优势 6：跨模型缓存优化潜力

**什么含义**：由于模型层抽象和事件驱动架构，可以在 `nova_ai` / `nova_harness` 层实现统一的跨模型缓存。

**为什么突出**：
- Kimi 的缓存只对 Kimi 模型有效
- Codex 的缓存只对 OpenAI 模型有效
- Nova 的缓存可以对所有接入模型生效
- 生物研发中重复检索、重复分析多，缓存价值大

**与竞品对比**：

| 竞品 | 问题 |
|------|------|
| Kimi/Codex | 缓存优化绑定自家模型 |
| LangChain | 需自行实现缓存 |
| OpenAI SDK | 依赖 OpenAI 内部优化 |

**一句话**：
> 别人的缓存是模型厂商的福利，Nova 的缓存是架构层面的能力。

---

## 三、按竞品类别对比

### vs 通用框架（LangChain / AutoGen / CrewAI / OpenAI SDK / Google ADK）

| 维度 | 通用框架 | Nova |
|------|---------|------|
| 目标 | 解决所有场景的 Agent 编排 | 解决生物科学计算场景 |
| 抽象 | 通用组件 | 自底向上分层运行时 |
| 上下文 | 通用 Memory | 场景化压缩 + 会话树 |
| 垂直适配 | 需大量定制 | 原生设计 |
| 本地部署 | 部分支持 | 第一优先级 |

**核心差异**：通用框架试图"大而全"，Nova 选择"小而深"。

---

### vs Coding Agent（Kimi Code / Codex CLI / Codex Desktop / Claude Code）

| 维度 | Coding Agent | Nova |
|------|-------------|------|
| 定位 | 编程辅助工具 | 科学计算 Agent 运行时 |
| 模型绑定 | 深度绑定单一厂商 | 模型中立 |
| 部署 | 云端/本地终端 | 本地私有化优先 |
| 场景 | 写代码 | 靶点调研、实验、生物信息学 |
| 可扩展性 | 应用级 | 框架级 |

**核心差异**：Codex Desktop 有了多 Agent 和项目记忆，但它仍是 OpenAI 的 coding 产品；Nova 是企业自研的科学计算基础设施。

---

### vs LLM 应用平台（Dify / Coze）

| 维度 | 应用平台 | Nova |
|------|---------|------|
| 目标用户 | 非技术人员快速搭应用 | 领域专家 + 工程师共建 |
| 灵活性 | 受平台限制 | 完全可控 |
| 长流程 | 有限 | 原生支持 |
| 场景化压缩 | 无 | 有 |
| 本地部署 | 部分支持 | 原生支持 |

**核心差异**：Dify 让不懂技术的人快速出 demo，Nova 让懂科学的人造专业系统。

---

### vs 个人助理框架（OpenClaw）

| 维度 | OpenClaw | Nova |
|------|---------|------|
| 场景 | 个人助理 / 消息平台 | 企业科学计算 |
| 技术栈 | TypeScript/Node.js | Python |
| 长流程 | 不适合 | 原生支持 |
| 工具链 | 生活/工作流自动化 | 实验设备 / 生物信息学 |

**核心差异**：OpenClaw 是个人生活的数字员工，Nova 是生物研发的生产工具。

---

### vs RAG 框架（LlamaIndex）

| 维度 | LlamaIndex | Nova |
|------|-----------|------|
| 定位 | 数据连接与 RAG | Agent 运行时 |
| 关系 | Nova 可将其作为工具集成 | 可以集成 LlamaIndex 做检索 |

**核心差异**：LlamaIndex 是 Nova 可能用到的工具之一，不是竞品。

---

## 四、投资人常问的三大问题

### Q1：为什么不用 LangChain？

**回答**：
> LangChain 是通用框架，试图解决所有问题，但也意味着它不懂生命科学。我们需要的是场景化压缩、长流程可观测、实验设备可编排、本地部署可控，这些在 LangChain 上要么没有，要么需要大量定制。而且 LangChain 版本变化快，会被框架演进绑架。

### Q2：Codex Desktop 已经有项目记忆和多 Agent 了，Nova 还有什么优势？

**回答**：
> Codex Desktop 确实很强，但它是 OpenAI 的 coding 产品，深度绑定 OpenAI 模型和云服务，数据在云端处理。Nova 是模型中立、本地部署的运行时，承载的是靶点调研、自动化实验、生物信息学分析，不是写代码。我们的数据不能上云，我们的场景也不是 coding。

### Q3：你们比 Dify 强在哪里？

**回答**：
> Dify 适合快速搭建简单 LLM 应用，灵活性和可控性不够。Nova 是底层运行时，我们有完全的代码控制权，可以根据生物研发的特殊需求做深度定制。而且 Dify 没有场景化压缩、没有事件驱动可观测、没有我们这种领域专家 bundle 机制。

---

## 五、Nova 的不可替代性

Nova 的核心竞争策略不是"比所有框架都好"，而是在这个细分赛道建立不可替代性：

> **为需要高度可控、本地部署、长流程、场景化优化的企业科学计算场景，提供自研的 Agent 运行时。**

这个赛道的关键成功因素：

1. **可控性** — 自研运行时
2. **场景适配** — 场景化压缩 + 垂直 bundle
3. **合规性** — 模型中立 + 本地部署
4. **组织能力** — 领域专家定制 Agent
5. **可观测性** — 事件驱动全链路审计
6. **成本效率** — 跨模型缓存优化潜力

---

## 六、一句话总结

> **LangChain 们做通用工具箱，Kimi/Codex 做模型应用，Dify 做应用平台，OpenClaw 做个人助理，而 Nova 做的是属于我们自己的、为生物研发优化的 Agent 操作系统。**

---

## 七、路演建议

1. **先讲赛道定位**：Nova 不是通用框架，是企业科学计算 Agent 运行时
2. **再讲三大核心优势**：自研运行时、场景化压缩、Bundle 专家定制
3. **补充合规与安全**：模型中立、本地部署、事件驱动可观测
4. **最后讲成本优化**：跨模型缓存潜力
5. **诚实面对成熟度**：Alpha 阶段，但架构优势难以模仿

**时间分配**：前三个优势占 70%，其余占 30%。
