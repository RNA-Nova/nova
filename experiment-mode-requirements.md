# /experiment 模式需求说明书（测试）

> 长任务（AI for Science 多步骤计算）场景下的自动记忆与审计系统。
> 本文档为设计基线，随讨论演进；代码实现以本文档核对后的版本为准。
> 目标分支：legacy/0.1.x（个人开发分支，不合并 main）。

## 1. 背景与痛点

### 1.1 场景

用户经 nova 执行多步骤长任务，典型为 RNA 设计流水线：给定输入序列，经文献调研、约束分析、序列比对、候选生成、结构预测（boltz 等重计算工具）、结构过滤、排序、报告等若干步骤，筛选出 top N 条序列。这类任务的共性：

- 步骤多（约 8~10 步），每步有独立脚本与配置文件；
- 参数多且单步计算成本高；
- 同一流程会被反复执行（不同目标、不同配体）。

### 1.2 痛点

1. **参数漂移**：skill 执行时参数偏离配置文件（先例：CLI 默认值遮蔽 YAML 配置）；
2. **静默缩水**：重计算工具因运算量自动缩小计算范围（如输入 300 条序列只计算 100 多条便进入下一步），产出不可用且不易察觉；
3. **记忆靠人肉**：现有记忆体系需要用户每次用提示词驱动 agent 记录与读取，不可持续；
4. **效果不可度量**：无法回答"引入记忆后任务成功率是否提升"。

## 2. 总体设计原则

| 原则 | 说明 |
|---|---|
| **零硬编码** | 代码中不得出现任何具体 skill 的领域信息（脚本名、配置名等）；一切 schema 在绑定时从用户提供的 skill 现场推导。示例项目（rnaclaw_demo_en）仅作测试夹具 |
| **框架不动** | 全部实现落在 `bundles/nova_coding_agent` 扩展层；`packages/` 框架代码零改动 |
| **跟随项目** | 记忆与审计数据写入任务所在项目的 `memory/`，可 git 追踪、Obsidian 兼容 |
| **严格模式** | 未绑定合格 skill 不允许开启模式，避免产生无步骤归属的低质量记录 |
| **分层选型** | 记忆 = Markdown + frontmatter；审计流水 = JSONL；不使用 SQLite |
| **观测优先** | 记录以实际观测为准（看见什么记什么）；schema 只提供判定基准，提取不全的后果是"少检查"而非"记错" |

## 3. 术语

- **skill（多步骤长任务技能）**：用户提供的能力包，资格判据见 4.2；
- **步骤 schema**：绑定 skill 时从其步骤文档推导出的结构化定义（步骤清单、脚本、配置、期望产物、终产物声明）；
- **情景记忆**：`memory/tasks/` 下每次任务一页的完整档案；
- **语义记忆（learnings）**：`memory/learnings/` 下从任务中蒸馏的坑与规避规则；
- **召回**：任务开始前把相关记忆注入模型上下文的机制；
- **审计**：对每次任务/步骤的耗时、token、缓存命中率、成功率的量化采集。

## 4. 功能需求（一期：最小闭环）

> 目标：只要 `/experiment` 开着，每次实验自动留一份完整可信的档案；每次新实验自动带着历史经验上场。全程无需用户写任何关于记忆的提示词。

### 4.1 模式开关与持久化

- `/experiment` 命令切换模式；
- 模式状态与 skill 绑定经会话条目持久化，分支/恢复/重启后状态不丢；
- footer 状态条显示模式与当前绑定的 skill 名；
- 支持快捷键与启动旗标（可选，同 plan_mode 惯例）。

### 4.2 skill 发现与绑定

- 候选扫描位置：项目 `.agents/skills/`、nova skills 资源（已安装包 + 项目散养）；
- 资格判据（"多步骤长任务"标准）：
  1. 存在 `docs/` 目录；
  2. 其中含 ≥2 个编号步骤文档（如 `00_*.md`、`01_*.md`）；
  3. 步骤文档中可找到可执行脚本（`.py`）引用；
- 绑定交互：
  - 0 个候选 → 提示未找到并附放置指引，拒绝开启（严格模式）；
  - 1 个候选 → 直接绑定并通知（可用 `/experiment <名>` 改绑）；
  - ≥2 个候选 → 弹选择器让用户挑选；
  - `/experiment <skill名>` 直接指定绑定；
- 绑定状态按项目持久化，后续会话自动沿用。

#### 4.2.1 任务时的绑定决策链（提示词驱动优先）

模式下每次用户提交任务提示词时（`input` 事件），按以下优先级决定本任务使用的 skill：

1. **提示词点名**：解析输入文本，若提及某个合格 skill 名 → 本任务使用之；与持久绑定不同时询问是否切换默认绑定；点名但不合格/不存在 → 提示并列出当前候选；
2. **未点名 + 已有持久绑定** → 沿用绑定，任务页注明所用 skill；
3. **未点名 + 无绑定** → 现场扫描候选：
   - 1 个 → 直接绑定并通知；
   - ≥2 个 → 暂停任务弹选择器（选定后任务继续）；
   - 0 个 → 提示用户提供合格 skill（说明放置位置与资格判据），任务不启动。

即：`/experiment` 激活时的绑定是兜底，提示词点名是常态路径；任务页始终记录本任务实际使用的 skill 名。

### 4.3 步骤 schema 现场推导

绑定瞬间解析该 skill 的全部步骤文档，从任意 skill 的 md 文本提取：

| 提取项 | 来源 | 用途 |
|---|---|---|
| 步骤编号/名称 | 文档文件名（`04_generate.md` → 步骤 04） | 步骤边界 |
| 脚本清单 | 文档中 `scripts/xxx.py` 引用 | 步骤归属：bash 命令命中脚本名 → 归该步 |
| 配置文件 | 文档中 `*.yaml` 引用 | 参数快照对象 |
| 产物目录与清单 | 文档中 `outputs/...` 路径与文件名引用 | 期望产物，供完整性检查 |
| 终产物与计数方式 | 最后一步产物声明（如 CSV 行数） | 供数量符合性检查 |

推导结果落盘为 `memory/system/skill_schema.json`，后续记录/守卫/审计统一消费该文件。

### 4.4 memory 骨架现场初始化

首次激活时在项目根创建（模板为 bundle 自带的领域无关资源文件）：

```
memory/
├── INDEX.md              # 总索引（含 Dataview 表）
├── README.md             # 分层说明与写入约定
├── _templates/           # task/learning/playbook/discovery 等通用模板
├── tasks/                # 情景记忆
├── learnings/            # 语义记忆
├── playbooks/            # 程序记忆
├── discoveries/  hypotheses/  literature/  entities/
└── system/skill_schema.json
```

### 4.5 自动记录（核心）

- **任务开始**：参数快照——实际生效的 yaml 全文 + 命令行覆盖值，一并存档；
- **每步结束**：产物盘点——产物文件清单 + 序列/记录行数；输入规模承上一步产出；
- **任务结束**：生成 `memory/tasks/<task_id>.md` 情景记忆页并更新 INDEX.md；
- 记录触发全部来自框架事件（tool_call / tool_execution_end / agent_end 等），不依赖用户或模型的自觉。

### 4.6 成功/失败判定（四条标准同时满足）

1. 该步所有应调用的工具均调用成功（无失败 tool_result）；
2. 产物完整性：输出文件数量与种类对照期望清单不缺项；
3. 终产物数量符合用户提示词要求（如 top 20 即恰好 20 条，不多不少）；
4. 无中断（agent_end 正常触发而非 abort）。

一期仅记录判定结果，不做主动拦截。

### 4.7 任务前自动召回

- 触发点：`before_agent_start` 事件（每次 agent 循环启动前）；
- 内容：绑定 skill 的步骤文档摘要（无 playbook 先验时的降级形态）+ 全部 learnings 按严重度排序的一行式摘要；
- 注入形态：`display=False` 的上下文消息（模型可见、界面不刷屏），`custom_type="experiment-memory"`；
- 保鲜：每轮重新注入 + 经 `context` 事件滤除旧注入，防止压缩丢失与重复堆积。

## 5. 功能需求（二期：审计系统）

> 目标：量化每次任务的代价与效果，使"记忆系统是否有效"成为可回答的问题。

- 逐步耗时（tool_execution_start/end）；
- 逐步 token 消耗与缓存命中率（after_provider_response / turn_end 的 usage；各 provider 字段口径实现时验证）；
- 任务成功率统计（支持"有召回 vs 无召回"对比）；
- 双形态输出：`memory/audit/<task>.jsonl`（结构化流水，供统计画图）+ 任务页内汇总表（人类可读）。

### 5.1 对照实验支持（有召回 vs 无召回）

- **记录/召回双开关**：记录/审计与召回注入相互独立——`/experiment recall on|off` 控制召回（记录照常），档位按项目持久化，footer 显示当前档位（如 `🧪 exp·no-recall`）；
- **对照组不丢数据**：召回关闭时记录与审计照常工作，保证对照组同样产出可统计的数据；
- **盲态拦截**：召回关闭时 `tool_call` 拦截器禁止 read/grep/ls 等工具访问 `memory/` 路径，防止模型自发阅读记忆污染对照组；记录写入不受影响；
- **分组字段**：审计流水每条记录携带 `recall_enabled: true/false`，对比统计按该字段分组（成功率、耗时、token）；
- **实验规程建议**：可比任务交替分配两组、每任务新会话，避免时间混淆与上下文残留。

## 6. 功能需求（三期：主动防御与经验沉淀）

- **缩水守卫**：某步产物规模相对输入异常缩水时当场拦截并告警（如 300 进 100 出）；
- **learnings 自动蒸馏**：任务失败后从异常中提炼新 learning 写入 `learnings/`（含严重度与规避规则）；
- **playbook 沉淀**：从多次成功任务中沉淀可复用执行流程（含前置检查清单）。

## 7. 非功能需求

- 模式关闭时零开销（事件钩子首行即退）；
- 记录写盘失败不得中断任务本身（记日志降级）；
- 单测覆盖：`bundles/nova_coding_agent/backend/tests/nova_coding_agent/experiment/`（镜像结构）；
- 注释与文档字符串使用中文，遵循仓库代码风格。

## 8. 代码布局（一期动工范围）

```
bundles/nova_coding_agent/backend/
├── extensions/experiment_mode.py        # 薄编排：模式开关 + 事件接线
└── nova_coding_agent/experiment/
    ├── steps.py                         # 步骤 schema 推导（4.3）
    ├── memory_store.py                  # memory/ 读写与骨架初始化（4.4）
    ├── recorder.py                      # 自动记录与四标准判定（4.5/4.6）
    └── templates/                       # 领域无关的 memory 模板资源
```

## 9. 一期验收标准

1. 新项目（仅有合格 skill、无 memory/）开启模式：正确发现/询问/绑定 skill，自动生成 memory 骨架与 skill_schema.json；
2. 跑一次真实 RNA 设计任务（或重放历史任务）后：`memory/tasks/` 自动新增任务页，参数快照、逐步 I/O 统计、四标准判定齐全；
3. 新会话再发任务：模型上下文含召回内容（playbook 摘要 + learnings）；
4. 全程无任何记忆相关的人工提示词；
5. `nova_coding_agent` 既有 290 个测试全绿 + 新增单测通过。

## 10. 已拍板决策记录

| # | 决策 | 结论 |
|---|---|---|
| 1 | 记忆存放位置 | 跟随项目（`<项目>/memory/`） |
| 2 | 步骤粒度 | skill 的 docs/ 中每个编号 md 为一个步骤 |
| 3 | 成功标准 | 四标准同时满足（见 4.6） |
| 4 | 审计形态 | JSONL + 人类可读汇总，两者都要 |
| 5 | 实施顺序 | 记录+召回（一期）→ 审计（二期）→ 守卫+蒸馏（三期） |
| 6 | 存储引擎 | md + JSONL 分层，不用 SQLite |
| 7 | skill 绑定 | 必需，严格模式；无候选拒绝开启，多候选询问 |
| 8 | 先验缺失处理 | schema 与 memory 骨架现场构建；召回降级为 skill 文档摘要 |
| 9 | 对照实验 | 记录/召回双开关 + 盲态拦截 + `recall_enabled` 分组字段（见 5.1） |
| 10 | 任务时绑定 | 提示词点名优先，持久绑定兜底，无绑定现场扫描/询问/提示（见 4.2.1） |
