# Nova 最突出优势分析

> 本文档从投资人视角提炼 Nova 最突出的竞争优势，按优先级排序，并给出每一点的投资价值和表达话术。

---

## 一、优势优先级总览

| 优先级 | 优势 | 一句话概括 | 投资人价值 |
|--------|------|-----------|-----------|
| **1** | 自研可控的 Agent 运行时 | 不是套壳，真正自研发动机 | 技术壁垒、可控性 |
| **2** | 场景化上下文压缩 | 按任务类型定制压缩策略 | 显著降本、提升质量 |
| **3** | Bundle 化领域专家定制 | 让生物专家自己造专业 Agent | 组织能力、壁垒 |
| **4** | 模型中立 + 本地部署 | 不绑定单一厂商，数据不出内网 | 合规刚需、成本可控 |
| **5** | 事件驱动全链路可观测 | 每一步都可监控、可干预、可审计 | 安全合规、容错能力 |
| **6** | 跨模型缓存优化能力 | 架构层统一缓存，不限于单一模型 | 进一步降本、提高效率 |
| **7** | 多前端生态 | 同一套能力服务不同角色 | 使用范围、落地能力 |

---

## 二、第一优势：自研可控的 Agent 运行时

### 2.1 这是什么？

Nova 从零自研了 Agent 的核心运行时，包括：

- 自研 `agent_loop` 异步循环
- 自研事件系统
- 自研工具执行与校验机制
- 自研状态管理

不是基于 LangChain、Kimi、Codex 做包装或二次开发。

### 2.2 为什么这是最突出的优势？

**1. 真正的技术壁垒**

市面上大多数 Agent 应用都是基于 LangChain 或模型公司 API 做的。Nova 自研运行时意味着：

- 核心代码在自己手里
- 可以根据业务需求任意修改
- 不会被第三方框架的版本变化绑架

**2. 生物研发的高要求**

生物医药研发对 Agent 有特殊要求：

- 工具调用必须可控（不能乱动实验设备）
- 决策路径必须可审计（满足合规）
- 长流程必须可干预（错了能叫停）
- 数据不能上云（本地部署）

通用框架无法优雅满足这些要求，自研是唯一出路。

**3. 投资人容易理解**

"自研发动机"比"在别人车上改装"更有价值。

### 2.3 对投资人的表达

> "我们评估过 LangChain、Kimi Code、Codex CLI，但它们要么抽象过重难以定制，要么绑定单一模型、数据必须上云。所以我们自研了 Nova。自研意味着核心代码在手里，可以根据生物研发的特殊需求——长流程、高风险、强合规——做深度定制。这是真正的技术壁垒。"

---

## 三、第二优势：场景化上下文压缩

### 3.1 这是什么？

Nova 的上下文压缩不是一刀切，而是**按任务类型定制保留策略**：

| 场景 | 保留什么 | 压缩什么 |
|------|---------|---------|
| Coding | 代码修改点、关键错误 | 重复编译输出 |
| 文献调研 | 实体关系、结论 | 原始摘要冗余 |
| 实验记录 | 协议、异常事件 | 常规传感器读数 |
| 序列分析 | 关键参数、最终结论 | 中间文件、日志 |

### 3.2 为什么这是核心优势？

**1. 直接降低成本**

长上下文 = 高 token 成本。生物研发天然长上下文：

- 文献长
- 实验日志长
- 序列数据长
- pipeline 中间结果多

场景化压缩能显著降低模型调用成本。

**2. 直接提升输出质量**

通用压缩会丢失关键信息。场景化压缩保留了每类任务真正需要的信息，Agent 输出更准确。

**3. 已被验证**

coding 场景已经验证了这种思路，可以向生物场景迁移。

### 3.3 对投资人的表达

> "通用 AI 框架把所有上下文当作文本一起压缩，但生物研发不一样：文献要保留实体关系，实验记录要保留协议和异常，序列分析要保留关键参数。我们的压缩策略是场景感知的，coding 场景已经验证，这会直接降低 token 成本并提升输出质量。"

---

## 四、第三优势：Bundle 化领域专家定制

### 4.1 这是什么？

Nova 的 Bundle 机制让领域专家不需要懂框架底层，就能定义专业 Agent：

- 生物信息学家写靶点分析工具
- 实验专家写设备协议工具
- 数据科学家写分析 pipeline

他们只需要：

- `agent.yaml` 定义 Agent
- `schema.json` 定义工具参数
- `executor.py` 实现工具逻辑

### 4.2 为什么这是关键优势？

**1. 让专业的人干专业的事**

最优秀的生物 Agent 应该由懂生物的人定义，而不是由通用工程师猜测。

**2. 组织层面的壁垒**

框架代码可能被模仿，但公司内部积累的 bundle（实验协议、分析流程、文献洞察）是独特的 know-how。

**3. 加速创新**

每个领域专家都可以成为 Agent 的贡献者，创新速度远超中心化开发。

### 4.3 对投资人的表达

> "我们的 Bundle 机制让生物信息学家、实验专家这些真正懂生物的人，不需要懂框架底层，也能定义专业 Agent。这意味着我们的 Agent 会更专业，而且我们的研发 know-how 会沉淀成公司独有的数字资产。"

---

## 五、第四优势：模型中立 + 本地部署

### 5.1 这是什么？

- `nova_ai` 抽象层支持多厂商模型
- 不绑定 OpenAI、Moonshot 等单一厂商
- 支持国产模型和本地私有化部署

### 5.2 为什么这是刚需？

**1. 数据合规**

基因组、化合物、患者数据不能上云。本地部署是硬约束。

**2. 成本控制**

国产模型通常比 GPT-4 便宜一个数量级。

**3. 供应安全**

避免被单一海外厂商断供或涨价。

### 5.3 对投资人的表达

> "Kimi 绑定 Moonshot，Codex 绑定 OpenAI。我们不做这种绑定。Nova 的模型层是抽象的，可以用国产模型，也可以本地私有化部署。这对生物数据敏感、合规要求高的场景是刚需。"

---

## 六、第五优势：事件驱动全链路可观测

### 6.1 这是什么？

Nova 的 Agent 运行时会发出完整的事件流：

- Agent 开始/结束
- 每一轮对话开始/结束
- 消息生成过程
- 工具调用开始/结束
- 工具执行更新

### 6.2 为什么重要？

**1. 可观测**

外部系统可以实时看到 Agent 在做什么。

**2. 可干预**

可以在工具执行前拦截、修改或拒绝。

**3. 可审计**

完整记录决策路径，满足生物医药合规。

### 6.3 对投资人的表达

> "LangChain 的 Chain 像黑盒，输入输出之间发生了什么很难知道。Nova 是事件驱动的，Agent 的每一步都会广播事件。这意味着我们可以监控、干预、审计 Agent 的行为——在生物实验室里，这就是安全底线。"

---

## 七、第六优势：跨模型缓存优化能力

### 7.1 这是什么？

Nova 的分层架构允许在 `nova_ai` 模型抽象层和 `nova_harness` 会话层实现统一的缓存策略：

- 缓存模型响应
- 缓存工具执行结果
- 缓存会话上下文和摘要
- 缓存向量检索结果

由于模型层是抽象的，缓存机制**不依赖于特定模型厂商**，可以跨模型复用。

### 7.2 为什么这是优势？

**1. 竞品缓存通常绑定自家模型**

Kimi Code 的缓存优化主要对 Kimi 模型有效，Codex 的缓存主要对 OpenAI 模型有效。因为它们深度绑定单一模型生态。

**2. Nova 在架构层做缓存**

Nova 的缓存在模型抽象层之上或会话层实现，可以：

- 对 OpenAI、国产模型、本地模型都生效
- 根据任务类型配置不同缓存策略
- 结合场景化压缩，进一步提升效率

**3. 生物研发重复请求多**

- 相同文献被多次检索
- 相同分析 pipeline 被多次运行
- 相同实验协议被多次查询

统一的跨模型缓存可以显著降低重复调用的 token 消耗。

### 7.3 表达边界

需要诚实说明：

- 这是架构层面的缓存优化潜力
- 具体缓存策略正在完善中
- 不夸大已有实测数据

### 7.4 对投资人的表达

> "Kimi 和 Codex 的缓存优化主要服务于自家模型。但 Nova 的模型层是抽象的，我们可以在架构层面实现跨模型的统一缓存。结合场景化压缩，这对生物研发里大量重复的文献检索、pipeline 运行、协议查询能进一步降低成本。"

---

## 八、第七优势：多前端生态

### 7.1 这是什么？

Nova 后端通过 WebSocket 与前端解耦，可支持：

- TUI（研究员）
- Web（管理层）
- 飞书/钉钉/企业微信（实验员/项目经理）

### 7.2 价值

- 不同角色在自己熟悉的工具中使用 Agent
- 同一套后端能力服务全公司
- 降低推广成本

### 7.3 对投资人的表达

> "Nova 后端通过 WebSocket 解耦，可以接入 TUI、Web、飞书、钉钉、企业微信。研究员用 TUI 做复杂分析，实验员在企微接收异常提醒，管理层用 Web 看报告。同一套 Agent 能力触达不同角色。"

---

## 九、优势组合效应

单独看每一项优势都有价值，但组合起来才形成真正的护城河：

```
自研运行时（基础能力）
    +
场景化压缩（成本与质量优势）
    +
Bundle 化（组织与 know-how 壁垒）
    +
模型中立 + 本地部署（合规刚需）
    +
事件驱动（安全可控）
    +
跨模型缓存（进一步降本）
    +
多前端（落地能力）
    ↓
= 一个可长期托付给生物医药研发的 Agent 操作系统
```

---

## 十、与竞品的优势对比

| 优势 | Nova | Kimi/Codex | OpenClaw | LangChain |
|------|------|-----------|----------|-----------|
| 自研运行时 | ✅ | ❌ 黑盒应用 | ⚠️ 有但方向不同 | ❌ 通用框架 |
| 场景化压缩 | ✅ | ❌ | ❌ | ❌ |
| Bundle 领域专家定制 | ✅ | ❌ | ⚠️ Skill 偏个人 | ❌ |
| 模型中立 + 本地部署 | ✅ | ❌ | ✅ | ⚠️ |
| 事件驱动可观测 | ✅ | ❌ | ❌ | ❌ |
| 跨模型缓存 | ✅ | ⚠️ 仅自家模型 | ⚠️ 有限 | ⚠️ 需自行实现 |
| 多前端 | ✅ | ❌ | ✅ | ❌ |

---

## 十一、给投资人的最终话术

> "Nova 有七个突出优势，但最重要的是前三个：
> 
> 第一，我们是自研 Agent 运行时，不是套壳，核心代码在手里；> 第二，我们的上下文压缩是场景感知的，能显著降本并提升生物场景下的输出质量；> 第三，我们的 Bundle 机制让生物专家自己定义专业 Agent，把研发 know-how 沉淀为公司资产。
> 
> 再加上模型中立、本地部署、事件驱动可观测、跨模型缓存优化，Nova 不是一个工具，而是我们生物医药研发的长期基础设施。"

---

## 十二、建议的路演强调顺序

1. **先讲自研运行时** — 建立技术可信度
2. **再讲场景化压缩** — 展示核心技术洞察
3. **再讲 Bundle 化** — 展示组织层面的壁垒
4. **补充本地部署和可观测性** — 解决安全合规顾虑
5. **提跨模型缓存** — 展示进一步降本潜力
6. **最后提多前端** — 展示落地能力

**不要平均用力**，前三个优势应占 70% 的时间。
